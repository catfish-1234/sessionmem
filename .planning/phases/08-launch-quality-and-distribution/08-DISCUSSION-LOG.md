# Phase 8: Launch Quality and Distribution - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-11
**Phase:** 8-Launch Quality and Distribution
**Areas discussed:** CI pipeline scope, Docs gap closure, Benchmark report, npm publish & marketplace submissions

---

## CI Pipeline Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Ubuntu + macOS + Windows | Full major-OS matrix — matches roadmap wording 'major OSes' literally | ✓ |
| Ubuntu + macOS only | Drop Windows — common if Windows native module builds are flaky in CI | |
| Ubuntu only | Single OS, fastest CI | |

**User's choice:** Ubuntu + macOS + Windows

| Option | Description | Selected |
|--------|-------------|----------|
| Single LTS (Node 22) | Simplest — one version | |
| Two versions (20 + 22) | Covers current LTS + active LTS | ✓ |

**User's choice:** Two versions (20 + 22)

| Option | Description | Selected |
|--------|-------------|----------|
| Build + global install + CLI runs | npm pack/install globally, run CLI to confirm binary works | |
| Build + run dist CLI directly | node dist/cli/index.js --version, lighter | |
| Full install command end-to-end | Run `sessionmem install` against temp dir, verify config/hooks | |

**User's choice:** "Verify everything fully" — interpreted as combining all three: build + pack/global install + CLI binary run + full `sessionmem install` end-to-end against a temp dir.

| Option | Description | Selected |
|--------|-------------|----------|
| ESLint (new) | Add ESLint with TS-aware config as new CI gate | ✓ |
| tsc --noEmit only (typecheck as lint) | Treat typecheck as the lint gate | |

**User's choice:** ESLint (new)

---

## Docs Gap Closure

| Option | Description | Selected |
|--------|-------------|----------|
| docs/ folder, matching existing style | Add new docs alongside existing 3 docs | |
| Top-level README + docs/ for deep dives | Create root README.md as entry point, link to docs/ for details | ✓ |

**User's choice:** Top-level README + docs/ for deep dives

| Option | Description | Selected |
|--------|-------------|----------|
| High-level overview + diagram | Conceptual map for contributors/users | ✓ |
| Detailed module map with file references | Walks through src/ structure | |

**User's choice:** High-level overview + diagram

| Option | Description | Selected |
|--------|-------------|----------|
| DB schema migrations (existing migration system) | Document SQLite migration system | |
| User migration between versions (upgrade guide) | How users upgrade sessionmem versions | |
| Both | Cover schema migration internals AND version-upgrade policy | ✓ |

**User's choice:** Both

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, one test per new doc | Mirror existing pattern, one spec per doc | ✓ |
| Yes, single combined launch-docs test | One spec checks all new docs | |
| No | Skip automated doc-coverage tests | |

**User's choice:** Yes, one test per new doc

---

## Benchmark Report

| Option | Description | Selected |
|--------|-------------|----------|
| Compare injected context size vs full-history baseline | Directly demonstrates core value prop | (combined) |
| Measure injection token usage across realistic scenarios only | Simpler, no baseline comparison | (combined) |

**User's choice:** "Whichever one yields a better token reduction" — interpreted as Claude's discretion: run both, present the framing with the stronger reduction story.

| Option | Description | Selected |
|--------|-------------|----------|
| Curated query/expected-memory test set with hit-rate scoring | Precision/recall-style scoring against fixture set | ✓ |
| Qualitative examples only | Representative examples with commentary, no formal scoring | |

**User's choice:** Curated query/expected-memory test set with hit-rate scoring

| Option | Description | Selected |
|--------|-------------|----------|
| docs/benchmark.md, generated by a script (npm run benchmark) | Reproducible, re-runnable | ✓ |
| docs/benchmark.md, written manually from one-off test run | Faster, no script to maintain | |

**User's choice:** docs/benchmark.md, generated by a script (npm run benchmark)

---

## npm Publish & Marketplace Submissions

| Option | Description | Selected |
|--------|-------------|----------|
| Unscoped "sessionmem" | Matches current package.json name | ✓ |
| Scoped, e.g. "@sessionmem/cli" | Avoids name-collision risk | |

**User's choice:** Unscoped "sessionmem" (verify availability during planning/execution; fall back to scoped if taken)

| Option | Description | Selected |
|--------|-------------|----------|
| 1.0.0, MIT license | Bump to 1.0.0 to signal launch-ready | ✓ |
| Keep 0.x (e.g. 0.9.0), MIT license | Signal pre-1.0/early-adopter status | |

**User's choice:** 1.0.0, MIT license

| Option | Description | Selected |
|--------|-------------|----------|
| GitHub Actions release workflow on tag push | Automated publish via NPM_TOKEN on v* tag | ✓ |
| Manual `npm publish` from local machine | Documented but not automated | |

**User's choice:** GitHub Actions release workflow on tag push

| Option | Description | Selected |
|--------|-------------|----------|
| MCP server registries (official list, Smithery, mcp.so) | Most directly relevant since sessionmem is an MCP server | |
| MCP registries + Claude Code plugin marketplace format | Both general MCP registries and Claude-specific format | ✓ |
| Let Claude research and recommend during planning | Defer hub list to research | |

**User's choice:** MCP registries + Claude Code plugin marketplace format

---

## Claude's Discretion

- **QLTY-01 (test coverage):** No specific gray area discussed — researcher/planner to do gap analysis against "core flows and adapters" using existing extensive test suite as baseline, close meaningful gaps.
- **Benchmark token-reduction framing:** Run both baseline-comparison and scenario-based measurements; present whichever is more compelling.
- **npm package name fallback:** If "sessionmem" is unavailable on npm registry, fall back to a scoped name.
- **Specific MCP registries to target:** Researcher to confirm currently active/relevant registries (official MCP servers list, Smithery, mcp.so, etc.) and Claude Code plugin marketplace requirements.

## Deferred Ideas

None — discussion stayed within phase scope.
