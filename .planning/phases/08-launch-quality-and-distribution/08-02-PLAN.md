---
phase: 08-launch-quality-and-distribution
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.mjs
  - tests/integration/adapters/install-parity.spec.ts
autonomous: false
requirements: [QLTY-01, QLTY-02]
user_setup: []

must_haves:
  truths:
    - "`npx eslint .` runs against the TS source and reports no errors"
    - "ESLint uses a flat config (eslint.config.mjs), the only form ESLint 10 supports"
    - "At least one IDE adapter and one global adapter have install() parity tests"
    - "The generic adapter path (PLAT-08) has a smoke test"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Flat ESLint config: js+typescript-eslint recommended, ignores dist/coverage, test-file override"
      contains: "tseslint.config"
    - path: "tests/integration/adapters/install-parity.spec.ts"
      provides: "Adapter install() parity coverage (>=1 IDE + >=1 global) + generic path"
      min_lines: 40
  key_links:
    - from: "eslint.config.mjs"
      to: "package.json"
      via: "lint script + eslint/typescript-eslint devDeps resolve"
      pattern: "eslint"
    - from: "tests/integration/adapters/install-parity.spec.ts"
      to: "src/adapters/ide/installer.ts"
      via: "asserts injectMcpConfig writes config without mutating real HOME"
      pattern: "install"
---

<objective>
Stand up the lint gate (D-03) — ESLint 10 flat config plus the `lint` script and devDeps — and close the two meaningful QLTY-01 coverage gaps the research identified: adapter `install()` parity (≥1 IDE + ≥1 global) and the generic adapter path (PLAT-08).

Purpose: QLTY-02 requires a lint gate (currently none exists anywhere in the repo — greenfield). QLTY-01's "covering core flows and adapters" success criterion has real gaps in per-adapter install paths and the generic-host route; closing them is meaningful coverage, not padding (CONTEXT explicitly forbids padding).
Output: eslint.config.mjs + lint script/devDeps + adapter install-parity integration spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md
@.planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md

<!-- Plan 08-01 already set package.json to publishable + added scripts; this plan ADDS the lint script and eslint devDeps to the same file. -->

<interfaces>
<!-- ESLint 10 flat config shape (RESEARCH Pattern 1, verified against typescript-eslint.io). -->
```javascript
// @ts-check
import js from "@eslint/js";
import tseslint from "typescript-eslint";
export default tseslint.config(
  { ignores: ["dist/**", "node_modules/**", "coverage/**"] },
  js.configs.recommended,
  ...tseslint.configs.recommended,   // non-type-checked = fast for CI
  { files: ["tests/**/*.ts"], rules: { "@typescript-eslint/no-explicit-any": "off" } },
);
```

<!-- Adapter install seam (from src/adapters/global/claudeCode.ts + ide/installer.ts). -->
<!-- IDEInstaller.injectMcpConfig(configPath, "sessionmem", "sessionmem", ["run"]) writes the MCP config; -->
<!-- pass a temp configPath so the test never touches the runner's real ~/.claude.json. -->
<!-- Global adapters: src/adapters/global/{claudeCode,codex,qcoder,antigravity}.ts -->
<!-- IDE adapters: src/adapters/ide/{cursor,cline,windsurf}.ts -->
<!-- Generic: src/adapters/generic.ts (GenericMCPAdapter.call returns an error envelope when not run as a server). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ESLint 10 flat config, lint script, and devDeps; make lint pass</name>
  <files>eslint.config.mjs, package.json, package-lock.json</files>
  <read_first>
    - package.json (current state after Plan 01 — add `lint`/`typecheck` scripts + eslint devDeps here)
    - tsconfig.json (strict TS, NodeNext ESM — the config the linter operates against)
    - .planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md (Pattern 1 + version-compat note: eslint@10.4.1 / typescript-eslint@8.61.0; fallback eslint@9 if peer conflict)
  </read_first>
  <action>
    Install lint devDeps: `npm install --save-dev eslint@10 @eslint/js typescript-eslint`. Run `npm view eslint version` / `npm view typescript-eslint version` first and pin resolved versions. If `npm install` reports an ERESOLVE peer conflict between eslint@10 and typescript-eslint@8, fall back to `eslint@9` (RESEARCH version-compat note — there is no functional requirement forcing v10). Confirm none ship a network postinstall (`npm view eslint scripts.postinstall`).
    Create eslint.config.mjs at repo root using the flat-config shape in the interfaces block: `@ts-check` header, `js.configs.recommended`, spread `...tseslint.configs.recommended` (NON-type-checked for CI speed — do NOT use `recommendedTypeChecked`, RESEARCH anti-pattern), `ignores: ["dist/**", "node_modules/**", "coverage/**"]`, and a `files: ["tests/**/*.ts"]` override disabling `@typescript-eslint/no-explicit-any`.
    Add to package.json scripts: `"lint": "eslint ."` and `"typecheck": "tsc --noEmit"`.
    Run `npx eslint .` and FIX any real errors it surfaces in the source (do not blanket-disable rules to silence them; only the documented test-file override is allowed). If a rule produces noise inconsistent with the established codebase style, downgrade that specific rule to "warn" or "off" with an inline comment explaining why — but resolve genuine errors.
  </action>
  <verify>
    <automated>npx eslint . && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.mjs` exists, exports `tseslint.config(...)`, ignores `dist/**`, and has the `tests/**/*.ts` `no-explicit-any` override.
    - `eslint`, `@eslint/js`, `typescript-eslint` are in `devDependencies` and `package-lock.json`.
    - `package.json` has `"lint": "eslint ."` and `"typecheck": "tsc --noEmit"` scripts.
    - `npx eslint .` exits 0 (no errors).
    - No `.eslintrc*` file is created (ESLint 10 is flat-config only).
  </acceptance_criteria>
  <done>Lint gate works: `npx eslint .` passes with a flat config and a lint script wired.</done>
</task>

<task type="auto">
  <name>Task 2: Add adapter install() parity + generic-path integration tests</name>
  <files>tests/integration/adapters/install-parity.spec.ts</files>
  <read_first>
    - src/adapters/global/claudeCode.ts (install() calls IDEInstaller.injectMcpConfig with a configPath derived from homedir)
    - src/adapters/ide/installer.ts (IDEInstaller.injectMcpConfig / removeMcpConfig — the write seam; check whether configPath is injectable)
    - src/adapters/ide/cursor.ts and one of src/adapters/global/{codex,qcoder}.ts (pick one IDE + one global adapter to exercise)
    - src/adapters/generic.ts (GenericMCPAdapter.call error-envelope behavior for the PLAT-08 path)
    - tests/integration/cli/install.spec.ts (the existing isolated-config pattern: pass a temp path so the test never mutates the real ~/.claude.json — Pitfall 3)
  </read_first>
  <action>
    Create tests/integration/adapters/install-parity.spec.ts covering the QLTY-01 gaps (RESEARCH Gap Analysis):
    (1) At least one IDE adapter (e.g. Cursor) and at least one global adapter (e.g. Codex or QCoder): instantiate the adapter, run its `install()` against an isolated temp config path (use a `mkdtemp` dir and the IDEInstaller config-path seam, OR set HOME/USERPROFILE to the temp dir before instantiation per Pitfall 3 — never write to the real `~/.claude.json`), then assert the MCP config block was written with command `sessionmem` and args `["run"]`. Assert `uninstall()` removes it. This is the adapter-parity coverage Risk #1 ("adapter parity drift") calls for.
    (2) Generic adapter path (PLAT-08): instantiate `GenericMCPAdapter`, call `.call("retrieveMemories", <valid request>)` WITHOUT starting the server, and assert it returns the documented `{ ok:false, error:{ code:"INTERNAL" } }` envelope (the generic host's not-initialized contract). This documents/locks the generic-host route behavior.
    Use the existing vitest + temp-dir conventions; clean up temp dirs in afterEach. The spec must isolate all writes from the runner's real home.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/adapters/install-parity.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/adapters/install-parity.spec.ts` exercises ≥1 IDE adapter and ≥1 global adapter `install()`/`uninstall()` against an isolated temp config path.
    - The install assertion checks the written config contains command `sessionmem` and args `["run"]`.
    - A generic-adapter `.call()` test asserts the `INTERNAL` not-initialized error envelope (PLAT-08 path).
    - No test writes to the real `~/.claude.json` (verified via isolated temp HOME/configPath).
    - The spec passes.
  </acceptance_criteria>
  <done>Adapter install parity (IDE + global) and the generic-host path are covered by a passing integration spec that never mutates real home config.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Installed the ESLint toolchain (eslint, @eslint/js, typescript-eslint) as devDependencies. slopcheck could not run during research (sandbox-denied), so these are [ASSUMED] per the Package Legitimacy Gate and must be human-verified before they ship in the lockfile of a published package.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/eslint , https://www.npmjs.com/package/typescript-eslint , and https://www.npmjs.com/package/@eslint/js and confirm each is the official package (eslint by the ESLint team; typescript-eslint / @eslint/js from their official orgs).
    2. Confirm the installed versions in package.json match real published versions.
    3. Confirm  (and the other two) printed nothing (no postinstall scripts).
    Reply "approved" to continue, or describe the discrepancy to halt.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | eslint/@eslint/js/typescript-eslint devDeps (supply chain) |
| test process → runner home dir | adapter install() writes config; must be isolated to temp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC2 | Tampering | npm install of eslint toolchain | mitigate | [ASSUMED] official packages (registry-verified, from typescript-eslint.io docs); confirm no postinstall via `npm view <pkg> scripts.postinstall`; pinned via lockfile |
| T-08-07 | Tampering | adapter install test writing real ~/.claude.json | mitigate | Tests use a `mkdtemp` HOME/configPath seam (Pitfall 3) so no real host config is mutated |
</threat_model>

<verification>
- `npx eslint .` exits 0.
- `npx vitest run tests/integration/adapters/install-parity.spec.ts` passes.
- `npm run lint` and `npm run typecheck` both succeed.
</verification>

<success_criteria>
- ESLint flat config + lint script in place; `npx eslint .` clean.
- Adapter install parity (≥1 IDE + ≥1 global) and generic path covered.
- No test mutates the runner's real home config.
</success_criteria>

<output>
Create `.planning/phases/08-launch-quality-and-distribution/08-02-SUMMARY.md` when done
</output>
