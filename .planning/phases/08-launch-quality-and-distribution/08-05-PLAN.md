---
phase: 08-launch-quality-and-distribution
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-04"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [QLTY-02]
user_setup: []

must_haves:
  truths:
    - "ci.yml runs lint, typecheck, test, and build on a 3-OS x 2-Node matrix (Ubuntu/macOS/Windows x Node 20/22)"
    - "ci.yml has an install-smoke job that packs the artifact, globally installs it, and runs the binary"
    - "The install-smoke job runs `sessionmem install` against an isolated temp HOME and asserts config is written"
    - "ci.yml is a separate workflow from security.yml (additive, not a replacement)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline: checks matrix + install-smoke matrix"
      contains: "install-smoke"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "runs npm scripts lint/test/build + npm pack on the publishable manifest"
      pattern: "npm (ci|pack|run|test)"
    - from: ".github/workflows/ci.yml install-smoke"
      to: "src/cli/index.ts"
      via: "runs installed `sessionmem --version` (asserts 1.0.0) + `sessionmem install`"
      pattern: "sessionmem"
---

<objective>
Add `.github/workflows/ci.yml` (D-01..D-04): a 3-OS x 2-Node matrix running lint, typecheck, test, build, plus a dependent `install-smoke` matrix that packs the real tarball, globally installs it, runs the binary, and exercises `sessionmem install` end-to-end against an isolated temp HOME. Separate from `security.yml`, which stays as-is (D-04).

Purpose: QLTY-02 requires CI passing lint/typecheck/tests/install-smoke on major OSes. The install-smoke job IS the validation harness for D-02 — it validates the packaged artifact a user actually installs (not the working tree). Depends on the lint config (Plan 02), the publishable package + working MCP server + version fix (Plan 01), and the benchmark script not breaking the build (Plan 04).
Output: .github/workflows/ci.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md
@.planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md

<interfaces>
<!-- Trigger block + action pins to reuse verbatim (from .github/workflows/security.yml). -->
<!-- on: push: branches:[main] ; pull_request: -->
<!-- uses: actions/checkout@v6  (security.yml pins v6); add actions/setup-node@v5 with cache: npm -->

<!-- CLI surface the smoke step exercises (from src/cli/index.ts after Plan 01 version fix): -->
<!-- `sessionmem --version` must print 1.0.0 ; `sessionmem install` writes ~/.sessionmem/config.json + adapter config. -->
<!-- CONSTRAINT: `sessionmem run` is now a REAL stdio server (Plan 01); a smoke step must NOT block on it. -->
<!-- Limit binary-run to `--version`, `ping`, `install`. -->
<!-- Pitfall 3: on Windows os.homedir() reads USERPROFILE not HOME — set USERPROFILE/HOMEPATH (Windows) + HOME (POSIX). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ci.yml checks matrix (lint/typecheck/test/build)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/security.yml (trigger block + actions/checkout@v6 pin to reuse; confirm ci.yml is a NEW separate file, D-04)
    - package.json (the lint/typecheck/test/build scripts the matrix calls — added across Plans 01/02)
    - .planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md (Pattern 2 matrix + Pitfall 2 better-sqlite3 native compile)
  </read_first>
  <action>
    Create .github/workflows/ci.yml as a NEW file (NOT an edit to security.yml — D-04). Name it CI. Reuse security.yml's trigger block (push branches main + pull_request) and the actions/checkout@v6 pin.
    Add a `checks` job: strategy.fail-fast false, matrix.os listing ubuntu-latest, macos-latest, windows-latest, matrix.node listing 20 and 22 (full 3x2 per D-01), runs-on the matrix os. Steps in order: actions/checkout@v6, actions/setup-node@v5 with node-version from the matrix and cache npm, npm ci, npx eslint . (D-03 lint gate), npx tsc --noEmit (typecheck gate), npm test (vitest run), npm run build (tsc + copy-migrations). Keep fail-fast false so a single cell's better-sqlite3 native-compile failure surfaces all cells (Pitfall 2) instead of masking them.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');const need=['ubuntu-latest','macos-latest','windows-latest','eslint','tsc --noEmit','npm test','npm run build'];for(const t of need){if(!y.includes(t)){console.error('missing '+t);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists as a new file; `.github/workflows/security.yml` is unchanged.
    - The `checks` job matrix lists `ubuntu-latest`, `macos-latest`, `windows-latest` and Node `20` and `22` with `fail-fast: false`.
    - Steps include `npx eslint .`, `npx tsc --noEmit`, `npm test`, `npm run build` in that gate order.
    - Uses `actions/checkout@v6` and `actions/setup-node@v5` with `cache: npm`.
  </acceptance_criteria>
  <done>ci.yml checks matrix runs lint/typecheck/test/build across 3 OSes x 2 Node versions, separate from security.yml.</done>
</task>

<task type="auto">
  <name>Task 2: Add the install-smoke job (pack, global install, run binary, install against temp HOME)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the file Task 1 created — add the install-smoke job to it)
    - src/cli/commands/install.ts (what `sessionmem install` writes: ~/.sessionmem/config.json + adapter config)
    - tests/integration/cli/install.spec.ts (CliContextOverrides / isolated-config seam reference)
    - .planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md (Pattern 3 install-smoke + Pitfall 3 Windows os.homedir())
  </read_first>
  <action>
    Add an `install-smoke` job to ci.yml with `needs: checks`, the SAME 3x2 matrix, fail-fast false. Steps: checkout, setup-node (cache npm), npm ci, npm run build, npm pack (produces sessionmem-1.0.0.tgz), then globally install the real tarball (npm i -g ./sessionmem-*.tgz on POSIX; use a glob/explicit-name strategy that also works on Windows PowerShell), then run `sessionmem --version` and assert it prints 1.0.0 (catches version drift — Pitfall 5), then run `sessionmem ping`.
    Then run `sessionmem install` against an ISOLATED temp HOME so it never mutates the runner's real ~/.claude.json (Pitfall 3): set HOME (POSIX) AND USERPROFILE plus HOMEPATH (Windows — os.homedir() ignores HOME on Windows) to a freshly created temp directory, run `sessionmem install`, then assert the temp-HOME `.sessionmem/config.json` exists. Use separate POSIX (shell bash) and Windows (shell pwsh) steps gated by runner.os, OR a single cross-platform step that sets all three env vars — pick whichever keeps the Windows assertion reliable. Do NOT start `sessionmem run` as a blocking long-running server in the smoke (it is now a real stdio server that would hang a shell step); limit binary execution to --version/ping/install.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');const need=['install-smoke','needs: checks','npm pack','sessionmem --version','sessionmem install','config.json'];for(const t of need){if(!y.includes(t)){console.error('missing '+t);process.exit(1)}}"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml has an `install-smoke` job with `needs: checks` and the 3x2 matrix.
    - Steps pack the tarball (`npm pack`), globally install it, and run `sessionmem --version` asserting `1.0.0`.
    - `sessionmem install` runs against an isolated temp HOME (HOME on POSIX, USERPROFILE+HOMEPATH on Windows) and the step asserts `config.json` was written there.
    - No step starts `sessionmem run` as a blocking long-running server.
  </acceptance_criteria>
  <done>install-smoke validates the packaged artifact end-to-end per OS, isolated from the runner's real home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → published-artifact behavior | install-smoke installs and runs the packaged tarball |
| smoke step → runner home dir | `sessionmem install` writes host config; must be isolated to temp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | install-smoke writing the runner's real ~/.claude.json | mitigate | Smoke sets HOME (POSIX) + USERPROFILE/HOMEPATH (Windows) to a temp dir before `sessionmem install`; asserts config in the temp dir (Pitfall 3) |
| T-08-10 | Denial of Service | smoke hanging on `sessionmem run` (real stdio server) | mitigate | Binary-run limited to `--version`/`ping`/`install`; never starts the long-running server in a blocking shell step |
| T-08-11 | Tampering | CI lockfile integrity | mitigate | `npm ci` (lockfile-pinned) on every cell; native better-sqlite3 compiled per-cell validates portability (Pitfall 2) |
</threat_model>

<verification>
- Both Task verify node-scripts pass (ci.yml contains the required matrix + smoke tokens).
- security.yml untouched.
- Real CI matrix execution validated on first push (the workflow IS the harness for D-02).
</verification>

<success_criteria>
- ci.yml runs lint/typecheck/test/build on 3 OSes x 2 Node versions.
- install-smoke packs + installs + runs the real artifact and asserts version + config write, isolated from real home.
- Additive to security.yml (D-04).
</success_criteria>

<output>
Create `.planning/phases/08-launch-quality-and-distribution/08-05-SUMMARY.md` when done
</output>
