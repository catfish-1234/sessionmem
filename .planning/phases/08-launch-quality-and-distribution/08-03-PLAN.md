---
phase: 08-launch-quality-and-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/architecture.md
  - docs/troubleshooting.md
  - docs/migration.md
  - tests/integration/docs/readme-docs.spec.ts
  - tests/integration/docs/architecture-docs.spec.ts
  - tests/integration/docs/troubleshooting-docs.spec.ts
  - tests/integration/docs/migration-docs.spec.ts
autonomous: true
requirements: [QLTY-03]
user_setup: []

must_haves:
  truths:
    - "A top-level README.md exists with quickstart/install instructions and links into docs/"
    - "docs/architecture.md documents core engine, adapters, CLI, SQLite storage, retrieval, injection"
    - "docs/troubleshooting.md covers install failures, adapter issues, and native-build (better-sqlite3) symptoms"
    - "docs/migration.md documents the SQLite migration system AND a version-upgrade policy"
    - "Each new doc has a doc-coverage spec asserting it exists with required tokens"
  artifacts:
    - path: "README.md"
      provides: "Entry-point quickstart + links to docs/"
      min_lines: 40
    - path: "docs/architecture.md"
      provides: "High-level architecture overview + diagram"
      min_lines: 40
    - path: "docs/troubleshooting.md"
      provides: "Common issues and fixes"
      min_lines: 30
    - path: "docs/migration.md"
      provides: "Migration system + version-upgrade policy"
      min_lines: 30
    - path: "tests/integration/docs/readme-docs.spec.ts"
      provides: "README coverage drift guard"
      contains: "README.md"
  key_links:
    - from: "tests/integration/docs/architecture-docs.spec.ts"
      to: "docs/architecture.md"
      via: "readFileSync token assertions"
      pattern: "architecture\\.md"
    - from: "README.md"
      to: "src/cli/index.ts"
      via: "documented CLI commands match the real command surface"
      pattern: "sessionmem (install|run|search)"
---

<objective>
Author the four missing entry-point docs (D-05 README, D-06 architecture, D-07 troubleshooting, D-08 migration) in the existing house style, each paired with a doc-coverage drift-guard spec (D-09). Privacy/security (QLTY-03's fifth doc) is already covered by `docs/privacy-and-retention.md` + `cloud-summarization.md` (RESEARCH A6) — not in scope here.

Purpose: QLTY-03 requires a complete user-facing docs set for launch. Doc-coverage specs (mirroring the existing privacy/team specs) prevent the docs from silently drifting out of sync with the shipped CLI surface.
Output: README.md + 3 docs/*.md + 4 tests/integration/docs/*.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-launch-quality-and-distribution/08-CONTEXT.md
@.planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md

<interfaces>
<!-- Doc-coverage spec template — copy verbatim, change DOC_PATH + tokens. From tests/integration/docs/privacy-docs.spec.ts -->
```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "fs";
import { join } from "path";

const DOC_PATH = join(process.cwd(), "docs", "<name>.md"); // README spec uses join(process.cwd(), "README.md")

describe("<name> docs coverage", () => {
  it("the doc file exists", () => {
    expect(existsSync(DOC_PATH)).toBe(true);
  });
  it("covers the required topics", () => {
    const doc = readFileSync(DOC_PATH, "utf8");
    for (const token of [/* required tokens */]) {
      expect(doc, `missing topic token: ${token}`).toContain(token);
    }
  });
});
```

<!-- Real CLI command surface to document accurately (from src/cli/index.ts). Commands: -->
<!-- run, install, uninstall (--purge), ping, search, list, show, forget, export, import, -->
<!-- stats, redact-scan (--apply), retention prune (--force), config get, config set, team enable/disable/status, sync -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md + docs/architecture.md with their coverage specs</name>
  <files>README.md, docs/architecture.md, tests/integration/docs/readme-docs.spec.ts, tests/integration/docs/architecture-docs.spec.ts</files>
  <read_first>
    - docs/privacy-and-retention.md (house style: H1 + lede naming ~/.sessionmem/config.json, ## sections, tables, fenced bash blocks, blockquote callouts)
    - tests/integration/docs/privacy-docs.spec.ts (exact spec template to mirror, including the `expect(doc, \`missing ... ${token}\`)` failure-annotation convention)
    - src/cli/index.ts (real command/option strings — documented commands MUST match)
  </read_first>
  <action>
    Write a top-level README.md (D-05) as the project entry point: H1 title + one-paragraph lede stating the core value (cross-session, cross-platform agent memory), a Quickstart section with `npm install`/`sessionmem install`/`sessionmem run` fenced bash blocks, a short CLI command reference, a "Local-first / privacy" note pointing at `~/.sessionmem/config.json`, and a "Documentation" section with relative links into `docs/architecture.md`, `docs/troubleshooting.md`, `docs/migration.md`, `docs/privacy-and-retention.md`, `docs/team-mode.md`, `docs/benchmark.md`. README lives at repo ROOT (its spec reads `join(process.cwd(), "README.md")`, NOT docs/).
    Write docs/architecture.md (D-06): high-level conceptual overview (not module-by-module) with an ASCII or mermaid diagram showing the four subsystems — core engine, adapters, CLI, SQLite storage — plus the retrieval and injection flow. Name each subsystem literally so the coverage spec tokens appear in prose.
    Write tests/integration/docs/readme-docs.spec.ts: `DOC_PATH = join(process.cwd(), "README.md")`; assert existence + tokens including `sessionmem install`, `sessionmem run`, `quickstart` (case as written), and `docs/architecture.md` (the link). Write tests/integration/docs/architecture-docs.spec.ts: `DOC_PATH = join(process.cwd(), "docs", "architecture.md")`; assert tokens `["core engine", "adapter", "CLI", "SQLite", "retrieval", "injection"]` (RESEARCH Pattern 4). Author each doc and its spec as a pair — every asserted token MUST appear literally in the prose.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/docs/readme-docs.spec.ts tests/integration/docs/architecture-docs.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root with a quickstart section, fenced `sessionmem install`/`sessionmem run` blocks, and relative links to `docs/architecture.md`, `docs/troubleshooting.md`, `docs/migration.md`.
    - `docs/architecture.md` contains the literal tokens `core engine`, `adapter`, `CLI`, `SQLite`, `retrieval`, `injection`.
    - `readme-docs.spec.ts` reads `README.md` from repo root (not docs/) and passes.
    - `architecture-docs.spec.ts` passes.
    - Documented CLI commands match the real surface in `src/cli/index.ts`.
  </acceptance_criteria>
  <done>README and architecture docs exist in house style with passing coverage specs.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/troubleshooting.md + docs/migration.md with their coverage specs</name>
  <files>docs/troubleshooting.md, docs/migration.md, tests/integration/docs/troubleshooting-docs.spec.ts, tests/integration/docs/migration-docs.spec.ts</files>
  <read_first>
    - docs/privacy-and-retention.md (house style to match)
    - tests/integration/docs/team-docs.spec.ts (multi-`it` themed-token grouping pattern)
    - scripts/copy-migrations.mjs (subject of migration.md half (a): build copies .sql into dist/core/schema/migrations/)
    - src/core/schema/migrations (the migration files migration.md documents)
    - .planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md (Pitfall 2: better-sqlite3 native build — troubleshooting source material)
  </read_first>
  <action>
    Write docs/troubleshooting.md (D-07): common issues + fixes organized into `##` sections — install failures (adapter not detected, config not written), adapter-specific issues (Claude Code / Cursor / generic MCP host), and native-build failures for `better-sqlite3` (node-gyp errors, ABI mismatch on Node 20 vs 22, Windows MSVC). Include the literal tokens the spec asserts.
    Write docs/migration.md (D-08) covering BOTH halves: (a) how the SQLite migration system works for contributors/operators — `scripts/copy-migrations.mjs` copies `.sql` files from `src/core/schema/migrations/` into `dist/core/schema/migrations/` during build, and migrations resolve package-relative (Phase 5 Decision #15); AND (b) a version-upgrade policy/guide section (template-level for v1 since there is no prior released version — state the semver/upgrade policy users should expect).
    Write tests/integration/docs/troubleshooting-docs.spec.ts: `DOC_PATH = join(process.cwd(), "docs", "troubleshooting.md")`; assert tokens covering install-failure + adapter + `better-sqlite3` + native-build symptoms. Write tests/integration/docs/migration-docs.spec.ts: `DOC_PATH = join(process.cwd(), "docs", "migration.md")`; use themed `it` blocks — one asserting the migration-system tokens (`copy-migrations`, `migrations`, `dist`), one asserting version-upgrade-policy tokens (`upgrade`, `version`). Author doc + spec as a pair; tokens MUST appear literally.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/docs/troubleshooting-docs.spec.ts tests/integration/docs/migration-docs.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `docs/troubleshooting.md` covers install failures, adapter issues, and `better-sqlite3` native-build symptoms (literal tokens present).
    - `docs/migration.md` documents `copy-migrations` + `src/core/schema/migrations` + `dist` (half a) AND an upgrade/version policy section (half b).
    - `troubleshooting-docs.spec.ts` and `migration-docs.spec.ts` both pass.
    - `migration-docs.spec.ts` uses themed `it` blocks for the two D-08 halves.
  </acceptance_criteria>
  <done>Troubleshooting and migration docs exist with passing coverage specs covering both D-08 halves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → published package | README.md ships in the npm tarball (npm includes README by default) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | docs prose | mitigate | Docs are authored content; ensure no real secrets/tokens are pasted into example blocks (use placeholders); Gitleaks gate in existing security.yml backstops this |
| T-08-06 | Repudiation | troubleshooting/install docs | accept | Doc drift is guarded by coverage specs; no runtime security surface in static docs |
</threat_model>

<verification>
- `npx vitest run tests/integration/docs/ --reporter=dot` passes for all four new specs.
- Documented commands cross-checked against `src/cli/index.ts`.
</verification>

<success_criteria>
- README.md, docs/architecture.md, docs/troubleshooting.md, docs/migration.md exist in house style.
- Four doc-coverage specs pass.
- migration.md covers both D-08 halves; privacy/security doc requirement already satisfied by existing docs.
</success_criteria>

<output>
Create `.planning/phases/08-launch-quality-and-distribution/08-03-SUMMARY.md` when done
</output>
