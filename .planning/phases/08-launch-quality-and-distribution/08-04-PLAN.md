---
phase: 08-launch-quality-and-distribution
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - scripts/benchmark.mjs
  - docs/benchmark.md
  - tests/integration/docs/benchmark-docs.spec.ts
autonomous: true
requirements: [QLTY-04]
user_setup: []

must_haves:
  truths:
    - "`npm run benchmark` runs offline and regenerates docs/benchmark.md reproducibly"
    - "The benchmark measures token reduction using the SAME production countTokens + formatStartupInjection functions"
    - "The benchmark measures retrieval relevance via a curated query/expected-memory fixture set with hit-rate scoring"
    - "docs/benchmark.md reports a defensible token-reduction figure and a relevance hit-rate"
  artifacts:
    - path: "scripts/benchmark.mjs"
      provides: "Reproducible benchmark driving real core retrieve + injection, writes docs/benchmark.md"
      min_lines: 50
    - path: "docs/benchmark.md"
      provides: "Generated benchmark report (token reduction + relevance)"
      contains: "token"
    - path: "tests/integration/docs/benchmark-docs.spec.ts"
      provides: "Benchmark-report coverage drift guard"
      contains: "benchmark.md"
  key_links:
    - from: "scripts/benchmark.mjs"
      to: "dist/core/injection/tokenBudget.js"
      via: "imports countTokens + formatStartupInjection from built dist"
      pattern: "countTokens|formatStartupInjection"
    - from: "scripts/benchmark.mjs"
      to: "dist/core/retrieve/retrieveMemories.js"
      via: "drives real retrieval over seeded fixtures"
      pattern: "retrieveMemories"
---

<objective>
Build the reproducible benchmark (D-10/D-11/D-12): a `scripts/benchmark.mjs` that drives the REAL core retrieval + injection functions over curated fixtures, measures token reduction (injected-context vs full-history baseline) and retrieval relevance (hit-rate over a query/expected-memory set), and writes `docs/benchmark.md`. Guard the report with a coverage spec mirroring D-09.

Purpose: QLTY-04 requires a published benchmark demonstrating token reduction and retrieval relevance quality. Reusing production functions (`countTokens`, `formatStartupInjection`, `retrieveMemories`, offline `deterministicEmbed`) makes the numbers honest, network-free, and re-runnable when retrieval/injection logic changes.
Output: scripts/benchmark.mjs + docs/benchmark.md + benchmark-docs.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md
@.planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md

<!-- Plan 08-01 already added the `"benchmark": "node scripts/benchmark.mjs"` script to package.json. -->
<!-- Plan 08-01 also added `prepack`/build; this plan relies on `npm run build` having produced dist/. -->

<interfaces>
<!-- ESM script shell convention (from scripts/copy-migrations.mjs). -->
```javascript
import { writeFileSync } from "fs";
import { join, dirname } from "path";
import { fileURLToPath } from "url";
const __dirname = dirname(fileURLToPath(import.meta.url));
// ...measure...
writeFileSync(join(__dirname, "..", "docs", "benchmark.md"), report);
console.log(`Benchmark: reduction <pct>%, hit-rate <rate>`);
```

<!-- Production functions to reuse (DO NOT re-implement). benchmark.mjs imports from BUILT dist/ -->
<!-- (copy-migrations.mjs imports only fs/path; importing TS source needs tsx, so import dist/*.js instead). -->
<!-- dist/core/injection/tokenBudget.js     → countTokens(text): number -->
<!-- dist/core/injection/formatStartupInjection.js → formatStartupInjection(ranked, opts): string -->
<!-- dist/core/retrieve/retrieveMemories.js  → retrieveMemories({ db, projectId, queryText, topK?, limit?, now? }) -->
<!-- dist/core/embed/deterministicEmbed.js   → deterministicEmbed(text, dim): { vector, normalizedText, ... } (offline) -->
<!-- dist/core/storage/db.js                 → openDb() -->
<!-- dist/core/storage/memoryRepo.js         → insertMemory(db, row) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/benchmark.mjs driving real core functions over fixtures</name>
  <files>scripts/benchmark.mjs</files>
  <read_first>
    - scripts/copy-migrations.mjs (ESM script shell: __dirname derivation, top-level flow, closing console.log)
    - tests/quality/injection/injection-quality-harness.spec.ts (token-measurement pattern: countTokens + formatStartupInjection; the memory(overrides) fixture factory)
    - tests/integration/retrieve/retrieve-ranked.spec.ts (seed via insertMemory with JSON.stringify(deterministicEmbed(...).vector); call retrieveMemories)
    - src/core/injection/tokenBudget.ts (countTokens signature — js-tiktoken o200k_base)
    - src/core/retrieve/retrieveMemories.ts (retrieveMemories param shape: db, projectId, queryText, topK/limit, now)
  </read_first>
  <action>
    Create scripts/benchmark.mjs (ESM, mirroring copy-migrations.mjs shape). It imports the BUILT production functions from `dist/` (NOT from src — copy-migrations only imports fs/path and is no precedent for TS imports; importing TS would require tsx). The benchmark therefore REQUIRES `npm run build` to have run first — note this in a header comment and have the script fail with a clear message if a `dist/` import is missing.
    Build an in-memory DB via `openDb()`, seed a curated fixture set of realistic coding-session memories (decisions/facts/warnings) via `insertMemory` with embeddings from `deterministicEmbed` (offline → reproducible). Define a curated query set `[{ query, expectedMemoryIds }]` (reuse retrieve-ranked fixture shape).
    Token reduction (D-10): compute `baseline = countTokens(fullConcatenatedHistory)` (all fixture memory contents joined) and `injected = countTokens(formatStartupInjection(retrievedTopK))`; report `1 - injected/baseline` as a percentage. Run BOTH framings D-10 asks for — (a) injected-vs-full-history and (b) a realistic-scenario measurement — and include both, leading the report with whichever yields the stronger defensible reduction.
    Relevance (D-11): for each query run `retrieveMemories`, compare returned ids against `expectedMemoryIds`, compute hit@k / precision / recall, aggregate to an overall hit-rate.
    Write the results to `docs/benchmark.md` via `writeFileSync` in the existing doc house style (H1 + lede + ## sections + a results table). Close with a `console.log` summarizing reduction % and hit-rate. The script MUST be deterministic across runs (deterministicEmbed is hash-based, no network, no clock-dependent randomness — pin any `now` to a fixed date).
  </action>
  <verify>
    <automated>npm run build && npm run benchmark && node -e "const fs=require('fs'); if(!fs.existsSync('docs/benchmark.md')){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/benchmark.mjs` imports `countTokens`, `formatStartupInjection`, `retrieveMemories`, `deterministicEmbed`, `openDb`, `insertMemory` from `dist/`.
    - Running `npm run benchmark` (after build) writes `docs/benchmark.md` and exits 0 with no network access.
    - The script computes a token-reduction percentage from `countTokens` and a relevance hit-rate from `retrieveMemories` over a curated query/expected-id fixture set.
    - Two consecutive `npm run benchmark` runs produce identical `docs/benchmark.md` (deterministic).
  </acceptance_criteria>
  <done>A reproducible, offline benchmark script drives real core code and regenerates docs/benchmark.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark-docs coverage spec and confirm report content</name>
  <files>docs/benchmark.md, tests/integration/docs/benchmark-docs.spec.ts</files>
  <read_first>
    - tests/integration/docs/privacy-docs.spec.ts (coverage-spec template)
    - docs/benchmark.md (generated by Task 1 — confirm the literal tokens the spec will assert are present in the generated output)
    - docs/privacy-and-retention.md (house style the generated report should match)
  </read_first>
  <action>
    Write tests/integration/docs/benchmark-docs.spec.ts mirroring the D-09 template: `DOC_PATH = join(process.cwd(), "docs", "benchmark.md")`; assert existence + tokens proving both measurement dimensions are reported — e.g. `token`, `reduction`, `relevance`, `hit` (case-matched to the generated report). If the generated report does not contain these literal tokens, adjust the report-writing strings in scripts/benchmark.mjs so the report and spec form a token-contract pair (the report headings/labels must contain the asserted tokens). Re-run `npm run benchmark` to regenerate the report, then run the spec.
  </action>
  <verify>
    <automated>npm run build && npm run benchmark && npx vitest run tests/integration/docs/benchmark-docs.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/docs/benchmark-docs.spec.ts` asserts `docs/benchmark.md` exists and contains tokens for token-reduction (`token`, `reduction`) and relevance (`relevance`, `hit`).
    - The generated `docs/benchmark.md` contains every asserted token (report ⇄ spec token contract holds).
    - The spec passes after a fresh `npm run benchmark`.
  </acceptance_criteria>
  <done>The benchmark report is drift-guarded by a passing coverage spec; report and spec tokens agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark script → local DB/files | Script seeds an in-memory DB and writes docs/benchmark.md; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | docs/benchmark.md content | mitigate | Fixtures are synthetic (no real user data/secrets); report writes only aggregate metrics, never raw memory content beyond synthetic examples |
| T-08-09 | Tampering | benchmark reproducibility | accept | Deterministic offline embedding + pinned clock means no network/supply-chain surface at benchmark runtime; no external dep added |
</threat_model>

<verification>
- `npm run benchmark` regenerates docs/benchmark.md offline.
- `npx vitest run tests/integration/docs/benchmark-docs.spec.ts` passes.
- Two consecutive runs yield byte-identical reports.
</verification>

<success_criteria>
- Reproducible benchmark script reusing production token/retrieval functions.
- docs/benchmark.md reports token reduction (both D-10 framings) + relevance hit-rate.
- Coverage spec guards the report.
</success_criteria>

<output>
Create `.planning/phases/08-launch-quality-and-distribution/08-04-SUMMARY.md` when done
</output>
