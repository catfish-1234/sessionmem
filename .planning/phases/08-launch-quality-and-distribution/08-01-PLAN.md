---
phase: 08-launch-quality-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/cli/index.ts
  - src/adapters/generic.ts
  - tests/integration/mcp/stdio-server.spec.ts
  - LICENSE
  - .gitignore
autonomous: false
requirements: [QLTY-01, QLTY-05]
user_setup: []

must_haves:
  truths:
    - "`sessionmem run` starts a real MCP stdio server that completes a client initialize handshake"
    - "The MCP server exposes tools that dispatch to the existing MemoryCoreService methods"
    - "`sessionmem --version` prints 1.0.0 (matching package.json)"
    - "package.json is publishable (private:false, files allow-list, license, repository, mcpName)"
    - "A LICENSE file exists with MIT text"
  artifacts:
    - path: "src/adapters/generic.ts"
      provides: "Real stdio MCP server wired to @modelcontextprotocol/sdk dispatching MemoryCoreService"
      contains: "StdioServerTransport"
    - path: "tests/integration/mcp/stdio-server.spec.ts"
      provides: "Integration test spawning the server and asserting an initialize response"
      min_lines: 30
    - path: "package.json"
      provides: "Publishable package manifest with deps and metadata"
      contains: "\"mcpName\""
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
  key_links:
    - from: "src/adapters/generic.ts"
      to: "src/core/api/memoryCoreService.ts"
      via: "MCP tool handlers call createMemoryCoreService(...).call(method, request)"
      pattern: "createMemoryCoreService|MemoryCoreService"
    - from: "src/cli/index.ts"
      to: "package.json"
      via: "version sourced from package.json (no hardcoded 0.1.0)"
      pattern: "version"
---

<objective>
Make the package distribution-ready at its core: implement the real stdio MCP server (D-17) so `sessionmem run` is a functioning server (not the current stub), bump and de-drift the version (D-14, Pitfall 5), and flip package.json from `private:true`/`0.1.0` to a publishable `sessionmem@1.0.0` with all required metadata, deps, and an MIT LICENSE (D-13/D-14).

Purpose: Every other Phase 8 deliverable depends on this. The install-smoke job (Plan 05) must run a working binary; distribution metadata (Plan 06) advertises a functioning MCP server; the benchmark/eslint plans need the publishable manifest and devDeps. Shipping a dead MCP server in registry/marketplace listings is the single biggest launch risk (RESEARCH Pitfall 1).
Output: Working MCP stdio server + integration test, publishable package.json, version-fixed CLI, LICENSE, .gitignore dist/ entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-launch-quality-and-distribution/08-CONTEXT.md
@.planning/phases/08-launch-quality-and-distribution/08-RESEARCH.md
@.planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md

<interfaces>
<!-- Dispatch target for MCP tools. Extracted from src/core/api/memoryCoreService.ts + contracts.ts. -->
<!-- Use these directly — the service and its typed method map already exist. -->

From src/core/api/memoryCoreService.ts:
```typescript
export interface CreateMemoryCoreServiceDeps {
  db: Database;              // from openDb() in src/core/storage/db.ts
  embeddingDimension?: number;
  username?: string;
  policyConfig?: LocalOnlyPolicyConfig;
  // ...test seams omitted...
}
export function createMemoryCoreService(deps: CreateMemoryCoreServiceDeps): {
  // ...all methods...
  call<M extends MemoryCoreMethod>(method: M, request: MemoryCoreRequest<M>): Promise<MethodResult<M>>;
};
```

From src/core/api/contracts.ts — the typed method surface to expose as MCP tools:
```typescript
export type MemoryCoreMethod = keyof MemoryCoreRequestMap;
// Relevant methods to expose as tools: retrieveMemories, storeMemory,
// listMemories, getMemory, forgetMemory, stats
export type MemoryCoreRequest<M extends MemoryCoreMethod> = MemoryCoreRequestMap[M];
export type MemoryCoreResponse<M extends MemoryCoreMethod> = MemoryCoreResponseMap[M];
```

From src/adapters/contract/hostAdapterContract.ts (GenericMCPAdapter implements HostAdapterContract):
```typescript
// startMcpServer(): Promise<void> is the method whose stub body must be replaced.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, make package.json publishable, fix version drift, add LICENSE + .gitignore</name>
  <files>package.json, package-lock.json, src/cli/index.ts, LICENSE, .gitignore</files>
  <read_first>
    - package.json (current: version 0.1.0, private:true, no files/license/repository/mcpName)
    - src/cli/index.ts (line 25 hardcodes `.version("0.1.0")`)
    - .planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md (package.json section + Pitfall 5)
    - scripts/copy-migrations.mjs (referenced by the build script that prepack must run)
  </read_first>
  <action>
    Install the runtime MCP SDK and confirm versions. Run `npm view sessionmem version` to reconfirm name availability (expect E404 → name free per D-13); if it resolves, STOP and surface a scoped-name fallback `@kavishdua/sessionmem`. Run `npm install @modelcontextprotocol/sdk` (target 1.29.0 per RESEARCH; run `npm view @modelcontextprotocol/sdk version` first and pin the resolved version) and `npm view @modelcontextprotocol/sdk scripts.postinstall` to confirm no network/FS postinstall (Package Legitimacy gate, T-08-SC).
    Edit package.json (D-13/D-14): set `"version": "1.0.0"`, `"private": false`, add `"license": "MIT"`, `"author"`, `"repository": { "type": "git", "url": "git+https://github.com/kavishdua/sessionmem.git" }`, `"files": ["dist"]`, `"publishConfig": { "access": "public" }`, and `"mcpName": "io.github.kavishdua/sessionmem"` (D-19 — MUST exactly equal the future server.json `name`). Add a `"prepack": "npm run build"` script so `dist/` is built into the tarball, AND add a `"benchmark": "node scripts/benchmark.mjs"` script (Plan 04 authors the script file; wiring it here keeps package.json single-owner). Keep existing `build`/`test`/`test:schema` scripts intact.
    Fix version drift (Pitfall 5): in src/cli/index.ts replace the hardcoded `.version("0.1.0")` so the version is sourced from package.json via `createRequire(import.meta.url)("../../package.json").version` (NodeNext ESM, `.js` specifiers elsewhere unaffected since this reads JSON). The published `--version` MUST print `1.0.0`.
    Create LICENSE with standard MIT license text, copyright holder "kavishdua" / current year 2026.
    Edit .gitignore to add a `dist/` line (currently untracked but un-ignored per CONCERNS.md); the package ships dist via `files`+`prepack`, not git.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const p=require('./package.json'); if(p.private!==false||p.version!=='1.0.0'||!p.files||p.mcpName!=='io.github.kavishdua/sessionmem'||p.license!=='MIT'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has `"private": false`, `"version": "1.0.0"`, `"license": "MIT"`, `"files": ["dist"]`, `"publishConfig": {"access":"public"}`, `"repository"`, `"author"`, and `"mcpName": "io.github.kavishdua/sessionmem"`.
    - `package.json` has a `"prepack"` script that runs the build and a `"benchmark": "node scripts/benchmark.mjs"` script.
    - `@modelcontextprotocol/sdk` appears in `dependencies` and `package-lock.json`.
    - `src/cli/index.ts` no longer contains the literal `"0.1.0"`; version resolves from package.json.
    - `npx tsc --noEmit` exits 0.
    - `LICENSE` exists and contains `MIT License`.
    - `.gitignore` contains a `dist/` entry.
  </acceptance_criteria>
  <done>package.json is publishable with all D-14 fields, sdk dep added, CLI version drift fixed, LICENSE and .gitignore updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement real stdio MCP server in generic.ts dispatching to MemoryCoreService</name>
  <files>src/adapters/generic.ts, tests/integration/mcp/stdio-server.spec.ts</files>
  <behavior>
    - Test: spawning `node dist/cli/index.js run` (built binary), sending a JSON-RPC `initialize` request over stdin, yields a valid MCP initialize response on stdout (protocol handshake completes).
    - Test: a `tools/list` request returns at least the `retrieveMemories` and `storeMemory` tool names.
    - Test (optional but preferred): a `tools/call` for `storeMemory` then `retrieveMemories` against a temp DB returns the stored memory (round-trip through the real MemoryCoreService).
  </behavior>
  <read_first>
    - src/adapters/generic.ts (the stub `startMcpServer` at lines 26-29 — replace its body)
    - src/cli/commands/run.ts (caller — already calls `adapter.startMcpServer()`; no change needed there)
    - src/core/api/memoryCoreService.ts (createMemoryCoreService + .call dispatch target; method names retrieveMemories/storeMemory/listMemories/getMemory/forgetMemory/stats)
    - src/core/api/contracts.ts lines 366-405 (MemoryCoreRequestMap/ResponseMap typed surface)
    - src/core/storage/db.ts (openDb to construct the Database for the service)
    - .planning/phases/08-launch-quality-and-distribution/08-PATTERNS.md (generic.ts section — D-17 dispatch guidance, A3 SDK-API caveat)
  </read_first>
  <action>
    Replace the stub body of `GenericMCPAdapter.startMcpServer()` with a real stdio MCP server using `@modelcontextprotocol/sdk`. Verify the exact SDK API surface for the installed version FIRST via Context7 (resolve-library-id `@modelcontextprotocol/sdk`, then query "McpServer StdioServerTransport registerTool") — do NOT trust the sketched import paths blindly (RESEARCH A3). Expected shape: import `McpServer` from `@modelcontextprotocol/sdk/server/mcp.js` and `StdioServerTransport` from `@modelcontextprotocol/sdk/server/stdio.js`.
    Construct one `MemoryCoreService` instance via `createMemoryCoreService({ db: openDb() })`. Register MCP tools that each delegate to one `MemoryCoreMethod` through the service's `.call(method, request)`: at minimum `retrieveMemories`, `storeMemory`, `listMemories`, `getMemory`, `forgetMemory`, `stats`. Define each tool's input schema with zod (already a dependency) mirroring the corresponding `*RequestSchema` fields in contracts.ts — reuse those schemas where the SDK accepts a zod shape. Map the service's `{ ok, ... }` / `{ ok:false, error }` envelopes into MCP tool results (success content vs `isError`). Connect via `await server.connect(new StdioServerTransport())`. CRITICAL: the server writes MCP protocol frames to stdout, so route all diagnostic logging to stderr or the existing `~/.sessionmem/logs/mcp.log` file (run.ts already writes that) — never `console.log` to stdout, which would corrupt the protocol stream.
    Write tests/integration/mcp/stdio-server.spec.ts: build first (`dist/` must exist), then use `child_process.spawn` to launch `node <repo>/dist/cli/index.js run` with `HOME`/`USERPROFILE` pointed at a `mkdtemp` dir (isolation per Pitfall 3), write a JSON-RPC `initialize` message to stdin, and assert a well-formed initialize response is read from stdout within a timeout. Add a `tools/list` assertion. Use the existing vitest conventions.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/integration/mcp/stdio-server.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/generic.ts` imports from `@modelcontextprotocol/sdk` and contains `StdioServerTransport` and `server.connect`.
    - `startMcpServer` constructs a `MemoryCoreService` and registers ≥6 tools dispatching to `retrieveMemories`, `storeMemory`, `listMemories`, `getMemory`, `forgetMemory`, `stats`.
    - No `console.log` writes to stdout inside the server path (stdout reserved for MCP frames).
    - `tests/integration/mcp/stdio-server.spec.ts` spawns the built binary, sends `initialize`, and asserts a valid response; `tools/list` returns the expected tool names.
    - `npm run build` succeeds and the spec passes.
  </acceptance_criteria>
  <done>`sessionmem run` is a functioning MCP stdio server dispatching to the core service, proven by a spawn-based initialize/tools-list integration test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Installed `@modelcontextprotocol/sdk@<resolved>` as a runtime dependency. slopcheck could not be run during research (sandbox-denied), so this package is tagged [ASSUMED] per the Package Legitimacy Gate — it must be human-verified before it ships in a published package.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@modelcontextprotocol/sdk and confirm it is the official Anthropic Model Context Protocol SDK (publisher: modelcontextprotocol / anthropic; source repo github.com/modelcontextprotocol/typescript-sdk).
    2. Confirm the installed version in package.json matches a real published version on that page.
    3. Confirm `npm view @modelcontextprotocol/sdk scripts.postinstall` printed nothing (no postinstall script).
    Reply "approved" to continue, or describe the discrepancy to halt.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → stdio server | Untrusted JSON-RPC tool-call args cross into MemoryCoreService |
| npm registry → developer machine | Newly-added `@modelcontextprotocol/sdk` dependency (supply chain) |
| repo working tree → published tarball | `files` allow-list controls what leaves the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | MCP tool-call args → MemoryCoreService | mitigate | Each tool validates input via the existing zod `*RequestSchema` before dispatch; the service already re-parses with zod at the boundary (defense in depth) |
| T-08-04 | Information Disclosure | stdout protocol stream | mitigate | All diagnostic logging routed to stderr/log file, never stdout — prevents leaking server-internal data into the protocol frame and corrupting it |
| T-08-02 | Information Disclosure | published tarball contents | mitigate | `files: ["dist"]` allow-list; tarball contents reviewed via `npm pack --dry-run` in Plan 04 (`.planning/`, `tests/` must be absent) |
| T-08-SC | Tampering | npm install of @modelcontextprotocol/sdk | mitigate | [ASSUMED] package gated behind a blocking-human checkpoint verifying npmjs.com identity + no postinstall script |
</threat_model>

<verification>
- `npm run build` succeeds (dist/ produced).
- `npx vitest run tests/integration/mcp/stdio-server.spec.ts` passes.
- `node dist/cli/index.js --version` prints `1.0.0`.
- `node -e` package.json assertion (Task 1 verify) passes.
</verification>

<success_criteria>
- `sessionmem run` is a real MCP stdio server (no longer a stub).
- package.json is publishable with all D-14 metadata + mcpName (D-19).
- CLI version reports 1.0.0 sourced from package.json.
- LICENSE (MIT) exists; .gitignore ignores dist/.
- The [ASSUMED] SDK dependency is human-verified.
</success_criteria>

<output>
Create `.planning/phases/08-launch-quality-and-distribution/08-01-SUMMARY.md` when done
</output>
