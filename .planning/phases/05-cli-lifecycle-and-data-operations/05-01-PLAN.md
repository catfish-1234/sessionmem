---
phase: 05-cli-lifecycle-and-data-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - scripts/copy-migrations.mjs
  - src/cli/context.ts
  - src/cli/output.ts
  - src/cli/index.ts
  - tests/unit/cli/context.spec.ts
  - tests/unit/cli/output.spec.ts
  - tests/integration/cli/install.spec.ts
  - tests/integration/cli/uninstall.spec.ts
  - tests/integration/cli/search.spec.ts
  - tests/integration/cli/data-commands.spec.ts
  - tests/integration/cli/forget.spec.ts
  - tests/integration/cli/export-import.spec.ts
  - tests/unit/cli/stats.spec.ts
  - tests/unit/cli/error-contract.spec.ts
  - tests/helpers/cliTestContext.ts
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06]
user_setup: []

must_haves:
  truths:
    - "`npm run build` produces dist/cli/index.js with a #!/usr/bin/env node shebang"
    - "dist/core/schema/migrations/ contains the four .sql migration files after build"
    - "createCliContext() opens ~/.sessionmem/memories.db, runs migrations against a package-relative dir, and returns { db, service, projectId, dbPath }"
    - "createCliContext() accepts overrides so tests can inject a temp DB path and explicit projectId"
    - "formatTable and formatKeyValue emit plain text with no ANSI escape codes"
    - "D-01: package.json dependencies contains commander (^15.0.0)"
    - "D-01: commander program in src/cli/index.ts registers run, install, uninstall, ping, search, list, show, forget, export, import, and stats subcommands"
    - "Top-level errors print error.message to stderr and exit non-zero (D-03)"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript compile config emitting to dist/"
      contains: "outDir"
    - path: "scripts/copy-migrations.mjs"
      provides: "Copies src/core/schema/migrations/*.sql into dist/ after tsc"
    - path: "src/cli/context.ts"
      provides: "Shared CLI context: DB path resolve, migrations, projectId derivation, service factory"
      exports: ["createCliContext"]
      min_lines: 25
    - path: "src/cli/output.ts"
      provides: "Plain-text table + key:value formatters, no ANSI"
      exports: ["formatTable", "formatKeyValue"]
    - path: "src/cli/index.ts"
      provides: "commander program bin entry registering all subcommands"
      contains: "new Command"
    - path: "tests/helpers/cliTestContext.ts"
      provides: "Builds a CliContext over a temp-file/in-memory DB seeded with memories"
      exports: ["createTestCliContext"]
  key_links:
    - from: "src/cli/context.ts"
      to: "src/core/storage/db.ts"
      via: "openDb({ dbPath, migrationsDir })"
      pattern: "openDb\\("
    - from: "src/cli/context.ts"
      to: "src/core/api/memoryCoreService.ts"
      via: "createMemoryCoreService({ db })"
      pattern: "createMemoryCoreService\\("
    - from: "package.json"
      to: "dist/cli/index.js"
      via: "bin field"
      pattern: "\"sessionmem\""
---

<objective>
Build the foundational layer every Phase 5 CLI command depends on: the compile/bin pipeline, the shared CLI context (DB-path + migrations + projectId + service), the plain-text output formatters, the commander program shell that registers every subcommand, and all Wave 0 test scaffolds.

Purpose: RESEARCH.md identifies three blocking gaps — no build pipeline, no production DB wiring, no projectId derivation. This plan closes all three and defines the contracts (context, output, command-registration points) so Plans 02/03/04 implement command behavior in their own files without touching index.ts or re-opening the DB.
Output: tsconfig.json, scripts/copy-migrations.mjs, updated package.json (commander dep + bin + build script), src/cli/context.ts, src/cli/output.ts, src/cli/index.ts (full subcommand registration wired to per-command action modules), shared test helper, and failing/placeholder Wave 0 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-CONTEXT.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-RESEARCH.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From src/core/storage/db.ts:
```typescript
export interface OpenDbOptions { dbPath?: string; migrationsDir?: string; }
export function openDb(options: OpenDbOptions = {}): Database; // defaults dbPath ":memory:", migrationsDir cwd-relative
```

From src/core/schema/runMigrations.ts:
```typescript
// DEFAULT_MIGRATIONS_DIR = path.resolve(process.cwd(), "src/core/schema/migrations") — MUST be overridden for installed CLI
export function runMigrations(db: Database, migrationsDir?: string): void;
```

From src/core/api/memoryCoreService.ts:
```typescript
export interface CreateMemoryCoreServiceDeps { db: Database; embeddingDimension?: number; policyConfig?: LocalOnlyPolicyConfig; }
export function createMemoryCoreService(deps: CreateMemoryCoreServiceDeps): MemoryCoreService;
// Methods (all return { ok, ... } | { ok: false, error: { code, message } }):
//   listMemories({ projectId }) -> { ok, memories: MemoryDto[], total }
//   retrieveMemories({ projectId, query, limit }) -> { ok, memories: RetrievedMemoryDto[], total }
//   getMemory({ projectId, memoryId }) -> { ok, memory: MemoryDto }   (NOT_FOUND -> DomainError)
//   forgetMemory({ projectId, memoryId }) -> { ok }
//   exportMemories({ projectId }) -> { ok, memories: MemoryDto[] }
//   importMemories({ projectId, memories }) -> { ok, ... }  (upserts via ON CONFLICT(id) DO UPDATE)
//   stats({ projectId }) -> { ok, totalMemories, totalSessionEvents }
```

From src/core/storage/memoryRepo.ts (line 75) — needed by stats only:
```typescript
export function listMemoriesByProject(db: Database, projectId: string): MemoryRow[]; // row.content available
```

From src/core/injection/tokenBudget.ts (line 19):
```typescript
export function countTokens(text: string): number; // js-tiktoken o200k_base
```

Migrations present: src/core/schema/migrations/001_initial.sql, 002_indexes.sql, 003_summarization_failures.sql, 004_memory_feedback.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build/bin pipeline (tsconfig, migration copy, package.json)</name>
  <files>tsconfig.json, scripts/copy-migrations.mjs, package.json</files>
  <read_first>
    - package.json (current deps: better-sqlite3, js-tiktoken, zod; devDeps: typescript ^6.0.3, vitest ^4.1.8, @types/better-sqlite3; type: module; only test scripts exist)
    - src/core/schema/runMigrations.ts (DEFAULT_MIGRATIONS_DIR is cwd-relative — why migrations must be copied into dist)
    - src/cli/commands/run.ts (existing entry shape; shebang absent today)
  </read_first>
  <action>
    Lock the tsc-to-dist build path (RESEARCH Q2 recommendation — matches literal bin ./dist/cli/index.js and prepares for QLTY-05 npm publish).

    Create tsconfig.json: compilerOptions with "module": "NodeNext", "moduleResolution": "NodeNext", "target": "ES2022", "outDir": "dist", "rootDir": "src", "declaration": false, "sourceMap": false, "strict": true, "esModuleInterop": true, "skipLibCheck": true, "resolveJsonModule": true; "include": ["src/**/*.ts"]; "exclude": ["tests", "dist", "node_modules"]. Do NOT include tests in the build (they run via vitest on .ts directly).

    Create scripts/copy-migrations.mjs: a Node ESM script that copies every file from src/core/schema/migrations/ into dist/core/schema/migrations/ (mkdirSync recursive, then copyFileSync per .sql file). tsc does NOT copy non-TS assets (Pitfall 2/4) so this runs after tsc.

    Edit package.json:
    - Add to dependencies: "commander": "^15.0.0".
    - Add "bin": { "sessionmem": "./dist/cli/index.js" }.
    - Add scripts: "build": "tsc && node scripts/copy-migrations.mjs". Keep existing "test" and "test:schema" scripts unchanged.

    Run `npm install` after editing package.json so commander is present (CLI tests import it). Do NOT add tsx, cli-table3, chalk, or picocolors (RESEARCH anti-patterns / D-07).
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs');process.exit(fs.existsSync('dist/cli/index.js')&&fs.readdirSync('dist/core/schema/migrations').filter(f=>f.endsWith('.sql')).length===4?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json contains "outDir" set to "dist" and "rootDir" set to "src"
    - tsconfig.json "exclude" contains "tests"
    - package.json dependencies contains "commander"
    - package.json contains "bin" with key "sessionmem" pointing to "./dist/cli/index.js"
    - package.json scripts.build equals "tsc && node scripts/copy-migrations.mjs"
    - `npm run build` exits 0 and dist/cli/index.js exists
    - dist/core/schema/migrations/ contains 4 .sql files after build
    - dist/cli/index.js first line is "#!/usr/bin/env node" (shebang produced by index.ts in Task 3)
    - node_modules/commander exists after npm install
  </acceptance_criteria>
  <done>npm run build compiles src to dist/, copies all 4 migration .sql files into dist, and commander is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared CLI context and output formatters</name>
  <files>src/cli/context.ts, src/cli/output.ts</files>
  <read_first>
    - src/core/storage/db.ts (openDb signature; defaults to :memory: and cwd migrations — both must be overridden)
    - src/core/schema/runMigrations.ts (why migrationsDir must be package-relative, not cwd)
    - src/core/api/memoryCoreService.ts (createMemoryCoreService deps + MemoryDto field names in camelCase for formatKeyValue mapping)
    - .planning/phases/05-cli-lifecycle-and-data-operations/05-PATTERNS.md (context.ts and output.ts target shapes; D-07/D-08 formatting rules)
  </read_first>
  <action>
    Create src/cli/context.ts exporting createCliContext(overrides?). It resolves dir = join(homedir(), ".sessionmem"), mkdirSync(dir, { recursive: true }), dbPath = join(dir, "memories.db"). Resolve migrationsDir package-relative via fileURLToPath(import.meta.url) + dirname → join(here, "..", "core", "schema", "migrations") (fixes Pitfall 2; works from dist/cli or src/cli since the relative layout is identical). Call openDb({ dbPath, migrationsDir }) then createMemoryCoreService({ db }). Derive projectId via a deterministic deriveProjectId() helper.

    deriveProjectId rule (resolves Open Q1 / Assumption A1 — nothing in the codebase computes project_id; hosts supply it, so the CLI owns its own derivation): use the basename of process.cwd() as the projectId. Implement an optional override: createCliContext accepts an optional object { dbPath?, migrationsDir?, projectId?, db?, service? } so callers (and tests) can inject a temp DB and an explicit projectId. Add a CLI-level `--project <id>` safety valve note: the override path is the seam commands use to honor a future --project flag. Return { db, service, projectId, dbPath }. Use .js import extensions, no barrel files.

    Create src/cli/output.ts exporting formatTable(rows) and formatKeyValue(memory). formatTable (D-07): one header line "ID | importance | date | preview" then one row per memory using String.padEnd fixed-width columns joined with " | "; preview = content.replace(/\s+/g, " ").slice(0, 60); date = createdAt.slice(0, 10); NO ANSI/color. formatKeyValue (D-08): emit plain "key: value" lines in this exact order with snake_case labels mapping from MemoryDto camelCase fields — id, content, importance, created_at (from createdAt), session_id (from sessionId), project_id (from projectId), source_adapter (from sourceAdapter). Return strings; let the command print them.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/cli/context.spec.ts tests/unit/cli/output.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/context.ts contains "export function createCliContext"
    - src/cli/context.ts calls openDb with both dbPath and migrationsDir (pattern openDb\() and createMemoryCoreService (pattern createMemoryCoreService\()
    - createCliContext accepts an overrides object allowing injected dbPath/projectId (verified by tests/unit/cli/context.spec.ts passing)
    - migrationsDir is derived from import.meta.url (contains "import.meta.url"), NOT process.cwd()
    - src/cli/output.ts exports formatTable and formatKeyValue
    - formatTable output contains " | " separators and the header "ID | importance | date | preview"
    - formatKeyValue output contains the literal labels "source_adapter:" and "created_at:" mapped from camelCase DTO fields
    - No occurrence of "\x1b[" (ANSI escape) in src/cli/output.ts
    - tests/unit/cli/context.spec.ts and tests/unit/cli/output.spec.ts exit 0
  </acceptance_criteria>
  <done>context.ts opens the real ~/.sessionmem DB with package-relative migrations and a cwd-derived projectId (override-injectable); output.ts produces ANSI-free table and key:value text.</done>
</task>

<task type="auto">
  <name>Task 3: Create commander program shell and Wave 0 test scaffolds</name>
  <files>src/cli/index.ts, tests/helpers/cliTestContext.ts, tests/unit/cli/context.spec.ts, tests/unit/cli/output.spec.ts, tests/unit/cli/stats.spec.ts, tests/unit/cli/error-contract.spec.ts, tests/integration/cli/install.spec.ts, tests/integration/cli/uninstall.spec.ts, tests/integration/cli/search.spec.ts, tests/integration/cli/data-commands.spec.ts, tests/integration/cli/forget.spec.ts, tests/integration/cli/export-import.spec.ts</files>
  <read_first>
    - src/cli/commands/run.ts (exec-guard block to be replaced by commander routing — keep runMcpServer body for Plan 02)
    - .planning/phases/05-cli-lifecycle-and-data-operations/05-PATTERNS.md (index.ts target shape; test seeding + console-spy patterns; testability seam)
    - tests/integration/core/memory-core-service.spec.ts (openDb + createMemoryCoreService + storeMemory seeding pattern to mirror)
    - tests/unit/adapters/run-command.spec.ts (vi.spyOn(console,...) + envelope assertion + vi.restoreAllMocks in afterEach)
  </read_first>
  <action>
    Create src/cli/index.ts: first line "#!/usr/bin/env node". Import Command from "commander". Build `const program = new Command(); program.name("sessionmem").version("0.1.0");`. Register ALL eleven subcommands here so later plans never edit this file (avoids files_modified overlap): run, install, uninstall, ping, search, list, show, forget, export, import, stats. Each subcommand's .action imports an exported action function from its command module under ./commands/*.js (run.js, install.js, uninstall.js, ping.js, search.js, list.js, show.js, forget.js, export.js, import.js, stats.js). Wire arguments/options now per decisions: search <query> [--limit <n>]; show <id>; forget <id> [--force]; export [path]; import <path> [--merge]; uninstall [--purge]. End with `program.parseAsync(process.argv).catch((err) => { console.error(err instanceof Error ? err.message : String(err)); process.exit(1); });` (D-03).

    IMPORTANT — stub-tolerant imports: the command action modules are implemented in Plans 02/03/04. So that the build and program load even before those land, create minimal placeholder action exports inside EACH not-yet-implemented command module ONLY IF the file does not already exist: a `export async function <name>Command(...) { throw new Error("not implemented"); }`. Do NOT stub run.ts or install.ts (they already exist — import their existing exports: runMcpServer, installCommand). For files Plans 02/03/04 own (uninstall, ping, search, list, show, forget, export, import, stats), create a placeholder file with only the exported action stub so index.ts type-checks and builds; the owning plan replaces the body. Each placeholder file's stub MUST be the function name index.ts imports.

    Create tests/helpers/cliTestContext.ts exporting createTestCliContext(): builds a CliContext over a temp-file DB (or :memory:) seeded with a few memories via createMemoryCoreService().storeMemory, returning the same { db, service, projectId, dbPath } shape with an explicit test projectId. Mirror memory-core-service.spec.ts seeding.

    Create the test files. Fully implement tests/unit/cli/context.spec.ts (asserts createCliContext override injects temp dbPath + projectId, opens DB, returns service) and tests/unit/cli/output.spec.ts (asserts formatTable header/separators/ANSI-free, formatKeyValue label mapping). For the command spec files owned by later plans (stats.spec.ts, error-contract.spec.ts, install/uninstall/search/data-commands/forget/export-import.spec.ts), create them with a single `it.todo(...)` placeholder describing the behavior to implement, so the Wave 0 directory + files exist and `npm test` stays green. Each later plan converts its it.todo into real assertions.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/unit/cli tests/integration/cli tests/helpers --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/index.ts first line is "#!/usr/bin/env node"
    - src/cli/index.ts contains "new Command" and registers subcommands named run, install, uninstall, ping, search, list, show, forget, export, import, stats (grep each command name appears in a .command(...) call)
    - src/cli/index.ts ends with a program.parseAsync(...).catch handler that calls process.exit(1)
    - `npm run build` exits 0 (program + all command modules type-check, including placeholders)
    - tests/helpers/cliTestContext.ts exports createTestCliContext
    - tests/unit/cli/context.spec.ts and tests/unit/cli/output.spec.ts contain real assertions and exit 0
    - tests/integration/cli/ contains install.spec.ts, uninstall.spec.ts, search.spec.ts, data-commands.spec.ts, forget.spec.ts, export-import.spec.ts
    - tests/unit/cli/ contains stats.spec.ts and error-contract.spec.ts
    - `npm test` exits 0 (it.todo placeholders do not fail)
  </acceptance_criteria>
  <done>commander program registers all 11 subcommands wired to per-module actions (real or placeholder); Wave 0 test directory + helper + real context/output tests exist and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation → filesystem | createCliContext reads/writes ~/.sessionmem/ and resolves a migrations dir |
| build script → filesystem | copy-migrations.mjs writes into dist/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | migrationsDir resolution in context.ts | mitigate | Resolve from import.meta.url (package-relative), never from process.cwd(); avoids loading attacker-controlled migrations from the invocation directory (Pitfall 2) |
| T-05-02 | Information Disclosure | ~/.sessionmem/ directory + memories.db | accept | V4 partial — relies on OS filesystem perms; CLI runs as invoking user only; no new perms widened (ASVS L1, single-user local tool) |
| T-05-03 | Tampering | scripts/copy-migrations.mjs file copy | accept | Operates only on package-internal src/core/schema/migrations → dist; no user-supplied paths; build-time only |
| T-05-SC | Tampering | npm install commander | mitigate | commander audited [OK] in RESEARCH Package Legitimacy Audit (28k+ stars, no postinstall); no [ASSUMED]/[SUS] packages — no blocking checkpoint required |
</threat_model>

<verification>
- `npm run build` exits 0; dist/cli/index.js exists with shebang; dist migrations copied (4 .sql).
- `npm test` exits 0 — full suite green including new Wave 0 files.
- src/cli/context.ts resolves migrations via import.meta.url, not cwd.
- src/cli/output.ts contains no ANSI escapes.
</verification>

<success_criteria>
- Build pipeline produces a runnable dist/cli/index.js bin with copied migrations.
- createCliContext provides DB + service + cwd-derived projectId + dbPath, with a test/override injection seam.
- formatTable / formatKeyValue produce ANSI-free output per D-07/D-08.
- commander program registers all 11 subcommands; later plans only touch their own command modules.
- All Wave 0 test files exist; suite is green.
</success_criteria>

<output>
Create `.planning/phases/05-cli-lifecycle-and-data-operations/05-01-SUMMARY.md` when done.
</output>
