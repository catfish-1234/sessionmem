---
phase: 05-cli-lifecycle-and-data-operations
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/cli/commands/search.ts
  - src/cli/commands/list.ts
  - src/cli/commands/show.ts
  - src/cli/commands/stats.ts
  - tests/integration/cli/search.spec.ts
  - tests/integration/cli/data-commands.spec.ts
  - tests/unit/cli/stats.spec.ts
  - tests/unit/cli/error-contract.spec.ts
autonomous: true
requirements: [CLI-03, CLI-04, CLI-06]
user_setup: []

must_haves:
  truths:
    - "`sessionmem search \"<query>\"` prints ranked memories as a plain-text table with no ANSI (D-07)"
    - "`sessionmem list` prints all project memories as a plain-text table (D-07)"
    - "`sessionmem show <id>` prints all fields as plain key: value lines (D-08)"
    - "`sessionmem show <missing-id>` prints the error message to stderr and exits non-zero (D-03)"
    - "`sessionmem stats` prints memory count, db_size_bytes, and total_content_tokens (CLI-06)"
    - "Every read command prints only error.message (not the JSON envelope) to stderr on failure (D-03)"
  artifacts:
    - path: "src/cli/commands/search.ts"
      provides: "search command: retrieveMemories -> table"
      exports: ["searchCommand"]
    - path: "src/cli/commands/list.ts"
      provides: "list command: listMemories -> table"
      exports: ["listCommand"]
    - path: "src/cli/commands/show.ts"
      provides: "show command: getMemory -> key:value"
      exports: ["showCommand"]
    - path: "src/cli/commands/stats.ts"
      provides: "stats command: count + db size + token total"
      exports: ["statsCommand"]
  key_links:
    - from: "src/cli/commands/stats.ts"
      to: "src/core/injection/tokenBudget.ts"
      via: "countTokens over memory content"
      pattern: "countTokens"
    - from: "src/cli/commands/search.ts"
      to: "src/cli/output.ts"
      via: "formatTable(result.memories)"
      pattern: "formatTable"
---

<objective>
Implement the read/query CLI surface: search (ranked), list, show, and stats. Each is a thin formatter over an existing memoryCoreService method plus the Plan 01 output helpers; stats additionally fills the CLI-06 gap (DB size + token totals).

Purpose: CLI-03 (search), CLI-04 (list, show), CLI-06 (stats). No new data logic — wire to existing zod-validated service methods and format per D-07/D-08.
Output: search.ts, list.ts, show.ts, stats.ts, plus real tests replacing Wave 0 it.todo for search, data-commands (list/show), stats, and the error-contract unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-CONTEXT.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-RESEARCH.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-PATTERNS.md
@.planning/phases/05-cli-lifecycle-and-data-operations/05-01-SUMMARY.md

<interfaces>
From src/cli/context.ts (Plan 01):
```typescript
export function createCliContext(overrides?): { db; service; projectId: string; dbPath: string };
```

From src/cli/output.ts (Plan 01):
```typescript
export function formatTable(rows): string;       // D-07 ID | importance | date | preview, no ANSI
export function formatKeyValue(memory): string;  // D-08 snake_case key: value lines
```

From src/core/api/memoryCoreService.ts (all return { ok, ... } | { ok:false, error:{ code, message } }):
```typescript
retrieveMemories({ projectId, query, limit }) -> { ok, memories: RetrievedMemoryDto[], total }
listMemories({ projectId }) -> { ok, memories: MemoryDto[], total }
getMemory({ projectId, memoryId }) -> { ok, memory: MemoryDto }   // NOT_FOUND -> DomainError envelope
stats({ projectId }) -> { ok, totalMemories, totalSessionEvents }
```

From src/core/storage/memoryRepo.ts (line 75):
```typescript
export function listMemoriesByProject(db: Database, projectId: string): MemoryRow[]; // row.content
```

From src/core/injection/tokenBudget.ts (line 19):
```typescript
export function countTokens(text: string): number;
```

Universal error contract (D-03): if (!result.ok) { console.error(result.error.message); process.exit(1); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement search and list (table output)</name>
  <files>src/cli/commands/search.ts, src/cli/commands/list.ts, tests/integration/cli/search.spec.ts, tests/integration/cli/data-commands.spec.ts</files>
  <read_first>
    - src/cli/context.ts (createCliContext — accept injected context for testability)
    - src/cli/output.ts (formatTable signature + columns)
    - src/core/api/memoryCoreService.ts (retrieveMemories + listMemories request/response shapes)
    - tests/helpers/cliTestContext.ts (createTestCliContext seeding)
    - .planning/phases/05-cli-lifecycle-and-data-operations/05-PATTERNS.md (search/list target shapes; universal envelope-unwrap)
  </read_first>
  <action>
    Create src/cli/commands/search.ts exporting searchCommand(query, options) where options may carry --limit (registered in index.ts as search <query> [--limit <n>]). Build context (createCliContext, injectable for tests), call ctx.service.retrieveMemories({ projectId: ctx.projectId, query, limit: options?.limit ?? undefined }) — let the service default limit (20) when unset (A5). Unwrap envelope: on !ok print error.message to stderr + process.exit(1); on ok print formatTable(result.memories) to stdout via console.log.

    Create src/cli/commands/list.ts exporting listCommand() — build context, call ctx.service.listMemories({ projectId: ctx.projectId }), same envelope unwrap, print formatTable(result.memories). Both commands accept an optional injected ctx parameter (default to createCliContext()) so tests pass a temp-DB context.

    Tests: search.spec.ts — seed a temp-DB context via createTestCliContext, call searchCommand with a query, spy console.log, assert output contains " | " separators and is ANSI-free (no "\x1b["). data-commands.spec.ts — add the list portion: assert listCommand prints a table with the seeded memory IDs. Replace the Wave 0 it.todo for these.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cli/search.spec.ts tests/integration/cli/data-commands.spec.ts --reporter=dot && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/commands/search.ts exports searchCommand and contains "formatTable" and "retrieveMemories"
    - src/cli/commands/list.ts exports listCommand and contains "formatTable" and "listMemories"
    - both commands accept an injectable context parameter (verified by tests passing a test context)
    - search output and list output contain " | " and no "\x1b[" sequences
    - on service !ok both print result.error.message (not the JSON envelope) to stderr and exit non-zero
    - search.spec.ts and the list portion of data-commands.spec.ts contain real assertions and exit 0
  </acceptance_criteria>
  <done>search and list format service results as ANSI-free tables and surface errors per D-03; covered by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement show and stats (key:value + metrics) with error-contract test</name>
  <files>src/cli/commands/show.ts, src/cli/commands/stats.ts, tests/integration/cli/data-commands.spec.ts, tests/unit/cli/stats.spec.ts, tests/unit/cli/error-contract.spec.ts</files>
  <read_first>
    - src/cli/output.ts (formatKeyValue field mapping)
    - src/core/api/memoryCoreService.ts (getMemory NOT_FOUND envelope; stats returns count only — gap)
    - src/core/storage/memoryRepo.ts (listMemoriesByProject line 75 for token aggregation)
    - src/core/injection/tokenBudget.ts (countTokens line 19)
    - tests/helpers/cliTestContext.ts (createTestCliContext)
  </read_first>
  <action>
    Create src/cli/commands/show.ts exporting showCommand(id, ctx?) — build/accept context, call ctx.service.getMemory({ projectId: ctx.projectId, memoryId: id }); on !ok (includes NOT_FOUND) print error.message to stderr + process.exit(1); on ok print formatKeyValue(result.memory) to stdout (D-08 ordered snake_case lines).

    Create src/cli/commands/stats.ts exporting statsCommand(ctx?) — call ctx.service.stats({ projectId: ctx.projectId }); on !ok print error.message + exit non-zero. On ok, fill the CLI-06 gap: sizeBytes = statSync(ctx.dbPath).size; totalTokens = listMemoriesByProject(ctx.db, ctx.projectId).reduce((sum, m) => sum + countTokens(m.content), 0). Print three lines via process.stdout.write: "memories: {totalMemories}", "db_size_bytes: {sizeBytes}", "total_content_tokens: {totalTokens}".

    Tests: data-commands.spec.ts — add show portion: showCommand for a seeded id prints key:value lines including "source_adapter:"; showCommand for a missing id exits non-zero with the error message on stderr (spy process.exit + console.error). stats.spec.ts — seed N memories, assert output contains "memories:", "db_size_bytes:", and "total_content_tokens:" with the token total > 0. error-contract.spec.ts (unit) — assert the shared contract: a command given a service that returns { ok:false, error:{ code, message } } prints ONLY error.message to console.error and triggers process.exit(1) (use showCommand or a small harness with an injected failing service; mirror run-command.spec.ts envelope assertion). Replace all remaining it.todo placeholders.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cli/data-commands.spec.ts tests/unit/cli/stats.spec.ts tests/unit/cli/error-contract.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/commands/show.ts exports showCommand and contains "formatKeyValue" and "getMemory"
    - src/cli/commands/stats.ts exports statsCommand and contains "statSync", "countTokens", and "listMemoriesByProject"
    - stats output contains the literal lines "memories:", "db_size_bytes:", and "total_content_tokens:"
    - show on a missing id exits non-zero and prints the error message to stderr (asserted in data-commands.spec.ts)
    - error-contract.spec.ts asserts a failing service path prints only error.message and calls process.exit(1)
    - data-commands.spec.ts, stats.spec.ts, error-contract.spec.ts contain real assertions (no remaining it.todo) and exit 0
  </acceptance_criteria>
  <done>show prints ordered key:value fields; stats reports count + DB size + token total (CLI-06 gap filled); the D-03 error contract is unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → service queries | user-supplied query/id reach zod-validated service methods |
| CLI → memories.db read | stats reads DB file size and content for token counting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | search/show query + id inputs | mitigate | All inputs flow through existing zod schemas at the service boundary (V5); queries use better-sqlite3 prepared statements — never string-concat SQL |
| T-05-08 | Information Disclosure | error.message to stderr | accept | D-03 prints only human-readable error.message, not the internal envelope/stack; messages are non-sensitive domain errors (NOT_FOUND, VALIDATION) |
| T-05-09 | Information Disclosure | stats/show expose stored content | accept | CLI runs as invoking user reading their own ~/.sessionmem DB (V4 OS perms); no new exposure beyond existing data ownership |
</threat_model>

<verification>
- `npm run build` exits 0.
- search/list/show/stats specs + error-contract spec pass.
- table output ANSI-free; stats includes db_size_bytes + token total; show NOT_FOUND exits non-zero.
</verification>

<success_criteria>
- CLI-03: search returns ranked rows in a plain table.
- CLI-04 (read half): list prints a table; show prints ordered key:value fields; missing id errors per D-03.
- CLI-06: stats reports memory count, DB size, and token totals.
</success_criteria>

<output>
Create `.planning/phases/05-cli-lifecycle-and-data-operations/05-03-SUMMARY.md` when done.
</output>
