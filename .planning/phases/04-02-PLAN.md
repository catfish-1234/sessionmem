# Phase 4, Plan 2: Claude Code, Codex, Antigravity, QCoder, and Generic MCP

## Goal
Implement adapters and config-injection logic for hosts using global configurations or standard MCP pathways.

## Requirements Mapped
- PLAT-01: Claude Code
- PLAT-02: Codex
- PLAT-06: Antigravity
- PLAT-07: QCoder
- PLAT-08: Generic MCP

## Proposed Changes
1. **`src/adapters/global/claudeCode.ts`**
   - Implement adapter.
   - Add installer logic to inject the MCP server into `~/.claude.json`.

2. **`src/adapters/global/antigravity.ts`**
   - Implement adapter.
   - Add installer logic for Antigravity's global configuration.

3. **`src/adapters/global/codex.ts` & `qcoder.ts`**
   - Implement basic stdio adapters.
   - Installer hooks for their respective configuration points.

4. **`src/adapters/generic.ts`**
   - Default `GenericMCPAdapter` handling standard stdio without host-specific assumptions.

## Verification
- Mock file system tests verifying `~/.claude.json` injection and cleanup.
