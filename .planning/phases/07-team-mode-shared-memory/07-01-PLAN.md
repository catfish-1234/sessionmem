---
phase: 07-team-mode-shared-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/schema/migrations/005_team_provenance.sql
  - src/core/storage/types.ts
  - src/core/storage/memoryRepo.ts
  - src/core/storage/memorySearchRepo.ts
  - src/core/retrieve/retrieveMemories.ts
  - src/core/api/memoryCoreService.ts
  - src/core/api/contracts.ts
  - src/cli/context.ts
  - tests/integration/storage/schema.spec.ts
  - tests/unit/core/author-stamp.spec.ts
autonomous: true
requirements: [TEAM-02]
must_haves:
  truths:
    - "After migration 005, the memories table has author and origin_project_id columns and existing rows survive"
    - "Every memory write (storeMemory, importMemories, session summary) stamps author = local OS username"
    - "author and origin_project_id flow through every SELECT into MemoryRecord, RetrievedMemoryCandidate, and DTOs without becoming undefined"
  artifacts:
    - path: "src/core/schema/migrations/005_team_provenance.sql"
      provides: "ALTER TABLE adding author + origin_project_id columns"
      contains: "ALTER TABLE memories ADD COLUMN author"
    - path: "src/core/storage/types.ts"
      provides: "author + origin_project_id on MemoryRecord and InsertMemoryInput"
      contains: "author"
    - path: "src/cli/context.ts"
      provides: "username field on CliContext via os.userInfo()"
      contains: "username"
    - path: "tests/unit/core/author-stamp.spec.ts"
      provides: "Test that every write path stamps author"
  key_links:
    - from: "src/core/storage/memorySearchRepo.ts"
      to: "memories.author column"
      via: "SELECT column list"
      pattern: "author"
    - from: "src/core/retrieve/retrieveMemories.ts"
      to: "RetrievedMemoryCandidate.author"
      via: "candidate mapper"
      pattern: "author"
---

<objective>
Lay the schema + type foundation for team-mode provenance: add a `005_team_provenance.sql` migration that adds `author` and `origin_project_id` columns to the `memories` table, then thread both columns through every storage type, SELECT statement, INSERT/upsert column list, DTO, and retrieval candidate so they never silently drop to `undefined`. Resolve the local OS username once per CLI invocation and stamp `author` on every memory write path (D-07).

Purpose: This is the dependency root of the entire phase. The conflict/merge logic (Plan 03), the team CLI (Plan 02), and the injection annotation (Plan 04) all read the `author` column. Per RESEARCH Pitfall 3, missing a single SELECT silently breaks D-10 downstream. This plan treats column-threading as an exhaustive checklist.

Output: Migration 005, updated storage/service/retrieve types and SQL, a `username` field on `CliContext`, and `author` stamped at write time across storeMemory / importMemories / session-summary upsert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-mode-shared-memory/07-RESEARCH.md
@.planning/phases/07-team-mode-shared-memory/07-PATTERNS.md
@.planning/phases/07-team-mode-shared-memory/07-CONTEXT.md

<interfaces>
<!-- Current contracts the executor builds against. Threading targets are listed in the column-threading checklist below. -->

Current `memories` table (src/core/schema/migrations/001_initial.sql) has NO author/origin_project_id columns. Latest migration is `004_memory_feedback.sql`; new file is `005_team_provenance.sql`. `runMigrations.ts` applies `*.sql` sorted by localeCompare, tracked in `_migrations`, each in a transaction.

MemoryRecord (src/core/storage/types.ts:11) fields:
  id, project_id, session_id, source_adapter, kind, content, normalized_content,
  importance, embedding, embedding_dim, embedding_version, created_at, updated_at

RetrievedMemoryCandidate (src/core/retrieve/retrieveMemories.ts:25) — same fields minus embedding, plus semantic + score. NO author yet.

MemoryDto / RetrievedMemoryDto (src/core/api/memoryCoreService.ts:58, 74) — camelCase mirror of MemoryRecord.

context.ts:30 deriveProjectId() is the shape-template for a sibling localUsername().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 005 + storage type/SQL threading</name>
  <files>src/core/schema/migrations/005_team_provenance.sql, src/core/storage/types.ts, src/core/storage/memoryRepo.ts, src/core/storage/memorySearchRepo.ts, src/core/retrieve/retrieveMemories.ts, tests/integration/storage/schema.spec.ts</files>
  <read_first>
    - src/core/schema/migrations/004_memory_feedback.sql (migration file convention)
    - src/core/schema/migrations/001_initial.sql (current memories DDL, DEFAULT/strftime conventions)
    - src/core/schema/runMigrations.ts (how migrations are discovered + applied)
    - src/core/storage/types.ts (MemoryRecord, InsertMemoryInput to extend)
    - src/core/storage/memoryRepo.ts (insertMemory, upsertSessionSummaryMemory, listMemoriesByProject SELECT + INSERT column lists)
    - src/core/storage/memorySearchRepo.ts (searchMemoryCandidates SELECT column list, MemorySearchRow/MemorySearchCandidate types)
    - src/core/retrieve/retrieveMemories.ts (RetrievedMemoryCandidate interface + candidate mapper)
    - tests/integration/storage/schema.spec.ts (existing schema test to extend)
  </read_first>
  <behavior>
    - Test: opening a DB with an existing pre-005 row, then running migrations, leaves the row intact and adds author (defaulting to '') + origin_project_id (NULL).
    - Test: a freshly stored memory's row exposes author and origin_project_id columns selectable from memories.
    - Test: listMemoriesByProject and searchMemoryCandidates return objects whose author field is a string (not undefined) and origin_project_id is string|null.
  </behavior>
  <action>
    Create `src/core/schema/migrations/005_team_provenance.sql` with two `ALTER TABLE memories ADD COLUMN` statements: `author TEXT NOT NULL DEFAULT ''` and `origin_project_id TEXT` (nullable — only set on synced rows). NOT NULL requires the DEFAULT clause (RESEARCH Pitfall 1). Do NOT backfill historical rows in SQL (local username is not available in static SQL; leaving '' is the safe sentinel — Open Q1).

    Thread both columns everywhere they must travel. In `src/core/storage/types.ts`: add `author: string;` and `origin_project_id: string | null;` to `MemoryRecord`, and `author?: string;` + `origin_project_id?: string | null;` to `InsertMemoryInput`. In `src/core/storage/memoryRepo.ts`: add `author, origin_project_id` to the INSERT column lists and `@author, @origin_project_id` param placeholders in `insertMemory` and `upsertSessionSummaryMemory` (and add `author = excluded.author`, `origin_project_id = excluded.origin_project_id` to that upsert's DO UPDATE SET); add `author, origin_project_id` to the `listMemoriesByProject` SELECT list; in `toParams`, default `author` to `''` and `origin_project_id` to `null` when absent. In `src/core/storage/memorySearchRepo.ts`: add `author, origin_project_id` to the `searchMemoryCandidates` SELECT and to `MemorySearchRow`/`MemorySearchCandidate` types. In `src/core/retrieve/retrieveMemories.ts`: add `author: string;` and `origin_project_id: string | null;` to `RetrievedMemoryCandidate` and carry both through the candidate mapper that builds it from search candidates.

    Extend `tests/integration/storage/schema.spec.ts` to cover the migration-adds-columns + existing-row-survival behavior described above.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/storage/schema.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/schema/migrations/005_team_provenance.sql` contains `ALTER TABLE memories ADD COLUMN author TEXT NOT NULL DEFAULT ''` and `ALTER TABLE memories ADD COLUMN origin_project_id TEXT`.
    - `grep -n "author" src/core/storage/memorySearchRepo.ts` shows `author` present in the SELECT column list.
    - `RetrievedMemoryCandidate` in `retrieveMemories.ts` declares an `author` field of type string.
    - `npx vitest run tests/integration/storage/schema.spec.ts` passes, including a case asserting a pre-existing row survives the 005 migration with `author === ''`.
    - `npx tsc --noEmit` reports no errors from the new/changed type fields.
  </acceptance_criteria>
  <done>Migration 005 adds both columns; every storage-layer SELECT/INSERT/type carries author + origin_project_id; schema spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author stamping at write paths + DTO threading + username resolution</name>
  <files>src/core/api/memoryCoreService.ts, src/core/api/contracts.ts, src/cli/context.ts, tests/unit/core/author-stamp.spec.ts</files>
  <read_first>
    - src/cli/context.ts (deriveProjectId shape-template, CliContext interface, createCliContext)
    - src/core/api/memoryCoreService.ts (toMemoryDto:101, toRetrievedMemoryDto:119, getMemoryById SELECT:144, storeMemory:262, importMemories INSERT+stmt.run:384-478)
    - src/core/api/contracts.ts (importMemoryRecordSchema:117, importMemoriesRequestSchema:129)
    - src/core/storage/types.ts (the author/origin_project_id fields added in Task 1)
    - tests/helpers/cliTestContext.ts (test context + storeMemory seam used by the new test)
  </read_first>
  <behavior>
    - Test: storeMemory persists a row whose author equals the username passed via the service/context (D-07), not ''.
    - Test: importMemories persists rows whose author is set (preserves an incoming author when present, else stamps local username).
    - Test: getMemoryById / toMemoryDto expose author on the returned DTO.
    - Test: localUsername() sanitizes a username containing a path separator to a filename-safe token.
  </behavior>
  <action>
    Add OS-username resolution to `src/cli/context.ts`: a `localUsername()` helper using `userInfo().username` from `node:os`, sanitized with `.replace(/[^A-Za-z0-9._-]/g, "_")` and falling back to `"user"` if empty (RESEARCH Code Examples, D-05). Add `username: string` to the `CliContext` interface and set it in `createCliContext` (allow an override on `CliContextOverrides` for the test seam, mirroring `projectId`).

    Stamp `author` on every write path (D-07). The service needs the username: add an optional `username?: string` to `CreateMemoryCoreServiceDeps` (default to `userInfo().username` sanitized the same way, or `''` — pick a single resolution point and reuse it) so the service can stamp author without a CLI dependency. In `storeMemory` (memoryCoreService.ts:262) pass `author: <resolved username>` (and `origin_project_id: null` for locally-authored rows) into the `insertMemory` call. In `importMemories` (memoryCoreService.ts:384) add `author` and `origin_project_id` to the INSERT column list, the `ON CONFLICT(id) DO UPDATE SET` clause, and the `stmt.run({...})` params — for plain import (not pull), stamp `author` = each record's incoming `author` if present else the resolved local username, and `origin_project_id` = the record's `originProjectId` if present else null. Update `upsertSessionSummaryMemory` callers (session-end path) to pass author similarly.

    Thread `author` + `originProjectId` into DTOs: add `author` and `originProjectId` to `MemoryDto` and `RetrievedMemoryDto` interfaces and map them in `toMemoryDto` (sibling to `record.source_adapter`, line 106) and `toRetrievedMemoryDto`. Add `author, origin_project_id` to the `getMemoryById` SELECT column list.

    In `src/core/api/contracts.ts`: add `author: z.string().optional()` and `originProjectId: z.string().optional()` to `importMemoryRecordSchema` (OPTIONAL for backward-compat with old exports lacking them — A3).

    Create `tests/unit/core/author-stamp.spec.ts` covering the four behaviors above using `createMemoryCoreService` directly with an explicit username dep.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/core/author-stamp.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `CliContext` interface includes `username: string`; `createCliContext` populates it from `userInfo().username`.
    - `importMemoryRecordSchema` includes optional `author` and `originProjectId` fields (`grep -n "author" src/core/api/contracts.ts` matches).
    - `toMemoryDto` maps `author` from the record (`grep -n "author" src/core/api/memoryCoreService.ts` shows it in the DTO mappers and the storeMemory/importMemories insert paths).
    - `npx vitest run tests/unit/core/author-stamp.spec.ts` passes, asserting storeMemory writes a non-empty author equal to the configured username.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Username resolves once per invocation; author is stamped on all write paths and surfaced through DTOs; contracts accept optional author/originProjectId; author-stamp spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invoker → SQLite | Operator-controlled username/projectId env seams; same trust level as the process (T-05-15 precedent). |
| Migration runner → existing DB | DDL alters an existing user-owned DB; must not destroy existing rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | 005 migration on existing DB | mitigate | `ADD COLUMN ... NOT NULL DEFAULT ''` so existing rows are preserved (no destructive rewrite); schema spec asserts pre-existing row survives. |
| T-07-04 | Tampering | author column threading | mitigate | Exhaustive SELECT/INSERT/DTO threading + schema/author-stamp tests so `author` cannot silently become undefined (RESEARCH Pitfall 3). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Phase 7 installs no new packages (RESEARCH Package Legitimacy Audit: none). No install task; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with the new columns/types.
- `npm test` full suite green (no regression from threaded columns).
- Manual DB check optional: a freshly stored memory row has `author` non-empty and `origin_project_id` NULL.
</verification>

<success_criteria>
- Migration 005 applies cleanly on an existing DB; both columns present; existing rows intact.
- `author` + `origin_project_id` threaded through every storage SELECT/INSERT, retrieval candidate, and DTO.
- `username` resolvable on `CliContext`; author stamped on storeMemory / importMemories / session-summary writes.
- `tests/integration/storage/schema.spec.ts` and `tests/unit/core/author-stamp.spec.ts` green.
</success_criteria>

<output>
Create `.planning/phases/07-team-mode-shared-memory/07-01-SUMMARY.md` when done
</output>
