---
phase: 07-team-mode-shared-memory
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-03"]
files_modified:
  - src/core/injection/formatStartupInjection.ts
  - docs/team-mode.md
  - tests/unit/injection/author-annotation.spec.ts
  - tests/integration/docs/team-docs.spec.ts
autonomous: true
requirements: [TEAM-02]
must_haves:
  truths:
    - "Startup injection prefixes a memory's content with 'author: ' only when the memory's author differs from the local username (D-10)"
    - "Memories authored by the local user are rendered with no author prefix"
    - "docs/team-mode.md documents setup, the sync/team commands, the shared-dir trust boundary, and disable/recovery behavior"
  artifacts:
    - path: "src/core/injection/formatStartupInjection.ts"
      provides: "author-aware formatLine with localUsername option"
      contains: "author"
    - path: "docs/team-mode.md"
      provides: "Team mode setup + failure-recovery documentation"
      contains: "team enable"
    - path: "tests/unit/injection/author-annotation.spec.ts"
      provides: "annotation present/absent coverage"
    - path: "tests/integration/docs/team-docs.spec.ts"
      provides: "doc-coverage smoke test"
  key_links:
    - from: "src/core/injection/formatStartupInjection.ts"
      to: "RetrievedMemoryCandidate.author"
      via: "formatLine author prefix"
      pattern: "author"
---

<objective>
Surface teammate provenance in-context (D-10) and document team mode for users (success criterion 5). Update `formatStartupInjection` so a retrieved memory's content is prefixed with `${author}: ` when the memory's `author` differs from the local username (e.g. `- [decision] alice: decided to use X (...)`), and rendered with no prefix when the memory is locally authored. Thread the local username into the formatter via its options. Then write `docs/team-mode.md` covering setup, the `team`/`sync` command surface, the shared-filesystem trust boundary, and disable/recovery behavior, guarded by a doc-coverage smoke test.

Purpose: Closes the last two TEAM-02 / success-criterion items — the agent can now see who authored a shared memory, and users have setup + failure-recovery guidance. Depends on Plan 01 (author threaded into `RetrievedMemoryCandidate`) and Plan 03 (the `sync`/`team` commands the docs describe).

Output: Author-aware injection formatter, `docs/team-mode.md`, and unit + doc-coverage specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-mode-shared-memory/07-RESEARCH.md
@.planning/phases/07-team-mode-shared-memory/07-PATTERNS.md
@.planning/phases/07-team-mode-shared-memory/07-CONTEXT.md
@.planning/phases/07-team-mode-shared-memory/07-01-SUMMARY.md
@.planning/phases/07-team-mode-shared-memory/07-03-SUMMARY.md

<interfaces>
<!-- From Plan 01: RetrievedMemoryCandidate now has author: string. From Plan 03: sync/team commands exist. -->

formatStartupInjection.ts:
  FormatStartupInjectionOptions { tokenCap?: number }   // EXTEND with localUsername?: string
  formatLine(entry: IncludedMemory): string   // line 59 — single render site for D-10
    current body: `- [${memory.kind}] ${entry.content} (score ...; source=${memory.source_adapter}; date=${memory.updated_at})`
  formatStartupInjection(rankedMemories, options) is the only exported entry; threads options into formatLine.

RetrievedMemoryCandidate now exposes `author: string` (Plan 01).

Existing doc-coverage model: tests/integration/docs/privacy-docs.spec.ts asserts a doc file exists and contains required topic/command tokens. docs/privacy-and-retention.md and docs/cloud-summarization.md are the prose-style models.

Security framing (RESEARCH Security Domain): team mode's trust boundary is the shared filesystem's OS-level permissions, not the app. `author` is advisory (a teammate could spoof it). Docs must state this (T-07-11 disposition).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author annotation in startup injection</name>
  <files>src/core/injection/formatStartupInjection.ts, tests/unit/injection/author-annotation.spec.ts</files>
  <read_first>
    - src/core/injection/formatStartupInjection.ts (full file — FormatStartupInjectionOptions, formatLine:59, formatStartupInjection entry that threads options)
    - src/core/retrieve/retrieveMemories.ts (RetrievedMemoryCandidate.author added in Plan 01)
    - tests/unit (existing injection test structure, if any, for fixture shape of RetrievedMemoryCandidate)
  </read_first>
  <behavior>
    - Test: a memory with author "alice" and localUsername "bob" renders content prefixed `alice: ` (e.g. `- [decision] alice: decided to use X (...)`).
    - Test: a memory with author equal to localUsername renders with NO prefix (content unchanged).
    - Test: a memory with empty/missing author renders with no prefix (locally-authored legacy rows from the '' default).
    - Test: when localUsername is not provided in options, no prefix is ever added (safe default — never leaks a wrong attribution).
  </behavior>
  <action>
    Add `localUsername?: string` to `FormatStartupInjectionOptions`. Thread it from `formatStartupInjection` into `formatLine` (either pass it as a second arg or close over it). In `formatLine` (line 59), compute the rendered body: when `memory.author` is non-empty AND `localUsername` is set AND `memory.author !== localUsername`, prefix the content with `${memory.author}: ` (D-10); otherwise render `entry.content` unchanged. Keep the trailing score/source/date suffix exactly as-is. Do not alter sorting, token-budget trimming, or the HEADER.

    Create `tests/unit/injection/author-annotation.spec.ts` covering the four behaviors above with `RetrievedMemoryCandidate` fixtures (include `author`, `score`, `kind`, etc.).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/injection/author-annotation.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `FormatStartupInjectionOptions` includes `localUsername?: string`.
    - `formatLine` prefixes content with `${author}: ` only when `author` is non-empty and differs from `localUsername` (`grep -n "author" src/core/injection/formatStartupInjection.ts` shows the conditional).
    - `npx vitest run tests/unit/injection/author-annotation.spec.ts` passes all four behaviors, including the no-prefix-when-equal and no-prefix-when-localUsername-absent cases.
    - `npx tsc --noEmit` clean; existing injection-quality tests still green.
  </acceptance_criteria>
  <done>Injection prefixes teammate-authored content with the author name only when it differs from the local user; local/legacy rows render unchanged; annotation spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Team mode documentation + doc-coverage test</name>
  <files>docs/team-mode.md, tests/integration/docs/team-docs.spec.ts</files>
  <read_first>
    - docs/privacy-and-retention.md (prose style + structure model)
    - tests/integration/docs/privacy-docs.spec.ts (doc-coverage smoke-test pattern to copy)
    - .planning/phases/07-team-mode-shared-memory/07-CONTEXT.md (D-01..D-16 — the exact behaviors to document)
    - .planning/phases/07-team-mode-shared-memory/07-RESEARCH.md (Security Domain — trust-boundary framing to document)
  </read_first>
  <action>
    Write `docs/team-mode.md` documenting: (1) Setup — `sessionmem team enable <shared-path>` where the path is a shared/network directory all teammates can read/write; the per-project, per-user file layout `{sharedPath}/{project_id}/{username}.json` (D-03). (2) Usage — `sessionmem sync` pushes a full snapshot of local memories and pulls teammates' memories in one command (D-02/D-04/D-16), with the exact `Pushed N memories, pulled M new + updated K from teammates.` summary. (3) Provenance — memories carry `author` (OS username) and `created_at`; teammate-authored memories show an `author:` prefix in injected context (D-05/D-07/D-08/D-10). (4) Conflict behavior — last-write-wins by id, importance is preserved when the local value is higher, cross-project id collisions are skipped, and pulled content is re-redacted (D-09/D-11/D-12). (5) Disable / recovery — `sessionmem team disable` stops sync and KEEPS already-pulled memories (no data loss); `sessionmem team disable --remove-team-memories` reverts to local-only by deleting teammate-authored memories (D-13/D-15, TEAM-03). (6) Trust boundary / security — state plainly that team mode trusts everyone with write access to the shared path; `author` is advisory provenance, not an authentication control; the OS filesystem ACLs on the shared directory are the real boundary (RESEARCH Security Domain, T-07-11). (7) Failure recovery — behavior on a missing/unwritable shared path (stderr + non-zero exit), and that one corrupt teammate file is skipped without aborting the pull.

    Create `tests/integration/docs/team-docs.spec.ts` modeled on `privacy-docs.spec.ts`: assert `docs/team-mode.md` exists and contains required command/topic tokens — at minimum `team enable`, `team disable`, `--remove-team-memories`, `sync`, `author`, `last-write-wins` (or the conflict phrasing used), `shared` (path/dir), and a trust-boundary mention.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/docs/team-docs.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `docs/team-mode.md` exists and documents setup, sync usage with the D-16 summary string, provenance/annotation, conflict behavior, disable-with/without-data-loss, the shared-dir trust boundary, and failure recovery.
    - `tests/integration/docs/team-docs.spec.ts` asserts the doc exists and contains the required tokens (`team enable`, `team disable`, `--remove-team-memories`, `sync`, `author`, a conflict-behavior token, a trust-boundary mention).
    - `npx vitest run tests/integration/docs/team-docs.spec.ts` passes.
  </acceptance_criteria>
  <done>docs/team-mode.md covers setup through failure-recovery including the trust-boundary caveat; doc-coverage spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teammate-supplied author → injected context | The `author` value rendered in injection originates from teammate JSON and is advisory only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Spoofing / Information Disclosure | author annotation in injection | mitigate (display) / accept (trust) | Annotation only prefixes when author differs from local user; a missing localUsername yields no prefix (never a wrong attribution). The deeper spoofing risk (a teammate setting a fake author) is accepted and documented as advisory provenance (T-07-11) in docs/team-mode.md. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npm test` full suite green (injection-quality + doc-coverage).
- Manual: a seeded teammate-authored memory appears with the `author:` prefix in startup injection output.
</verification>

<success_criteria>
- Startup injection annotates teammate-authored memories with the author name and omits the prefix for local/legacy rows (D-10, TEAM-02).
- `docs/team-mode.md` documents setup, sync, provenance, conflict handling, disable/recovery, and the trust boundary (success criterion 5).
- `tests/unit/injection/author-annotation.spec.ts` and `tests/integration/docs/team-docs.spec.ts` green.
</success_criteria>

<output>
Create `.planning/phases/07-team-mode-shared-memory/07-04-SUMMARY.md` when done
</output>
