---
phase: 07-team-mode-shared-memory
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/core/config/policyConfig.ts
  - src/cli/commands/team.ts
  - src/cli/index.ts
  - tests/integration/cli/team.spec.ts
autonomous: true
requirements: [TEAM-03]
must_haves:
  truths:
    - "team enable <path> persists { team: { enabled: true, sharedPath } } to config.json and team status reads it back"
    - "team disable (default) flips enabled to false and KEEPS already-pulled teammate rows (no data loss)"
    - "team disable --remove-team-memories deletes rows where author != local username"
  artifacts:
    - path: "src/core/config/policyConfig.ts"
      provides: "team config section (enabled, sharedPath) with strict-write/strip-read + resolveTeamConfig"
      contains: "team"
    - path: "src/cli/commands/team.ts"
      provides: "teamEnableCommand / teamDisableCommand / teamStatusCommand"
      exports: ["teamEnableCommand", "teamDisableCommand", "teamStatusCommand"]
    - path: "tests/integration/cli/team.spec.ts"
      provides: "enable/disable/status + --remove-team-memories coverage"
  key_links:
    - from: "src/cli/commands/team.ts"
      to: "writePolicyConfig"
      via: "team section persistence"
      pattern: "writePolicyConfig"
    - from: "src/cli/index.ts"
      to: "team command group"
      via: "program.command(\"team\")"
      pattern: "team"
---

<objective>
Add the team-mode config surface and CLI command group. Extend `policyConfig.ts` with a nested `team` section (`{ enabled, sharedPath }`) following the existing strict-write / strip-read / safe-default discipline (D-14), and add a `sessionmem team` command group with `enable <path>`, `disable [--remove-team-memories]`, and `status` subcommands (D-13). `team disable` defaults to no data loss — it stops sync but keeps pulled rows; the `--remove-team-memories` flag does a full local-only revert by deleting `author != local username` rows (D-15, TEAM-03).

Purpose: Establishes how team mode is turned on/off and where the shared path lives, so the `sync` command (Plan 03) can read `team.enabled`/`team.sharedPath` and no-op cleanly when disabled. Delivers TEAM-03's disable-without-data-loss guarantee.

Output: Extended `policyConfig.ts`, new `team.ts` command module, registered `team` group in `index.ts`, and `team.spec.ts` integration coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-mode-shared-memory/07-RESEARCH.md
@.planning/phases/07-team-mode-shared-memory/07-PATTERNS.md
@.planning/phases/07-team-mode-shared-memory/07-CONTEXT.md
@.planning/phases/07-team-mode-shared-memory/07-01-SUMMARY.md

<interfaces>
<!-- Existing contracts to extend. From Plan 01: author column + CliContext.username exist. -->

policyConfig.ts (current):
  DEFAULT_POLICY_CONFIG = { retentionDays: 90, redactionEnabled: true }
  policyConfigShape = { retentionDays, redactionEnabled }  // flat scalars with .default()
  policyConfigSchema = z.object(shape).strict()    // write — rejects unknown keys
  policyConfigReadSchema = z.object(shape).strip() // read — drops unknown keys
  readPolicyConfig(filePath) -> PolicyConfig (safe-default on any failure)
  writePolicyConfig(filePath, Partial<PolicyConfig>) -> PolicyConfig (strict partial-merge)
  resolvePolicySettings({override?, config?}) -> per-key flat resolve (override>config>default)
  configFilePath() -> ~/.sessionmem/config.json

CAUTION (RESEARCH Pitfall 5): resolvePolicySettings' per-key loop assumes FLAT scalars.
`team` is an object — resolve it as a unit, do not feed it through the scalar resolve<K>() loop.

config.ts command pattern (test seam): handlers accept `{ configPath?: string }`; resolvePath = options?.configPath ?? configFilePath().
uninstall.ts --purge pattern is the model for --remove-team-memories gating.
Error convention (Phase 5 D-03): console.error to stderr + process.exit(1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend policyConfig.ts with the team section</name>
  <files>src/core/config/policyConfig.ts</files>
  <read_first>
    - src/core/config/policyConfig.ts (full file — policyConfigShape, schemas, read/write, resolvePolicySettings)
    - src/cli/commands/config.ts (how config get/set consume the schema, CONFIG_KEYS map)
  </read_first>
  <behavior>
    - Test: writePolicyConfig with `{ team: { enabled: true, sharedPath: "/x" } }` then readPolicyConfig returns team.enabled true and team.sharedPath "/x".
    - Test: reading a config.json that lacks a `team` key returns team defaulting to `{ enabled: false }` (backward compat via .strip() + .default).
    - Test: an unknown key inside `team` is rejected on write (.strict()).
  </behavior>
  <action>
    Add a nested `team` object to the config schema (D-14, RESEARCH Pattern 6). Define `teamConfigShape = z.object({ enabled: z.boolean().default(false), sharedPath: z.string().optional() }).strict()` and add `team: teamConfigShape.default({ enabled: false })` to `policyConfigShape`. The existing `.strict()` write schema and `.strip()` read schema pick this up automatically. Extend `DEFAULT_POLICY_CONFIG` with `team: { enabled: false }`.

    Do NOT route `team` through the flat per-key `resolve<K>()` loop in `resolvePolicySettings` (RESEARCH Pitfall 5 — it returns scalars). Either (a) add a dedicated exported `resolveTeamConfig({ override?, config? })` that resolves the `team` object as a unit (object-level override > config > default), or (b) leave `resolvePolicySettings` untouched for scalars and have `team` consumers read it directly via `readPolicyConfig`. Pick one and keep the existing scalar resolution unchanged. Ensure `writePolicyConfig`'s strict partial-merge still accepts `{ team: {...} }`.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cli/config-command.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "team" src/core/config/policyConfig.ts` shows `teamConfigShape` with `enabled` and `sharedPath`, added to `policyConfigShape`.
    - `DEFAULT_POLICY_CONFIG` includes `team: { enabled: false }`.
    - `resolvePolicySettings`' scalar `resolve<K>()` loop still returns only `retentionDays` and `redactionEnabled` (team is resolved as a unit, not via the scalar loop).
    - `npx tsc --noEmit` clean; existing config-command spec still green.
  </acceptance_criteria>
  <done>Config schema carries a strict nested team section with safe defaults; team resolved as an object unit, not via the flat scalar loop; existing config tests unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: team command group (enable/disable/status) + registration</name>
  <files>src/cli/commands/team.ts, src/cli/index.ts, tests/integration/cli/team.spec.ts</files>
  <read_first>
    - src/cli/commands/config.ts (configPath test seam, write-then-confirm output pattern)
    - src/cli/commands/uninstall.ts (--purge gating model for --remove-team-memories)
    - src/cli/index.ts:40-126 (arrow-wrap NOTE + retention/config group registration template)
    - src/cli/context.ts (CliContext.username from Plan 01, createCliContext seam)
    - src/core/config/policyConfig.ts (readPolicyConfig/writePolicyConfig/configFilePath after Task 1)
    - tests/integration/cli/config-command.spec.ts (CLI integration test structure + temp configPath usage)
    - tests/helpers/cliTestContext.ts (test context helper)
  </read_first>
  <behavior>
    - Test: teamEnableCommand("/shared") writes team.enabled true + sharedPath "/shared" to the temp config and prints a confirmation line.
    - Test: teamStatusCommand prints enabled state and sharedPath read back from config; reports whether the path exists/is writable.
    - Test: teamDisableCommand (no flag) sets team.enabled false and leaves teammate-authored rows in the DB intact.
    - Test: teamDisableCommand({ removeTeamMemories: true }) deletes rows where author != local username for the current project; local-authored rows remain.
  </behavior>
  <action>
    Create `src/cli/commands/team.ts` exporting `teamEnableCommand(path, options?)`, `teamDisableCommand(options?)`, and `teamStatusCommand(options?)`. Use a `{ configPath?: string }` test seam resolved via `options?.configPath ?? configFilePath()`, mirroring `config.ts`. Accept an optional `CliContext` parameter (the `ctx ?? createCliContext()` seam) for the DB-touching disable path.

    `teamEnableCommand`: `writePolicyConfig(resolvePath(options), { team: { enabled: true, sharedPath } })`; print `Team mode enabled. Shared path: ${sharedPath}`. If `path` is missing/empty, error to stderr + `process.exit(1)` (Phase 5 D-03).

    `teamStatusCommand`: read config; print enabled/disabled and the sharedPath; report whether the shared path exists and is writable (use `fs.existsSync` + an access check; surface "not set" when no sharedPath). Do NOT print a last-sync time — there is no `synced_at` column (D-08, RESEARCH Open Q5).

    `teamDisableCommand`: default — `writePolicyConfig(..., { team: { enabled: false } })` and print `Team mode disabled. Teammate memories preserved.` (TEAM-03 no-data-loss default). With `--remove-team-memories` — additionally run a parameterized `DELETE FROM memories WHERE project_id = ? AND author != ?` binding `(context.projectId, context.username)` (never string-concatenate — T-06-11 precedent), then print how many rows were removed. Model the flag gating on `uninstall.ts --purge`.

    Register in `src/cli/index.ts` the `team` command group with the three subcommands, arrow-wrapping every handler to drop commander's trailing Command arg (NOTE at index.ts:42-50). `team disable` declares `--remove-team-memories`.

    Create `tests/integration/cli/team.spec.ts` covering the four behaviors using a temp config path and a temp-DB CliContext with seeded local + teammate-authored (`author != username`) rows.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cli/team.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `team.ts` exports `teamEnableCommand`, `teamDisableCommand`, `teamStatusCommand`.
    - `grep -n "team" src/cli/index.ts` shows a `program.command("team")` group with `enable <path>`, `disable` (with `--remove-team-memories`), and `status` subcommands, each arrow-wrapped.
    - The `--remove-team-memories` DELETE uses bound parameters (`grep -n "author != ?" src/cli/commands/team.ts` or equivalent parameterized form — no string interpolation of projectId/username into SQL).
    - `npx vitest run tests/integration/cli/team.spec.ts` passes all four behaviors, including default-disable preserving teammate rows and --remove-team-memories deleting only `author != local` rows.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>team enable/disable/status work against a temp config; disable defaults to no data loss; --remove-team-memories does a parameterized author-scoped delete; group registered with arrow-wrapped handlers; team.spec.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invoker → config.json | Operator writes team.sharedPath; same trust as the process. |
| CLI invoker → memories table | --remove-team-memories deletes rows; must be project- + author-scoped and parameterized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | --remove-team-memories DELETE | mitigate | Parameterized `DELETE ... WHERE project_id = ? AND author != ?` — projectId/username are bound, never string-concatenated (T-06-11 precedent). |
| T-07-09 | Tampering | team config write | mitigate | `.strict()` write schema rejects unknown keys inside `team` (T-06-04 precedent); read uses `.strip()` + safe default. |
| T-07-10 | Information Disclosure | team status path probe | accept | `team status` reads local fs only (existence/writability of an operator-supplied path); no remote surface. Low risk. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npm test` full suite green.
- Manual: `sessionmem team enable <tmp>` then `sessionmem team status` round-trips the path; `team disable` keeps rows; `team disable --remove-team-memories` removes only teammate-authored rows.
</verification>

<success_criteria>
- Nested `team` config section persists and reads back with strict-write/strip-read discipline (D-14).
- `team enable/disable/status` subcommands registered and functional (D-13).
- Default disable preserves pulled rows (TEAM-03); `--remove-team-memories` reverts to local-only (D-15).
- `tests/integration/cli/team.spec.ts` green.
</success_criteria>

<output>
Create `.planning/phases/07-team-mode-shared-memory/07-02-SUMMARY.md` when done
</output>
