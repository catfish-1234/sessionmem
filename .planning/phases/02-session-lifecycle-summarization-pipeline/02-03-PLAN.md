---
phase: 02-session-lifecycle-summarization-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/core/api/contracts.ts
  - src/core/api/sessionLifecycleService.ts
  - tests/unit/core/cloud-status-warning.spec.ts
  - tests/integration/core/cloud-optin-policy.spec.ts
  - docs/cloud-summarization.md
autonomous: true
requirements: [SECU-04, CAPT-04]
user_setup:
  - service: anthropic
    why: "Enable optional cloud summarization path with explicit user consent"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys"
must_haves:
  truths:
    - "Users can clearly see when cloud summarization is active."
    - "Cloud summarization never activates without explicit user opt-in."
    - "Documentation explains cloud enablement conditions, warning surfaces, and local fallback behavior."
  artifacts:
    - path: "src/core/api/sessionLifecycleService.ts"
      provides: "Cloud-status warning generation and warning payload wiring"
      contains: "cloud_summarization_enabled"
    - path: "tests/unit/core/cloud-status-warning.spec.ts"
      provides: "Unit coverage for warning payload creation"
      contains: "cloud_summarization_enabled"
    - path: "tests/integration/core/cloud-optin-policy.spec.ts"
      provides: "Integration coverage for explicit opt-in requirements"
      contains: "allowCloudSummarization"
    - path: "docs/cloud-summarization.md"
      provides: "User-visible policy and security documentation"
      contains: "allowCloudSummarization=true"
  key_links:
    - from: "src/core/api/contracts.ts"
      to: "src/core/api/sessionLifecycleService.ts"
      via: "warningCodes + warningMessages fields"
      pattern: "warningCodes"
    - from: "docs/cloud-summarization.md"
      to: "src/core/api/sessionLifecycleService.ts"
      via: "documents exact runtime warning code and activation rules"
      pattern: "cloud_summarization_enabled"
---

<objective>
Finish SECU-04 by making cloud summarization activation observable, test-protected, and documented.

Purpose: satisfy security transparency requirements while preserving local-first defaults and CAPT-04 manual controls.
Output: warning payload wiring, cloud opt-in tests, and cloud summarization policy documentation.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-RESEARCH.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-VALIDATION.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-02-PLAN.md

<interfaces>
From src/core/api/contracts.ts (after 02-02):
```ts
status: "stored" | "skipped_threshold" | "skipped_disabled" | "failed";
usedMode: "local" | "cloud";
warningCodes: string[];
```

From src/core/api/localOnlyPolicy.ts:
```ts
export function assertLocalOnlyPolicy(config: LocalOnlyPolicyConfig): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cloud activation warning payloads to lifecycle responses</name>
  <files>src/core/api/contracts.ts, src/core/api/sessionLifecycleService.ts, tests/unit/core/cloud-status-warning.spec.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, src/core/api/contracts.ts, src/core/api/sessionLifecycleService.ts, src/core/api/localOnlyPolicy.ts</read_first>
  <behavior>
    - Test 1: When cloud path is selected, response includes warning code `cloud_summarization_enabled`.
    - Test 2: Warning payload includes explicit condition text for `allowCloudSummarization=true` and key presence.
    - Test 3: Local-only runs produce empty cloud warning payloads.
  </behavior>
  <action>Extend `handleSessionEnd` response schema in `contracts.ts` with `warningMessages: string[]` alongside `warningCodes`. In `sessionLifecycleService.ts`, emit warning code `cloud_summarization_enabled` and message `Cloud summarization active: allowCloudSummarization=true and ANTHROPIC_API_KEY present` whenever `usedMode='cloud'`. Keep warning arrays empty for local mode and for skipped statuses. Add `tests/unit/core/cloud-status-warning.spec.ts` covering cloud-enabled and local-only outcomes.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/unit/core/cloud-status-warning.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/api/sessionLifecycleService.ts` contains `cloud_summarization_enabled`
    - `src/core/api/sessionLifecycleService.ts` contains `allowCloudSummarization=true`
    - `src/core/api/contracts.ts` contains `warningMessages`
    - `tests/unit/core/cloud-status-warning.spec.ts` contains `Cloud summarization active`
  </acceptance_criteria>
  <done>Lifecycle responses surface cloud-mode status in machine- and human-readable forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify explicit cloud opt-in policy and fallback behavior end-to-end</name>
  <files>tests/integration/core/cloud-optin-policy.spec.ts, src/core/api/sessionLifecycleService.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-RESEARCH.md, tests/integration/core/cloud-optin-policy.spec.ts, src/core/api/sessionLifecycleService.ts, src/core/api/localOnlyPolicy.ts</read_first>
  <behavior>
    - Test 1: Provider key without opt-in keeps `usedMode='local'`.
    - Test 2: Opt-in plus key allows `usedMode='cloud'` and emits cloud warning.
    - Test 3: Cloud execution failure triggers local fallback with non-empty warning payload.
  </behavior>
  <action>Implement `tests/integration/core/cloud-optin-policy.spec.ts` using deterministic session fixtures and injected summarizer stubs to force cloud/local paths. Assert that `allowCloudSummarization=false` always resolves to local mode even when `ANTHROPIC_API_KEY` is present, and that `allowCloudSummarization=true` with key resolves to cloud mode. Assert fallback path returns `usedMode='local'` plus warning code indicating cloud failure fallback.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/core/cloud-optin-policy.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/core/cloud-optin-policy.spec.ts` contains `allowCloudSummarization=false`
    - `tests/integration/core/cloud-optin-policy.spec.ts` contains `usedMode`
    - `tests/integration/core/cloud-optin-policy.spec.ts` contains `cloud_summarization_enabled`
    - `tests/integration/core/cloud-optin-policy.spec.ts` contains `fallback`
  </acceptance_criteria>
  <done>Cloud opt-in enforcement and fallback semantics are validated at integration level.</done>
</task>

<task type="auto">
  <name>Task 3: Document cloud summarization security and visibility contract</name>
  <files>docs/cloud-summarization.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, docs/cloud-summarization.md</read_first>
  <action>Create `docs/cloud-summarization.md` with sections: `Default Mode`, `How to Enable Cloud Mode`, `Runtime Warning Signals`, `Failure Fallback Behavior`, and `Manual Summarization When Auto Is Disabled`. Include exact enablement condition `allowCloudSummarization=true` plus `ANTHROPIC_API_KEY` requirement, warning code `cloud_summarization_enabled`, and explicit note that manual `summarizeSessionToMemory` remains available when `autoSummarize=false`.</action>
  <verify>
    <automated>cmd /c rg -n \"allowCloudSummarization=true|ANTHROPIC_API_KEY|cloud_summarization_enabled|autoSummarize=false|summarizeSessionToMemory\" docs/cloud-summarization.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/cloud-summarization.md` contains `allowCloudSummarization=true`
    - `docs/cloud-summarization.md` contains `ANTHROPIC_API_KEY`
    - `docs/cloud-summarization.md` contains `cloud_summarization_enabled`
    - `docs/cloud-summarization.md` contains `autoSummarize=false`
    - `docs/cloud-summarization.md` contains `summarizeSessionToMemory`
  </acceptance_criteria>
  <done>SECU-04 documentation requirement is met with concrete activation and warning details.</done>
</task>

</tasks>

<verification>
- [ ] `cmd /c npx vitest run tests/unit/core/cloud-status-warning.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run tests/integration/core/cloud-optin-policy.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
- [ ] `cmd /c rg -n "allowCloudSummarization=true|cloud_summarization_enabled" docs/cloud-summarization.md`
</verification>

<success_criteria>
- Cloud summarization state is visible in lifecycle responses via warning code/message fields.
- Cloud mode activation is test-protected behind explicit opt-in + key requirement.
- User documentation clearly explains cloud enablement, warnings, fallback, and CAPT-04 manual path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-lifecycle-summarization-pipeline/02-03-SUMMARY.md`
</output>

