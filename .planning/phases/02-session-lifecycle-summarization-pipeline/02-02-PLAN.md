---
phase: 02-session-lifecycle-summarization-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/core/api/memoryCoreService.ts
  - src/core/api/sessionLifecycleService.ts
  - src/core/summarize/summaryShape.ts
  - src/core/summarize/redaction.ts
  - src/core/summarize/localSummarizer.ts
  - src/core/summarize/cloudSummarizer.ts
  - src/core/summarize/strategySelector.ts
  - tests/integration/core/session-lifecycle-summary.spec.ts
  - tests/integration/core/summarization-retry-failure.spec.ts
  - tests/integration/core/manual-summary-when-auto-off.spec.ts
autonomous: true
requirements: [CAPT-02, CAPT-04]
user_setup:
  - service: anthropic
    why: "Optional cloud summarization mode for session-end pipeline"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys"
must_haves:
  truths:
    - "A `session_end` trigger runs summarization immediately when auto mode is enabled and threshold is met."
    - "Cloud summarizer is only used when explicit cloud opt-in and provider key are both present."
    - "If cloud summarization fails, pipeline retries and then falls back to local summarizer automatically."
    - "Disabling auto summarization does not disable manual summarizeSessionToMemory."
  artifacts:
    - path: "src/core/api/sessionLifecycleService.ts"
      provides: "Session-end orchestration with retry/fallback and failure recording"
      exports: ["createSessionLifecycleService"]
    - path: "src/core/summarize/strategySelector.ts"
      provides: "Cloud/local mode resolution from policy config"
      exports: ["resolveSummarizerMode"]
    - path: "src/core/summarize/localSummarizer.ts"
      provides: "Default deterministic local summarizer path"
      exports: ["summarizeLocalSessionEvents"]
    - path: "tests/integration/core/manual-summary-when-auto-off.spec.ts"
      provides: "CAPT-04 regression protection"
      contains: "manual summarizeSessionToMemory still succeeds"
  key_links:
    - from: "src/core/api/memoryCoreService.ts"
      to: "src/core/api/sessionLifecycleService.ts"
      via: "new handleSessionEnd method delegation"
      pattern: "handleSessionEnd"
    - from: "src/core/api/sessionLifecycleService.ts"
      to: "src/core/storage/summarizationFailuresRepo.ts"
      via: "failure persistence after retries/fallback exhaustion"
      pattern: "insertSummarizationFailure"
    - from: "src/core/api/sessionLifecycleService.ts"
      to: "src/core/storage/memoryRepo.ts"
      via: "idempotent summary memory upsert"
      pattern: "upsertSessionSummaryMemory"
---

<objective>
Implement the session lifecycle orchestration pipeline that fulfills CAPT-02 and preserves CAPT-04 manual behavior.

Purpose: convert stored session events into bounded durable summary memories with deterministic local-first behavior and resilient cloud fallback.
Output: lifecycle service + summarizer strategy modules + integration tests passing for core lifecycle scenarios.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-RESEARCH.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-VALIDATION.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-01-PLAN.md
@.planning/phases/01-core-memory-engine-foundation/01-04-SUMMARY.md

<interfaces>
From src/core/api/contracts.ts (after 02-01):
```ts
handleSessionEnd: HandleSessionEndRequest;
```

From src/core/storage/sessionEventsRepo.ts:
```ts
export function listSessionEventsBySession(
  db: Database,
  projectId: string,
  sessionId: string,
): SessionEventRecord[];
```

From src/core/storage/memoryRepo.ts:
```ts
export function upsertSessionSummaryMemory(db: Database, input: InsertMemoryInput): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement summary shaping, token cap, and redaction behavior</name>
  <files>src/core/summarize/summaryShape.ts, src/core/summarize/redaction.ts, src/core/summarize/localSummarizer.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, src/core/embed/textNormalize.ts, src/core/summarize/localSummarizer.ts, tests/integration/core/session-lifecycle-summary.spec.ts</read_first>
  <behavior>
    - Test 1: Local summarizer returns sections `goals/actions/decisions/blockers/outcomes` in deterministic order.
    - Test 2: Final persisted summary payload is capped to `summaryTokenCap` default `300`.
    - Test 3: Redaction partial failure emits warning code and continues with partially redacted output.
  </behavior>
  <action>Create `buildStructuredSummary(events, options)` in `summaryShape.ts` that outputs fixed section headings and supports `factMode` values `summary-only`, `facts-only`, `summary+facts`. In `redaction.ts`, implement default `redactionEnabled=true` pass with rule execution wrapped per-rule so one failing rule adds warning code `redaction_partial_failure` and does not abort summarization. In `localSummarizer.ts`, assemble pipeline `events -> structured summary -> redaction -> token cap enforcement` using deterministic whitespace normalization and a bounded token estimator shared across local path calls.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/summarize/summaryShape.ts` contains `goals`
    - `src/core/summarize/summaryShape.ts` contains `actions`
    - `src/core/summarize/summaryShape.ts` contains `decisions`
    - `src/core/summarize/summaryShape.ts` contains `blockers`
    - `src/core/summarize/summaryShape.ts` contains `outcomes`
    - `src/core/summarize/redaction.ts` contains `redaction_partial_failure`
    - `src/core/summarize/localSummarizer.ts` contains `summaryTokenCap`
  </acceptance_criteria>
  <done>Local summarization path enforces required format, redaction policy, and token budget defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session-end orchestrator with mode selection, retry, and fallback</name>
  <files>src/core/summarize/strategySelector.ts, src/core/summarize/cloudSummarizer.ts, src/core/api/sessionLifecycleService.ts, src/core/api/memoryCoreService.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, .planning/phases/02-session-lifecycle-summarization-pipeline/02-RESEARCH.md, src/core/api/memoryCoreService.ts, src/core/api/localOnlyPolicy.ts, src/core/storage/summarizationFailuresRepo.ts</read_first>
  <behavior>
    - Test 1: `session_end` with enough events and `autoSummarize=true` stores summary memory via upsert.
    - Test 2: Cloud mode requires `allowCloudSummarization=true` plus key; key alone never enables cloud mode.
    - Test 3: Cloud failure retries exactly 2 times, then falls back to local; if local also fails, failure record is inserted.
  </behavior>
  <action>Create `resolveSummarizerMode(config)` in `strategySelector.ts` returning `cloud` only when `allowCloudSummarization===true` and `anthropicApiKey` exists, otherwise `local`. Implement `summarizeWithCloud(...)` in `cloudSummarizer.ts` using model `claude-sonnet-4-20250514`, and add a retry wrapper with `retries: 2`, base delay `250ms`, jitter `0..100ms`. Implement `createSessionLifecycleService(...)` in `sessionLifecycleService.ts` to load ordered events, apply threshold gate (`minimumEventThreshold` default 3, bounds 1..100), run mode-selected summarizer, fallback cloud->local on cloud errors, upsert summary memory idempotently, and persist failure details through `insertSummarizationFailure` when all attempts fail. Update `createMemoryCoreService` to expose `handleSessionEnd` method while preserving existing manual `summarizeSessionToMemory` behavior when `autoSummarize=false`.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts tests/integration/core/summarization-retry-failure.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/summarize/strategySelector.ts` contains `allowCloudSummarization`
    - `src/core/summarize/strategySelector.ts` contains `anthropicApiKey`
    - `src/core/api/sessionLifecycleService.ts` contains `minimumEventThreshold`
    - `src/core/api/sessionLifecycleService.ts` contains `retries: 2`
    - `src/core/api/sessionLifecycleService.ts` contains `insertSummarizationFailure`
    - `src/core/api/memoryCoreService.ts` contains `handleSessionEnd`
  </acceptance_criteria>
  <done>Session-end orchestration is resilient, local-first by default, and policy-gated for cloud mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete CAPT-02 and CAPT-04 integration assertions</name>
  <files>tests/integration/core/session-lifecycle-summary.spec.ts, tests/integration/core/summarization-retry-failure.spec.ts, tests/integration/core/manual-summary-when-auto-off.spec.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-VALIDATION.md, tests/integration/core/session-lifecycle-summary.spec.ts, tests/integration/core/summarization-retry-failure.spec.ts, tests/integration/core/manual-summary-when-auto-off.spec.ts, src/core/api/memoryCoreService.ts</read_first>
  <behavior>
    - Test 1: Session-end with event count below threshold returns `status=skipped_threshold` and no summary insert.
    - Test 2: Auto summarize disabled returns `status=skipped_disabled`, but manual summarize API still returns `ok=true`.
    - Test 3: Exhausted summarization path stores one failure record including `attempt_count`.
  </behavior>
  <action>Replace any remaining `it.todo` placeholders in the three integration specs with executable assertions that verify lifecycle statuses (`stored`, `skipped_threshold`, `skipped_disabled`, `failed`), idempotent summary upsert behavior for repeated `handleSessionEnd` calls on same `projectId+sessionId`, and manual summarize success path under `autoSummarize=false` config. Keep fixtures deterministic by inserting explicit event indexes and timestamps.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts tests/integration/core/summarization-retry-failure.spec.ts tests/integration/core/manual-summary-when-auto-off.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/core/session-lifecycle-summary.spec.ts` contains `skipped_threshold`
    - `tests/integration/core/manual-summary-when-auto-off.spec.ts` contains `skipped_disabled`
    - `tests/integration/core/manual-summary-when-auto-off.spec.ts` contains `summarizeSessionToMemory`
    - `tests/integration/core/summarization-retry-failure.spec.ts` contains `attempt_count`
  </acceptance_criteria>
  <done>CAPT-02 and CAPT-04 behaviors are enforced by integration coverage.</done>
</task>

</tasks>

<verification>
- [ ] `cmd /c npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run tests/integration/core/summarization-retry-failure.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run tests/integration/core/manual-summary-when-auto-off.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
</verification>

<success_criteria>
- `session_end` executes immediate summarize pipeline with threshold controls and durable upsert output.
- Cloud path is explicit opt-in and safely falls back to local path on failure.
- Manual summarize path remains functional even when auto summarization is disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-lifecycle-summarization-pipeline/02-02-SUMMARY.md`
</output>

