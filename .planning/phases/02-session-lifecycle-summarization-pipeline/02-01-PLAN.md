---
phase: 02-session-lifecycle-summarization-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/api/contracts.ts
  - src/core/schema/migrations/003_summarization_failures.sql
  - src/core/storage/summarizationFailuresRepo.ts
  - src/core/storage/types.ts
  - tests/integration/core/session-lifecycle-summary.spec.ts
  - tests/integration/core/summarization-retry-failure.spec.ts
  - tests/integration/core/manual-summary-when-auto-off.spec.ts
autonomous: true
requirements: [CAPT-02]
user_setup: []
must_haves:
  truths:
    - "Session-end handling has explicit typed contract and config controls before orchestration implementation starts."
    - "Summarization failure outcomes can be stored durably for manual retry workflows."
    - "Phase 2 integration tests exist before lifecycle implementation work begins."
  artifacts:
    - path: "src/core/api/contracts.ts"
      provides: "Session lifecycle request/response schemas and config enums"
      contains: "handleSessionEndRequestSchema"
    - path: "src/core/schema/migrations/003_summarization_failures.sql"
      provides: "Failure-record table for exhausted retries"
      contains: "CREATE TABLE IF NOT EXISTS summarization_failures"
    - path: "src/core/storage/summarizationFailuresRepo.ts"
      provides: "Insert/list APIs for summarization failures"
      exports: ["insertSummarizationFailure", "listSummarizationFailures"]
    - path: "tests/integration/core/session-lifecycle-summary.spec.ts"
      provides: "CAPT-02 session_end test scaffold"
      contains: "describe(\"session lifecycle summary pipeline\""
  key_links:
    - from: "src/core/api/contracts.ts"
      to: "src/core/api/sessionLifecycleService.ts"
      via: "typed request/response map for session-end orchestration"
      pattern: "handleSessionEnd"
    - from: "src/core/storage/summarizationFailuresRepo.ts"
      to: "src/core/schema/migrations/003_summarization_failures.sql"
      via: "repo reads/writes failure table introduced by migration"
      pattern: "summarization_failures"
---

<objective>
Define the Phase 2 interfaces, persistence primitives, and test scaffolds that all lifecycle orchestration work will implement against.

Purpose: reduce implementation ambiguity and lock CAPT-02 contracts before behavior code lands.
Output: new lifecycle schemas, failure-record migration/repository, and Wave 0 integration test files.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-RESEARCH.md
@.planning/phases/02-session-lifecycle-summarization-pipeline/02-VALIDATION.md
@.planning/phases/01-core-memory-engine-foundation/01-01-SUMMARY.md
@.planning/phases/01-core-memory-engine-foundation/01-04-SUMMARY.md

<interfaces>
From src/core/api/contracts.ts:
```ts
export interface MemoryCoreRequestMap {
  ingestSessionEvents: IngestSessionEventsRequest;
  summarizeSessionToMemory: SummarizeSessionToMemoryRequest;
  storeMemory: StoreMemoryRequest;
  retrieveMemories: RetrieveMemoriesRequest;
}
```

From src/core/storage/sessionEventsRepo.ts:
```ts
export function listSessionEventsBySession(
  db: Database,
  projectId: string,
  sessionId: string,
): SessionEventRecord[];
```

From src/core/storage/memoryRepo.ts:
```ts
export function upsertSessionSummaryMemory(
  db: Database,
  input: InsertMemoryInput,
): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 integration test files for CAPT-02 behaviors</name>
  <files>tests/integration/core/session-lifecycle-summary.spec.ts, tests/integration/core/summarization-retry-failure.spec.ts, tests/integration/core/manual-summary-when-auto-off.spec.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, .planning/phases/02-session-lifecycle-summarization-pipeline/02-VALIDATION.md, tests/integration/core/memory-core-service.spec.ts, tests/integration/core/local-only-policy.spec.ts</read_first>
  <action>Create the three missing integration spec files listed in `02-VALIDATION.md` with concrete test names and fixture setup using `openDb()` and `createMemoryCoreService()`. Define these initial cases: `session_end triggers summarize when event count >= threshold`, `cloud failure retries 2 times then records failure`, and `autoSummarize=false skips auto pipeline while manual summarizeSessionToMemory still succeeds`. Mark tests as `it.todo(...)` only when implementation dependency is not available yet; otherwise add executable assertions immediately.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts tests/integration/core/summarization-retry-failure.spec.ts tests/integration/core/manual-summary-when-auto-off.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/core/session-lifecycle-summary.spec.ts` contains `session_end triggers summarize`
    - `tests/integration/core/summarization-retry-failure.spec.ts` contains `retries 2 times`
    - `tests/integration/core/manual-summary-when-auto-off.spec.ts` contains `autoSummarize=false`
  </acceptance_criteria>
  <done>Wave 0 CAPT-02 test surfaces exist and run in vitest.</done>
</task>

<task type="auto">
  <name>Task 2: Add durable summarization failure storage migration and repository</name>
  <files>src/core/schema/migrations/003_summarization_failures.sql, src/core/storage/summarizationFailuresRepo.ts, src/core/storage/types.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, src/core/schema/migrations/001_initial.sql, src/core/schema/migrations/002_indexes.sql, src/core/storage/sessionEventsRepo.ts, src/core/storage/types.ts</read_first>
  <action>Create migration `003_summarization_failures.sql` with columns: `id TEXT PRIMARY KEY`, `project_id TEXT NOT NULL`, `session_id TEXT NOT NULL`, `source_adapter TEXT NOT NULL`, `reason TEXT NOT NULL`, `attempt_count INTEGER NOT NULL`, `last_error_json TEXT NOT NULL`, `created_at TEXT NOT NULL DEFAULT strftime(...)`, `updated_at TEXT NOT NULL DEFAULT strftime(...)`. Add index `idx_sum_fail_project_session` on `(project_id, session_id, updated_at DESC)`. Add `SummarizationFailureRecord` and `InsertSummarizationFailureInput` types in `src/core/storage/types.ts`, then implement `insertSummarizationFailure` and `listSummarizationFailures` repository functions in `src/core/storage/summarizationFailuresRepo.ts` using deterministic ordering by `updated_at DESC`.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/storage/schema.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/schema/migrations/003_summarization_failures.sql` contains `CREATE TABLE IF NOT EXISTS summarization_failures`
    - `src/core/schema/migrations/003_summarization_failures.sql` contains `attempt_count INTEGER NOT NULL`
    - `src/core/storage/types.ts` contains `SummarizationFailureRecord`
    - `src/core/storage/summarizationFailuresRepo.ts` contains `insertSummarizationFailure`
  </acceptance_criteria>
  <done>Failure records can be persisted and queried for later manual retry flows.</done>
</task>

<task type="auto">
  <name>Task 3: Extend contracts for explicit session-end lifecycle orchestration</name>
  <files>src/core/api/contracts.ts</files>
  <read_first>.planning/phases/02-session-lifecycle-summarization-pipeline/02-CONTEXT.md, .planning/phases/02-session-lifecycle-summarization-pipeline/02-RESEARCH.md, src/core/api/contracts.ts</read_first>
  <action>Add lifecycle schemas and map entries for a new method `handleSessionEnd`. Define `factMode` enum exactly as `summary-only | facts-only | summary+facts`. Define config defaults and bounds in schema: `autoSummarize` default `true`, `minimumEventThreshold` default `3`, min `1`, max `100`, `summaryTokenCap` default `300`, and `redactionEnabled` default `true`. Add cloud policy fields: `allowCloudSummarization` default `false`, provider key field `anthropicApiKey` optional string. Define response schema with `ok`, `status` (`stored | skipped_threshold | skipped_disabled | failed`), `usedMode` (`local | cloud`), `warningCodes` string array, and optional `failureRecordId`.</action>
  <verify>
    <automated>cmd /c npx vitest run tests/integration/core/memory-core-service.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/api/contracts.ts` contains `handleSessionEndRequestSchema`
    - `src/core/api/contracts.ts` contains `summary-only`
    - `src/core/api/contracts.ts` contains `facts-only`
    - `src/core/api/contracts.ts` contains `summary+facts`
    - `src/core/api/contracts.ts` contains `minimumEventThreshold`
    - `src/core/api/contracts.ts` contains `failureRecordId`
  </acceptance_criteria>
  <done>Session-end pipeline contract is fully specified with locked defaults and bounded controls.</done>
</task>

</tasks>

<verification>
- [ ] `cmd /c npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts tests/integration/core/summarization-retry-failure.spec.ts tests/integration/core/manual-summary-when-auto-off.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run tests/integration/storage/schema.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
</verification>

<success_criteria>
- Session lifecycle request/response contracts include threshold, token cap, fact mode, and cloud opt-in fields.
- Summarization failure records are durable via migration + repository.
- Wave 0 CAPT-02 test files exist and execute in CI command path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-lifecycle-summarization-pipeline/02-01-SUMMARY.md`
</output>

