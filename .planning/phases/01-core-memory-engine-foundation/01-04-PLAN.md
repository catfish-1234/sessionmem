---
phase: 01-core-memory-engine-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-03"]
files_modified:
  - src/core/api/contracts.ts
  - src/core/api/errors.ts
  - src/core/api/memoryCoreService.ts
  - src/core/api/localOnlyPolicy.ts
  - src/adapters/contract/hostAdapterContract.ts
  - tests/integration/core/local-only-policy.spec.ts
  - tests/integration/core/memory-core-service.spec.ts
autonomous: true
requirements: [CAPT-01, RETR-01, SECU-03]
user_setup: []
must_haves:
  truths:
    - "Adapters can call one host-agnostic typed core API."
    - "Core API exposes lifecycle operations needed by phase and downstream phases."
    - "Local-only policy blocks external providers unless explicit opt-in is enabled."
  artifacts:
    - path: "src/core/api/contracts.ts"
      provides: "Strict typed request/response contracts"
      exports: ["MemoryCoreRequest", "MemoryCoreResponse"]
    - path: "src/core/api/errors.ts"
      provides: "Domain errors with stable codes"
      contains: "\"VALIDATION\" | \"NOT_FOUND\" | \"CONFLICT\" | \"INTERNAL\""
    - path: "src/core/api/memoryCoreService.ts"
      provides: "Host-agnostic lifecycle API facade"
      exports: ["createMemoryCoreService"]
    - path: "src/core/api/localOnlyPolicy.ts"
      provides: "SECU-03 gate enforcement"
      exports: ["assertLocalOnlyPolicy"]
  key_links:
    - from: "src/core/api/memoryCoreService.ts"
      to: "src/core/retrieve/retrieveMemories.ts"
      via: "retrieve method delegates to retrieval service"
      pattern: "retrieveMemories\\("
    - from: "src/core/api/memoryCoreService.ts"
      to: "src/core/api/localOnlyPolicy.ts"
      via: "policy guard checked during initialization"
      pattern: "assertLocalOnlyPolicy\\("
---

<objective>
Build strict host-agnostic core API and adapter contract with local-only policy guarantees.

Purpose: phase success requires adapters to call stable core interfaces and preserve local-only defaults.
Output: API contracts, domain errors, service facade, local-only guard, integration tests.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md
@.planning/phases/01-core-memory-engine-foundation/01-RESEARCH.md
@.planning/phases/01-core-memory-engine-foundation/01-01-SUMMARY.md
@.planning/phases/01-core-memory-engine-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define strict core contracts and domain error model</name>
  <files>src/core/api/contracts.ts, src/core/api/errors.ts, src/adapters/contract/hostAdapterContract.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/api/contracts.ts, src/core/api/errors.ts, src/adapters/contract/hostAdapterContract.ts</read_first>
  <action>Define typed contracts for lifecycle operations: `ingestSessionEvents`, `summarizeSessionToMemory`, `storeMemory`, `retrieveMemories`, `listMemories`, `getMemory`, `forgetMemory`, `exportMemories`, `importMemories`, `stats`. Define request/response schemas using `zod` with explicit required fields. Implement domain error class with allowed codes `VALIDATION`, `NOT_FOUND`, `CONFLICT`, `INTERNAL`. Add adapter-side shared contract type mapping to ensure all adapters use same payload shapes and error envelopes.</action>
  <verify>cmd /c npx vitest run tests/integration/core/memory-core-service.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/api/contracts.ts` contains `ingestSessionEvents`
    - `src/core/api/contracts.ts` contains `retrieveMemories`
    - `src/core/api/contracts.ts` contains `exportMemories`
    - `src/core/api/errors.ts` contains `VALIDATION`
    - `src/core/api/errors.ts` contains `NOT_FOUND`
    - `src/core/api/errors.ts` contains `CONFLICT`
    - `src/core/api/errors.ts` contains `INTERNAL`
  </acceptance_criteria>
  <done>Core contract surface and domain errors standardized for all adapter consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement memory core service and local-only policy guard</name>
  <files>src/core/api/memoryCoreService.ts, src/core/api/localOnlyPolicy.ts, tests/integration/core/local-only-policy.spec.ts, tests/integration/core/memory-core-service.spec.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/api/memoryCoreService.ts, src/core/api/localOnlyPolicy.ts, tests/integration/core/local-only-policy.spec.ts</read_first>
  <action>Implement `assertLocalOnlyPolicy(config)` that throws `DomainError("VALIDATION", ...)` when local-only mode is true and any external/cloud provider config is enabled without explicit opt-in flag. Implement `createMemoryCoreService(deps)` facade that validates requests, calls repositories/retrieval pipeline, and returns typed responses with consistent error envelope. Add integration tests verifying service operations return typed shapes and local-only guard blocks non-compliant config while allowing explicit opt-in mode.</action>
  <verify>cmd /c npx vitest run tests/integration/core/local-only-policy.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/api/localOnlyPolicy.ts` contains `assertLocalOnlyPolicy`
    - `src/core/api/localOnlyPolicy.ts` contains `DomainError(\"VALIDATION\"`
    - `src/core/api/memoryCoreService.ts` contains `createMemoryCoreService`
    - `tests/integration/core/local-only-policy.spec.ts` contains `explicit opt-in`
    - `tests/integration/core/memory-core-service.spec.ts` contains `ingestSessionEvents`
  </acceptance_criteria>
  <done>Host-agnostic service and SECU-03 local-only policy behavior implemented and tested.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmd /c npx vitest run tests/integration/core/memory-core-service.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run tests/integration/core/local-only-policy.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
</verification>

<success_criteria>

- All tasks completed
- Core API is strict, host-agnostic, and callable via adapter contracts
- Local-only default guard enforces SECU-03 behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-memory-engine-foundation/01-04-SUMMARY.md`
</output>

