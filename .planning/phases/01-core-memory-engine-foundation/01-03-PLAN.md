---
phase: 01-core-memory-engine-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/core/retrieve/recencyBands.ts
  - src/core/retrieve/importance.ts
  - src/core/retrieve/score.ts
  - src/core/retrieve/retrieveMemories.ts
  - src/core/storage/memorySearchRepo.ts
  - tests/unit/retrieve/scoring-weights.spec.ts
  - tests/integration/retrieve/retrieve-ranked.spec.ts
autonomous: true
requirements: [RETR-01, RETR-02]
user_setup: []
must_haves:
  truths:
    - "Core retrieves semantically relevant memories for a query."
    - "Ranking combines semantic, recency, and importance using locked weights."
    - "Output ordering is deterministic with fixed tie-break rules."
  artifacts:
    - path: "src/core/retrieve/score.ts"
      provides: "Weighted scoring function with configured weights"
      exports: ["scoreMemoryCandidate"]
    - path: "src/core/retrieve/retrieveMemories.ts"
      provides: "Top-k retrieval with deterministic ordering"
      exports: ["retrieveMemories"]
    - path: "tests/integration/retrieve/retrieve-ranked.spec.ts"
      provides: "End-to-end ranking assertions"
      contains: "score desc"
  key_links:
    - from: "src/core/retrieve/retrieveMemories.ts"
      to: "src/core/retrieve/score.ts"
      via: "candidate reranking"
      pattern: "scoreMemoryCandidate\\("
    - from: "src/core/retrieve/retrieveMemories.ts"
      to: "src/core/storage/memorySearchRepo.ts"
      via: "candidate fetch pipeline"
      pattern: "searchMemoryCandidates\\("
---

<objective>
Deliver retrieval pipeline with weighted ranking and deterministic sort semantics.

Purpose: RETR-01 and RETR-02 define phase value: relevant memory lookup with semantic+recency+importance scoring.
Output: retrieval modules, ranking utilities, search repository integration, tests for deterministic behavior.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md
@.planning/phases/01-core-memory-engine-foundation/01-RESEARCH.md
@.planning/phases/01-core-memory-engine-foundation/01-01-SUMMARY.md
@.planning/phases/01-core-memory-engine-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scoring primitives with bucketed recency</name>
  <files>src/core/retrieve/recencyBands.ts, src/core/retrieve/importance.ts, src/core/retrieve/score.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/retrieve/score.ts, src/core/retrieve/recencyBands.ts</read_first>
  <action>Implement `getRecencyBandScore(updatedAt, now)` with buckets: `today <=1 day => 1.0`, `week <=7 days => 0.75`, `month <=30 days => 0.5`, `older >30 days => 0.25`. Implement `normalizeImportance(score1to10)` mapping 1..10 to 0..1. Implement `scoreMemoryCandidate(candidate)` as `semantic*0.60 + recency*0.25 + importance*0.15` and return structured score parts for debugging tests.</action>
  <verify>cmd /c npx vitest run tests/unit/retrieve/scoring-weights.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/retrieve/score.ts` contains `0.60`
    - `src/core/retrieve/score.ts` contains `0.25`
    - `src/core/retrieve/score.ts` contains `0.15`
    - `src/core/retrieve/recencyBands.ts` contains `<= 1`
    - `src/core/retrieve/recencyBands.ts` contains `<= 7`
    - `src/core/retrieve/recencyBands.ts` contains `<= 30`
  </acceptance_criteria>
  <done>Scoring behavior and recency bucket model match locked context decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Build retrieval orchestrator with deterministic tie-break</name>
  <files>src/core/storage/memorySearchRepo.ts, src/core/retrieve/retrieveMemories.ts, tests/integration/retrieve/retrieve-ranked.spec.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/retrieve/retrieveMemories.ts, src/core/storage/memorySearchRepo.ts, tests/integration/retrieve/retrieve-ranked.spec.ts</read_first>
  <action>Implement `searchMemoryCandidates` to fetch candidate rows with embeddings and metadata for given project. Implement `retrieveMemories({projectId, queryText, topK, now})` that computes deterministic query embedding (plan 02 utility), semantic similarity, weighted score, then sorts by `score DESC`, then `updated_at DESC`, then `id ASC`, returning topK with score metadata. Add integration test fixtures proving ordering stays stable for ties and that top results reflect combined score, not semantic-only sort.</action>
  <verify>cmd /c npx vitest run tests/integration/retrieve/retrieve-ranked.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/retrieve/retrieveMemories.ts` contains `updated_at`
    - `src/core/retrieve/retrieveMemories.ts` contains `id`
    - `src/core/retrieve/retrieveMemories.ts` contains `topK`
    - `tests/integration/retrieve/retrieve-ranked.spec.ts` contains `deterministic`
    - `tests/integration/retrieve/retrieve-ranked.spec.ts` contains `semantic`
    - `tests/integration/retrieve/retrieve-ranked.spec.ts` contains `importance`
  </acceptance_criteria>
  <done>Retrieval service returns deterministic ranked results satisfying RETR-01 and RETR-02.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmd /c npx vitest run tests/unit/retrieve/scoring-weights.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run tests/integration/retrieve/retrieve-ranked.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
</verification>

<success_criteria>

- All tasks completed
- Retrieval combines semantic + recency + importance with locked weights
- Deterministic tie-break guarantees stable output order
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-memory-engine-foundation/01-03-SUMMARY.md`
</output>

