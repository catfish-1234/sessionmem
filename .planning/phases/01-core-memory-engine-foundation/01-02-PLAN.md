---
phase: 01-core-memory-engine-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/embed/textNormalize.ts
  - src/core/embed/deterministicEmbed.ts
  - src/core/embed/embeddingVersion.ts
  - src/core/embed/reembedPolicy.ts
  - tests/unit/embed/deterministic-embed.spec.ts
autonomous: true
requirements: [CAPT-03, SECU-03]
user_setup: []
must_haves:
  truths:
    - "Same normalized input always produces same vector output."
    - "Embedding metadata stores version and dimension for each generated vector."
    - "Re-embedding only occurs when text or embedding version changes."
  artifacts:
    - path: "src/core/embed/textNormalize.ts"
      provides: "Stable normalization pipeline"
      exports: ["normalizeEmbeddingText"]
    - path: "src/core/embed/deterministicEmbed.ts"
      provides: "Deterministic local embedding generator"
      exports: ["deterministicEmbed"]
    - path: "src/core/embed/reembedPolicy.ts"
      provides: "Re-embedding decision helper"
      exports: ["shouldReembed"]
  key_links:
    - from: "src/core/embed/deterministicEmbed.ts"
      to: "src/core/embed/textNormalize.ts"
      via: "embedding pipeline uses normalized text"
      pattern: "normalizeEmbeddingText\\("
    - from: "src/core/embed/reembedPolicy.ts"
      to: "src/core/embed/embeddingVersion.ts"
      via: "version mismatch triggers re-embed"
      pattern: "embeddingVersion"
---

<objective>
Implement deterministic local embedding primitives aligned with context decisions.

Purpose: CAPT-03 needs deterministic local embedding behavior and metadata now, before retrieval ranking.
Output: text normalization utility, deterministic embedding generator, re-embed decision helper, unit tests.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md
@.planning/phases/01-core-memory-engine-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build normalization and deterministic embedding function</name>
  <files>src/core/embed/textNormalize.ts, src/core/embed/deterministicEmbed.ts, src/core/embed/embeddingVersion.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/embed/textNormalize.ts, src/core/embed/deterministicEmbed.ts</read_first>
  <action>Implement `normalizeEmbeddingText(text: string): string` with exact sequence: trim, collapse internal whitespace to single spaces, Unicode NFKC normalization, lowercase conversion. Implement `deterministicEmbed(input, dimension)` that hashes normalized text with stable hash (sha256) and deterministically maps bytes to a fixed-length numeric vector with values in [-1, 1]. Export `EMBEDDING_VERSION = "v1-hash-local"` and include `dimension` and `embeddingVersion` metadata in result payload.</action>
  <verify>cmd /c npx vitest run tests/unit/embed/deterministic-embed.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/embed/textNormalize.ts` contains `normalize(\"NFKC\")`
    - `src/core/embed/textNormalize.ts` contains `toLowerCase()`
    - `src/core/embed/deterministicEmbed.ts` contains `createHash(\"sha256\")`
    - `src/core/embed/deterministicEmbed.ts` contains `dimension`
    - `src/core/embed/embeddingVersion.ts` contains `v1-hash-local`
  </acceptance_criteria>
  <done>Deterministic embedding and normalization behavior implemented exactly once in shared core utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce re-embedding policy and deterministic tests</name>
  <files>src/core/embed/reembedPolicy.ts, tests/unit/embed/deterministic-embed.spec.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/embed/reembedPolicy.ts, tests/unit/embed/deterministic-embed.spec.ts</read_first>
  <action>Implement `shouldReembed(previous, nextNormalizedText, currentVersion)` that returns true only when normalized text differs or previous version differs. Add tests asserting: same text + same version => false, text change => true, version change => true, same input returns identical vectors across repeated calls, and vector length equals configured dimension.</action>
  <verify>cmd /c npx vitest run tests/unit/embed/deterministic-embed.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/embed/reembedPolicy.ts` contains `shouldReembed`
    - `tests/unit/embed/deterministic-embed.spec.ts` contains `same input`
    - `tests/unit/embed/deterministic-embed.spec.ts` contains `toHaveLength`
    - `tests/unit/embed/deterministic-embed.spec.ts` contains `version change`
  </acceptance_criteria>
  <done>Re-embed decision logic deterministic and covered by unit tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmd /c npx vitest run tests/unit/embed/deterministic-embed.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
- [ ] No external API call paths introduced in embedding module
</verification>

<success_criteria>

- All tasks completed
- Embedding generation deterministic, local-only, and versioned
- Re-embedding only triggered by allowed change conditions
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-memory-engine-foundation/01-02-SUMMARY.md`
</output>

