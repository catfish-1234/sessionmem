---
phase: 01-core-memory-engine-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/core/schema/migrations/001_initial.sql
  - src/core/schema/migrations/002_indexes.sql
  - src/core/schema/runMigrations.ts
  - src/core/storage/types.ts
  - src/core/storage/sessionEventsRepo.ts
  - src/core/storage/memoryRepo.ts
  - src/core/storage/db.ts
  - tests/integration/storage/schema.spec.ts
autonomous: true
requirements: [CAPT-01, CAPT-03, SECU-03]
user_setup: []
must_haves:
  truths:
    - "Core can persist session events with project and session IDs."
    - "Core can persist memory rows with provenance and importance fields."
    - "Core schema and indexes can be created from migrations."
  artifacts:
    - path: "src/core/schema/migrations/001_initial.sql"
      provides: "Base tables for session_events and memories"
      contains: "CREATE TABLE IF NOT EXISTS session_events"
    - path: "src/core/schema/migrations/002_indexes.sql"
      provides: "Indexes for retrieval and provenance lookups"
      contains: "CREATE INDEX IF NOT EXISTS idx_memories_project_updated"
    - path: "src/core/storage/sessionEventsRepo.ts"
      provides: "Session event insert/list repository"
      exports: ["insertSessionEvent", "listSessionEventsBySession"]
    - path: "src/core/storage/memoryRepo.ts"
      provides: "Memory insert/upsert/list repository"
      exports: ["insertMemory", "upsertSessionSummaryMemory", "listMemoriesByProject"]
  key_links:
    - from: "src/core/storage/db.ts"
      to: "src/core/schema/runMigrations.ts"
      via: "db initialization calls migration runner"
      pattern: "runMigrations\\("
    - from: "src/core/storage/sessionEventsRepo.ts"
      to: "src/core/storage/types.ts"
      via: "typed repository payload contracts"
      pattern: "SessionEventRecord"
---

<objective>
Establish local storage schema and repositories for phase-1 capture and durability.

Purpose: CAPT-01/CAPT-03 need durable, queryable event+memory data model before retrieval/API work.
Output: migration SQL, migration runner, typed storage repos, schema tests.
</objective>

<execution_context>
@C:/Users/kavis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/kavis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md
@.planning/phases/01-core-memory-engine-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase-1 schema and migration runner</name>
  <files>src/core/schema/migrations/001_initial.sql, src/core/schema/migrations/002_indexes.sql, src/core/schema/runMigrations.ts, src/core/storage/db.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/schema/migrations/001_initial.sql, src/core/schema/migrations/002_indexes.sql, src/core/storage/db.ts</read_first>
  <action>Create `session_events` table with columns `id`, `project_id`, `session_id`, `event_index`, `event_type`, `payload_json`, `created_at`. Create `memories` table with columns `id`, `project_id`, `session_id`, `source_adapter`, `kind`, `content`, `normalized_content`, `importance`, `embedding`, `embedding_dim`, `embedding_version`, `created_at`, `updated_at`. Add constraints: `importance` integer check between 1 and 10, non-null provenance fields. Add indexes: `(project_id, updated_at DESC)`, `(project_id, session_id)`, `(project_id, importance DESC)`, `(project_id, created_at DESC)` and `session_events(project_id, session_id, event_index)`. Implement `runMigrations(db)` that applies migration files in order and writes applied migration records in a `_migrations` table.</action>
  <verify>cmd /c npx vitest run tests/integration/storage/schema.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/schema/migrations/001_initial.sql` contains `CHECK (importance >= 1 AND importance <= 10)`
    - `src/core/schema/migrations/001_initial.sql` contains `CREATE TABLE IF NOT EXISTS session_events`
    - `src/core/schema/migrations/001_initial.sql` contains `CREATE TABLE IF NOT EXISTS memories`
    - `src/core/schema/migrations/002_indexes.sql` contains `idx_memories_project_updated`
    - `src/core/schema/runMigrations.ts` contains `runMigrations`
  </acceptance_criteria>
  <done>Schema + migration runner implemented and verified by schema test.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed repositories for events and memories</name>
  <files>src/core/storage/types.ts, src/core/storage/sessionEventsRepo.ts, src/core/storage/memoryRepo.ts</files>
  <read_first>.planning/phases/01-core-memory-engine-foundation/01-CONTEXT.md, src/core/storage/types.ts, src/core/storage/sessionEventsRepo.ts, src/core/storage/memoryRepo.ts</read_first>
  <action>Define explicit TypeScript interfaces `SessionEventRecord`, `MemoryRecord`, `InsertSessionEventInput`, `InsertMemoryInput`. Implement `insertSessionEvent` and `listSessionEventsBySession` using prepared statements. Implement `insertMemory`, `upsertSessionSummaryMemory` (keyed by `project_id+session_id+kind='summary'`), and `listMemoriesByProject`. Ensure all repository writes require `project_id`, `session_id`, `source_adapter`, and enforce `importance` range before DB write.</action>
  <verify>cmd /c npx vitest run tests/integration/storage/schema.spec.ts --reporter=dot</verify>
  <acceptance_criteria>
    - `src/core/storage/types.ts` contains `export interface SessionEventRecord`
    - `src/core/storage/types.ts` contains `export interface MemoryRecord`
    - `src/core/storage/sessionEventsRepo.ts` contains `insertSessionEvent`
    - `src/core/storage/memoryRepo.ts` contains `upsertSessionSummaryMemory`
    - `src/core/storage/memoryRepo.ts` contains `importance < 1 || importance > 10`
  </acceptance_criteria>
  <done>Storage repositories support CAPT-01 event capture and CAPT-03 memory persistence semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmd /c npx vitest run tests/integration/storage/schema.spec.ts --reporter=dot`
- [ ] `cmd /c npx vitest run --reporter=dot`
- [ ] Migration runner applies clean DB without SQL errors
</verification>

<success_criteria>

- All tasks completed
- Storage layer persists phase-1 event and memory records with required provenance
- Migration + index setup reproducible from clean state
  </success_criteria>

<output>
After completion, create `.planning/phases/01-core-memory-engine-foundation/01-01-SUMMARY.md`
</output>

