---
phase: 06-security-privacy-and-retention-hardening
plan: 07
type: execute
wave: 4
depends_on: ["06-03", "06-05"]
files_modified:
  - docs/privacy-and-retention.md
  - tests/integration/docs/privacy-docs.spec.ts
autonomous: true
requirements: [SECU-01, SECU-02]
must_haves:
  truths:
    - "A user-facing doc explains the secret-redaction behavior: which secret categories are redacted, that redaction runs on all write paths, and the redactionEnabled flag with its default"
    - "The doc explains the retention policy: configurable retentionDays, created_at age basis, 90-day default, 0/-1 to disable, hard-delete (export first to retain)"
    - "The doc documents the CLI surface: retention prune [--force], config get/set, redact-scan [--apply], and the stats retention/redaction lines"
    - "The doc explains config.json location, precedence (CLI > config.json > default), and install/uninstall behavior"
  artifacts:
    - path: "docs/privacy-and-retention.md"
      provides: "User documentation for redaction + retention + policy config"
      contains: "Retention"
  key_links:
    - from: "docs/privacy-and-retention.md"
      to: "the Phase 6 CLI commands"
      via: "documents retention prune / config / redact-scan / stats lines"
      pattern: "retention prune|redact-scan|config set"
---

<objective>
Document the Phase 6 privacy and retention controls for users (ROADMAP success criterion 4, PROJECT.md security/privacy documentation requirement): a `docs/privacy-and-retention.md` covering redaction coverage and the redactionEnabled flag, the retention policy and its configuration, the CLI commands (`retention prune`, `config get/set`, `redact-scan`, `stats` additions), and the `config.json` surface with precedence rules.

Purpose: Make redaction behavior and retention policy discoverable and trustworthy. Mirrors the existing docs/cloud-summarization.md style.
Output: `docs/privacy-and-retention.md` and a lightweight doc-presence test asserting the required topics are covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md
@docs/cloud-summarization.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-01-PLAN.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-05-PLAN.md

<interfaces>
Behaviors to document (final, from the implemented plans):
- Redaction categories (Plan 01 D-05): email, sk- API keys, AWS access keys, GitHub tokens, Bearer tokens, PEM private key blocks, password=/secret= assignments, JWTs -> REDACTED placeholders.
- Redaction runs on all write paths (Plan 03 D-06): auto-summarize, manual store, import; governed by redactionEnabled (default on).
- Retention (Plan 02 D-01/D-03/D-04): memories table only, created_at basis, default 90 days, 0/-1 disables, hard-delete (no archive) — export first to retain.
- Auto-prune at session-end (Plan 04 D-02), light + non-blocking.
- CLI (Plans 05/06): `sessionmem retention prune [--force]` (dry-run default), `sessionmem config get <key>` / `config set <key> <value>`, `sessionmem redact-scan [--apply]`, `sessionmem stats` retention/redaction lines.
- config.json (Plan 01/05 D-09/D-10/D-11): ~/.sessionmem/config.json, keys retentionDays/redactionEnabled, precedence CLI flag > config.json > default; install writes defaults if absent, uninstall preserves unless --purge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/privacy-and-retention.md</name>
  <files>docs/privacy-and-retention.md</files>
  <read_first>
    - docs/cloud-summarization.md (existing doc tone, heading style, structure to match)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md (authoritative decisions D-01..D-15 for accurate documentation)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-01-PLAN.md (redaction categories + config keys/precedence)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-05-PLAN.md (CLI command names + exact dry-run output strings)
  </read_first>
  <action>Create docs/privacy-and-retention.md matching the heading/section style of docs/cloud-summarization.md. Include sections: (1) Secret Redaction — list the redacted categories from D-05, state redaction runs on all write paths (auto-summarize, manual store, import) per D-06, document the redactionEnabled flag and its default of on, and the redaction_partial_failure warning code. (2) Retention Policy — explain retentionDays (default 90), created_at age basis, that 0 or -1 disables pruning, that pruning is a hard-delete of the memories table only (session_events/feedback untouched), and the guidance to run sessionmem export first to retain data (D-01/D-03/D-04). (3) Automatic vs Manual Pruning — session-end light auto-prune (D-02) plus the manual sessionmem retention prune [--force] dry-run-by-default command, quoting the exact "Would delete N memories older than {retentionDays} days. Pass --force to confirm." string. (4) One-time Scrub — sessionmem redact-scan [--apply] for existing data (D-07/D-14). (5) Policy Config — ~/.sessionmem/config.json keys (retentionDays, redactionEnabled), precedence CLI flag > config.json > default (D-11), config get/set commands (D-13), install writes defaults if absent and uninstall preserves unless --purge (D-10). (6) Stats Visibility — note the stats retention + redaction lines (D-15). Keep wording user-facing and accurate to the implemented behavior; do not invent settings not in CONTEXT.md.</action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/privacy-and-retention.md','utf8'); for (const k of ['Redaction','Retention','retentionDays','redactionEnabled','retention prune','redact-scan','config set','config.json','--force','--apply']) { if(!t.includes(k)) { console.error('MISSING: '+k); process.exit(1);} } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - docs/privacy-and-retention.md exists and contains sections covering Secret Redaction, Retention Policy, and Policy Config
    - The file mentions all redaction categories named in D-05 (at minimum: email, AWS, GitHub, Bearer, private key, JWT, sk-)
    - The file contains the literal CLI tokens: "retention prune", "redact-scan", "config set", "config get", "stats", "--force", "--apply"
    - The file states the 90-day default, the created_at basis, the disable values (0/-1), and that pruning is a hard-delete (with export-first guidance)
    - The file documents precedence: CLI flag > config.json > default
  </acceptance_criteria>
  <done>docs/privacy-and-retention.md authored covering redaction, retention, CLI commands, config.json, and stats visibility accurately per CONTEXT decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Doc-coverage assertion test</name>
  <files>tests/integration/docs/privacy-docs.spec.ts</files>
  <read_first>
    - docs/privacy-and-retention.md (the doc authored in Task 1)
    - tests/unit/cli/stats.spec.ts (vitest style reference in this repo)
  </read_first>
  <behavior>
    - The test fails if docs/privacy-and-retention.md is missing
    - The test fails if any required topic token is absent (redaction categories, retention defaults, CLI command names, config precedence)
  </behavior>
  <action>Create tests/integration/docs/privacy-and-retention coverage test (tests/integration/docs/privacy-docs.spec.ts) that reads docs/privacy-and-retention.md and asserts presence of required substrings: "redactionEnabled", "retentionDays", "90", "retention prune", "redact-scan", "config set", "config get", "config.json", "--force", "--apply", and at least the redaction category names (AWS, GitHub, JWT, private key, Bearer). Use vitest (describe/it/expect) reading the file via fs.readFileSync. This guards the docs against drifting out of sync with the shipped commands (it is a doc-presence smoke test, not a prose-quality judge).</action>
  <verify>
    <automated>npx vitest run tests/integration/docs/privacy-docs.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - The spec reads docs/privacy-and-retention.md and asserts each required substring listed in the action is present
    - The spec passes against the Task 1 doc
    - Removing any documented command token from the doc would make the spec fail (the assertions reference exact tokens)
  </acceptance_criteria>
  <done>privacy-docs.spec.ts asserts the doc covers redaction categories, retention defaults, CLI commands, and config precedence; passes against the authored doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> user understanding | inaccurate docs could mislead users about whether secrets are protected or data is retained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-24 | Repudiation / misinformation | docs claiming protection that does not match behavior | mitigate | Doc authored directly from CONTEXT.md decisions and the implemented plans; doc-coverage test asserts the documented command tokens exist, keeping docs aligned with shipped CLI |
| T-06-25 | Information Disclosure | doc accidentally encouraging unsafe config | accept | Doc states redaction default-on and export-before-prune guidance; low residual risk, no executable surface |
</threat_model>

<verification>
- `npx vitest run tests/integration/docs/privacy-docs.spec.ts --reporter=dot` passes
- The doc verify node script in Task 1 exits 0
- Full suite green: `npm test`
</verification>

<success_criteria>
- Redaction behavior and retention policy are documented for users (ROADMAP success criterion 4)
- Documentation covers all Phase 6 CLI commands and the config.json surface with precedence
- A doc-coverage test guards against documentation drift
</success_criteria>

<output>
Create `.planning/phases/06-security-privacy-and-retention-hardening/06-07-SUMMARY.md` when done
</output>
