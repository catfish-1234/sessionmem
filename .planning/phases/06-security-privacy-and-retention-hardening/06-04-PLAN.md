---
phase: 06-security-privacy-and-retention-hardening
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/core/api/sessionLifecycleService.ts
  - tests/integration/core/session-end-auto-prune.spec.ts
autonomous: true
requirements: [SECU-01]
must_haves:
  truths:
    - "Session-end lifecycle runs a light retention prune check after summarization, using the effective retentionDays"
    - "The auto-prune is applied (hard-delete), not dry-run, and does not block or fail summarization if pruning errors"
    - "retentionDays<=0 (disabled) skips pruning during session-end"
    - "Summarization result (status/usedMode/memoryId) is unchanged by adding the prune step"
  artifacts:
    - path: "src/core/api/sessionLifecycleService.ts"
      provides: "Post-summarize light prune call gated on retentionDays"
      contains: "deleteMemoriesOlderThan"
  key_links:
    - from: "src/core/api/sessionLifecycleService.ts"
      to: "memoryRepo.deleteMemoriesOlderThan"
      via: "post-summarize prune call wrapped in try/catch so failures never block summarization"
      pattern: "deleteMemoriesOlderThan"
---

<objective>
Wire the automatic light retention prune into the session-end lifecycle (D-02): after summarization completes, run a prune against the effective `retentionDays`, applied (not dry-run), wrapped so a prune failure never blocks or fails summarization. Pruning is skipped when `retentionDays<=0`.

Purpose: SECU-01 — dual-trigger pruning (automatic at session-end + manual CLI in Plan 05). This plan delivers the automatic trigger.
Output: Prune step in `sessionLifecycleService.handleSessionEnd` + integration test. Touches only `sessionLifecycleService.ts` to remain parallel-safe with Plan 03 in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-02-PLAN.md

<interfaces>
From src/core/api/sessionLifecycleService.ts (modify handleSessionEnd; it already returns HandleSessionEndResponse and has access to deps.db, request.projectId, request.config):
```typescript
async function handleSessionEnd(request: HandleSessionEndRequest): Promise<HandleSessionEndResponse>;
// stores the summary then returns { ok, status, usedMode, warningCodes, warningMessages, memoryId? }
```

From src/core/storage/memoryRepo.ts (added in Plan 02):
```typescript
export function deleteMemoriesOlderThan(db: Database, projectId: string, cutoffIso: string): number;
```

From src/core/config/policyConfig.ts (added in Plan 01):
```typescript
export const DEFAULT_POLICY_CONFIG; // retentionDays 90, redactionEnabled true
export function readPolicyConfig(filePath: string);
export function configFilePath(): string;
```

NOTE: handleSessionEndConfigSchema (contracts.ts) does NOT currently carry retentionDays. Do not add it to that schema here (avoid contracts.ts overlap with Plan 03). Resolve retentionDays from policyConfig (readPolicyConfig(configFilePath())) with the built-in default as fallback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add post-summarize light prune to handleSessionEnd</name>
  <files>src/core/api/sessionLifecycleService.ts, tests/integration/core/session-end-auto-prune.spec.ts</files>
  <read_first>
    - src/core/api/sessionLifecycleService.ts (handleSessionEnd flow, where each storeSummaryMemory call happens and where responses return)
    - src/core/storage/memoryRepo.ts (deleteMemoriesOlderThan from Plan 02, created_at ISO format)
    - src/core/config/policyConfig.ts (readPolicyConfig, configFilePath, DEFAULT_POLICY_CONFIG from Plan 01)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md §D-02, §D-03, §D-04 (light prune at session-end, retentionDays disable semantics, hard-delete)
  </read_first>
  <behavior>
    - After a successful (or skipped) session-end, memories older than retentionDays are hard-deleted for that project
    - retentionDays resolved from policy config (default 90) when not otherwise supplied
    - retentionDays<=0 skips the prune entirely
    - A thrown error inside the prune step does not change the summarization response (status/usedMode/memoryId preserved) and does not propagate
    - The prune runs once per session-end (light), not in a loop
  </behavior>
  <action>In sessionLifecycleService.ts, add a runLightPrune(projectId) helper that resolves effective retentionDays via readPolicyConfig(configFilePath()).retentionDays (falling back to DEFAULT_POLICY_CONFIG), returns early when retentionDays<=0, computes cutoffIso = now minus retentionDays days in the same ISO-8601 UTC format as created_at, and calls deleteMemoriesOlderThan(deps.db, projectId, cutoffIso) inside a try/catch that swallows/logs errors so summarization is never blocked (D-02 "light, doesn't block summarization"). Invoke runLightPrune(request.projectId) just before each successful return path in handleSessionEnd (stored / fallback-stored / skipped paths) — Claude's discretion on exact placement and on a throttle (D-02 allows every session-end or throttled) as long as it stays light and non-blocking. Do NOT modify handleSessionEndConfigSchema or contracts.ts. Optionally allow a deps.now seam or accept a test injection for retentionDays/cutoff so the integration test can deterministically age memories — keep production behavior reading policyConfig.</action>
  <verify>
    <automated>npx vitest run tests/integration/core/session-end-auto-prune.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - With a config (or injected setting) of retentionDays=90 and a seeded 200-day-old memory plus fresh session events, calling handleSessionEnd hard-deletes the old memory (listMemoriesByProject no longer contains it) while the new summary memory persists
    - With retentionDays=0, the 200-day-old memory survives handleSessionEnd (prune skipped)
    - The handleSessionEnd response status/usedMode/memoryId are identical with and without an old memory present (prune does not alter the summary outcome)
    - When deleteMemoriesOlderThan is forced to throw (mock), handleSessionEnd still returns ok:true with the normal stored/skipped status (failure swallowed, summarization unaffected)
  </acceptance_criteria>
  <done>handleSessionEnd performs a light, non-blocking, applied prune gated on effective retentionDays; disabled when retentionDays<=0; summarization outcome unchanged; session-end-auto-prune.spec.ts passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy config -> session-end prune | effective retentionDays drives an automatic hard-delete during the lifecycle |
| lifecycle -> SQLite | irreversible deletion runs inside an automated, unattended flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Denial of Service / data loss | unattended auto-prune deleting wrong/too many rows | mitigate | Reuses Plan 02 deleteMemoriesOlderThan (project + created_at scoped, parameterized); retentionDays<=0 disables; integration test proves only aged rows deleted and summary preserved |
| T-06-14 | Availability | prune failure blocking summarization | mitigate | Prune wrapped in try/catch that swallows errors; test asserts handleSessionEnd still returns ok on prune throw (D-02 non-blocking) |
| T-06-15 | Tampering | config-driven cutoff | mitigate | retentionDays sourced from validated policyConfig (zod, safe default) from Plan 01; malformed config falls back to 90-day default, never to a delete-everything cutoff |
</threat_model>

<verification>
- `npx vitest run tests/integration/core/session-end-auto-prune.spec.ts --reporter=dot` passes
- Existing lifecycle tests still pass: `npx vitest run tests/integration/core/session-lifecycle-summary.spec.ts tests/integration/core/summarization-retry-failure.spec.ts --reporter=dot`
- `npx tsc --noEmit` reports no type errors
</verification>

<success_criteria>
- Automatic light retention prune runs at session-end, applied, gated on effective retentionDays, non-blocking
- Summarization behavior and response shape are unchanged by the added prune step
- Pruning disabled when retentionDays<=0
</success_criteria>

<output>
Create `.planning/phases/06-security-privacy-and-retention-hardening/06-04-SUMMARY.md` when done
</output>
