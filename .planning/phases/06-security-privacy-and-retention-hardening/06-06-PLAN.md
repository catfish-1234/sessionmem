---
phase: 06-security-privacy-and-retention-hardening
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-05"]
files_modified:
  - src/cli/commands/redactScan.ts
  - src/cli/commands/stats.ts
  - src/cli/index.ts
  - tests/integration/cli/redact-scan.spec.ts
  - tests/unit/cli/stats.spec.ts
  - tests/integration/core/secret-leakage.spec.ts
autonomous: true
requirements: [SECU-01, SECU-02]
must_haves:
  truths:
    - "`sessionmem redact-scan` defaults to a scan that prints 'Found N memories with potential secrets' with truncated previews and deletes/updates nothing"
    - "`sessionmem redact-scan --apply` redacts matching stored memories in place and prints a summary count"
    - "`sessionmem stats` shows a retention line ('Retention: {days} days (M memories eligible for pruning)') and a redaction line ('Redaction: enabled|disabled')"
    - "Security tests cover common secret-pattern leakage scenarios across write paths and confirm raw secrets never persist when redaction is enabled"
  artifacts:
    - path: "src/cli/commands/redactScan.ts"
      provides: "redact-scan command (scan default, --apply)"
      contains: "Found"
    - path: "src/cli/commands/stats.ts"
      provides: "retention + redaction summary lines"
      contains: "Retention"
    - path: "tests/integration/core/secret-leakage.spec.ts"
      provides: "secret-pattern leakage coverage across paths"
      contains: "REDACTED"
  key_links:
    - from: "src/cli/commands/redactScan.ts"
      to: "service.call('redactExisting', ...)"
      via: "scan uses apply:false, --apply uses apply:true"
      pattern: "redactExisting"
    - from: "src/cli/commands/stats.ts"
      to: "src/core/config/policyConfig.ts + service pruneMemories(dryRun)"
      via: "read retentionDays/redactionEnabled and count eligible via pruneMemories dry-run"
      pattern: "Retention|Redaction"
---

<objective>
Complete the CLI surface and verification: a `redact-scan [--apply]` command over `redactExisting` (D-07/D-14), retention + redaction summary lines added to `stats` (D-15), and a dedicated security test suite covering common secret-pattern leakage across all write paths (ROADMAP success criterion 3).

Purpose: SECU-02 one-time scrub UX + policy visibility in `stats`, and the security test coverage that proves redaction holds. Mirrors Phase 5 scan/`--apply` and summary-count conventions.
Output: `redactScan.ts`, modified `stats.ts`, registration in `index.ts`, `redact-scan` + `stats` + `secret-leakage` tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-03-PLAN.md
@.planning/phases/06-security-privacy-and-retention-hardening/06-05-PLAN.md

<interfaces>
From src/core/api/memoryCoreService.ts (Plan 03): service.call("redactExisting", { projectId, apply }) -> { ok, scanned, matched, updated, previews }.
From src/core/api/memoryCoreService.ts (Plan 02): service.call("pruneMemories", { projectId, retentionDays, dryRun:true }) -> { ok, eligible } (reused to count eligible for stats line).
From src/core/config/policyConfig.ts (Plan 01): readPolicyConfig, configFilePath (retentionDays + redactionEnabled for stats lines).

From src/cli/commands/stats.ts (extend in place — current output via process.stdout.write):
```typescript
export async function statsCommand(ctx?: CliContext): Promise<void>;
// currently prints: "memories: N\n db_size_bytes: B\n total_content_tokens: T\n"
```

Command + registration seam: forget.ts (ctx?: CliContext) + index.ts arrow-wrap NOTE. NOTE: stats.spec.ts asserts on the existing "memories:" / "db_size_bytes:" / "total_content_tokens:" output — preserve those exact lines and append the new ones.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: redact-scan command (scan default, --apply)</name>
  <files>src/cli/commands/redactScan.ts, src/cli/index.ts, tests/integration/cli/redact-scan.spec.ts</files>
  <read_first>
    - src/cli/commands/forget.ts (dry-run-by-default + ctx seam pattern)
    - src/cli/commands/import.ts (existing --apply/--merge-style summary-count output to mirror)
    - src/cli/index.ts (registration + arrow-wrap NOTE)
    - src/core/api/memoryCoreService.ts (redactExisting request/response from Plan 03)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md §D-07, §D-14 (output string, dry-run default, --apply in place)
  </read_first>
  <behavior>
    - `redact-scan` (no flags) prints "Found N memories with potential secrets" plus truncated previews and updates nothing
    - `redact-scan --apply` redacts matching memories in place and prints a summary count (e.g. "Redacted M memories.")
    - scan with no matches prints "Found 0 memories with potential secrets" and no previews
    - service failure prints error to stderr and exits 1
    - scan (no --apply) never calls redactExisting with apply:true
  </behavior>
  <action>Create src/cli/commands/redactScan.ts exporting redactScanCommand(options: { apply?: boolean }, ctx?: CliContext) mirroring forget.ts: call service.call("redactExisting", { projectId, apply: !!options.apply }); on the scan (apply false) result print exactly "Found ${matched} memories with potential secrets" followed by the previews (truncated, never echoing a full raw secret per D-07/D-14) and return; on --apply print a summary count line "Redacted ${updated} memories."; on !result.ok print result.error.message to console.error and process.exit(1). Register a "redact-scan" command in index.ts with an --apply option, arrow-wrapping the action per the NOTE. console.log/console.error, no ANSI color.</action>
  <verify>
    <automated>npx vitest run tests/integration/cli/redact-scan.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - With a seeded memory whose content contains a raw "sk-abcdefghijkl", redactScanCommand({}, ctx) prints a line matching /Found 1 memories with potential secrets/ and getMemory still shows the raw key (scan is non-destructive)
    - redactScanCommand({ apply: true }, ctx) prints a line containing "Redacted 1" and getMemory now shows "[REDACTED_API_KEY]"
    - With no secret-bearing memories, redactScanCommand({}, ctx) prints /Found 0 memories with potential secrets/
    - Printed previews do not contain the full raw secret string
    - On forced service failure the command calls console.error and process.exit(1)
    - index.ts registers a "redact-scan" command exposing --apply
  </acceptance_criteria>
  <done>redact-scan command implemented with scan default and --apply in-place scrub, exact D-14 output, registered in index.ts; redact-scan.spec.ts passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stats retention + redaction summary lines</name>
  <files>src/cli/commands/stats.ts, tests/unit/cli/stats.spec.ts</files>
  <read_first>
    - src/cli/commands/stats.ts (current output lines via process.stdout.write — must be preserved)
    - tests/unit/cli/stats.spec.ts (existing assertions on memories:/db_size_bytes:/total_content_tokens: that must still pass)
    - src/core/config/policyConfig.ts (readPolicyConfig, configFilePath for retentionDays/redactionEnabled)
    - src/core/api/memoryCoreService.ts (pruneMemories dry-run to count eligible)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md §D-15 (exact stats line formats)
  </read_first>
  <behavior>
    - stats output still contains the original memories:/db_size_bytes:/total_content_tokens: lines
    - stats appends "Retention: {days} days ({M} memories eligible for pruning)" using effective retentionDays and a dry-run eligible count
    - stats appends "Redaction: enabled" or "Redaction: disabled" from effective redactionEnabled
    - when retentionDays<=0 the retention line indicates pruning disabled
  </behavior>
  <action>Extend statsCommand in stats.ts: after the existing process.stdout.write of memories/db_size_bytes/total_content_tokens, read effective policy via readPolicyConfig(configFilePath()) (with a config-path override seam for tests), obtain the eligible count via service.call("pruneMemories", { projectId, retentionDays, dryRun: true }).eligible, and append two lines: "Retention: ${retentionDays} days (${eligible} memories eligible for pruning)" (or a disabled variant when retentionDays<=0) and "Redaction: ${redactionEnabled ? "enabled" : "disabled"}" per D-15. Keep the original three lines verbatim so existing stats.spec.ts assertions pass. Use the same process.stdout.write/console.log channel already used.</action>
  <verify>
    <automated>npx vitest run tests/unit/cli/stats.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - stats output still contains "memories:", "db_size_bytes:", and "total_content_tokens:" (existing assertions unchanged)
    - stats output contains a line matching /Retention: \d+ days \(\d+ memories eligible for pruning\)/
    - stats output contains "Redaction: enabled" by default, and "Redaction: disabled" when config sets redactionEnabled=false
    - with retentionDays=0 in config, the retention line conveys pruning is disabled
  </acceptance_criteria>
  <done>stats appends D-15 retention + redaction summary lines while preserving the original three lines; stats.spec.ts (old + new assertions) passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: secret-leakage security test suite</name>
  <files>tests/integration/core/secret-leakage.spec.ts</files>
  <read_first>
    - src/core/summarize/redaction.ts (the full rule set from Plan 01 — categories to exercise)
    - src/core/api/memoryCoreService.ts (storeMemory/importMemories redaction from Plan 03, redactExisting)
    - src/core/summarize/localSummarizer.ts (auto-summarize redaction path)
    - tests/helpers/cliTestContext.ts (createTestCliContext seam for seeding/asserting)
    - .planning/phases/06-security-privacy-and-retention-hardening/06-CONTEXT.md §D-05, §D-06 (categories + all-paths coverage; ROADMAP success criterion 3)
  </read_first>
  <behavior>
    - For each D-05 category (email, sk- key, AWS key, GitHub token, Bearer token, private key block, password=/secret= assignment, JWT): the raw secret never appears in stored content when redactionEnabled is on
    - Coverage spans all write paths: manual storeMemory, importMemories, and auto-summarize (localSummarizer/handleSessionEnd)
    - redactExisting scrubs a pre-seeded raw secret so it no longer persists
    - With redactionEnabled off, the raw value persists (proves the flag governs behavior, no accidental always-on)
  </behavior>
  <action>Create tests/integration/core/secret-leakage.spec.ts exercising every D-05 secret category through each write path. For storeMemory and importMemories, seed content containing each category's sample secret with redactionEnabled true and assert getMemory/listMemories content contains the category placeholder and does NOT contain the raw secret substring. For the auto-summarize path, drive handleSessionEnd (or summarizeLocalSessionEvents) with events containing secrets and assert the stored summary omits the raw secret. Add a redactExisting case: seed a raw-secret memory directly, run redactExisting apply:true, assert the raw secret is gone. Add a redactionEnabled:false case asserting the raw value persists on storeMemory. Use table-driven cases (one assertion block per category) and the createTestCliContext / a fresh service over a temp DB. This is a test-only file — no production code changes; if a category leaks, the failure must be fixed in Plan 01/03 (do not weaken the test).</action>
  <verify>
    <automated>npx vitest run tests/integration/core/secret-leakage.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - A parameterized case exists for each of the 8 categories (email, sk-, AWS, GitHub, Bearer, private key, password/secret assignment, JWT)
    - For every category, after storeMemory with redactionEnabled true, the stored content does NOT contain the raw secret and DOES contain a REDACTED placeholder
    - importMemories path asserts the same for at least the sk-/email/AWS categories
    - auto-summarize path asserts a secret-bearing event does not leak the raw secret into the stored summary
    - redactExisting apply:true removes a pre-seeded raw secret (assert absent afterward)
    - storeMemory with redactionEnabled:false preserves the raw secret (negative control)
  </acceptance_criteria>
  <done>secret-leakage.spec.ts covers all 8 categories across manual/import/auto-summarize/redactExisting paths plus a disabled-flag negative control; suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> redact-scan | --apply triggers in-place rewrite of stored rows |
| stats -> policy config + DB | reads policy + counts eligible rows for visibility |
| test corpus -> all write paths | secret-bearing inputs probe every persistence path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Information Disclosure | redact-scan preview leaking secrets | mitigate | redactExisting (Plan 03) returns truncated previews; command prints them as-is; acceptance asserts no full raw secret in preview output |
| T-06-21 | Information Disclosure | undetected secret-pattern leakage | mitigate | Task 3 secret-leakage suite asserts every D-05 category is redacted across all write paths — the verification that satisfies ROADMAP criterion 3 |
| T-06-22 | Tampering | redact-scan modifying data without intent | mitigate | Scan (no --apply) is non-destructive default (D-14); apply required for writes; test asserts scan leaves rows intact |
| T-06-23 | Repudiation | stats misreporting policy state | accept | stats reads validated policyConfig and a dry-run count; low risk, no enforcement action driven by the display |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; no legitimacy gate required |
</threat_model>

<verification>
- `npx vitest run tests/integration/cli/redact-scan.spec.ts tests/unit/cli/stats.spec.ts tests/integration/core/secret-leakage.spec.ts --reporter=dot` passes
- `npx vitest run tests/integration/cli/cli-entrypoint.spec.ts --reporter=dot` passes (registration sanity)
- Full suite green: `npm test`
- `npx tsc --noEmit` reports no type errors
</verification>

<success_criteria>
- redact-scan (scan default + --apply) and stats retention/redaction lines complete the SECU-01/SECU-02 CLI visibility
- Security test suite covers all common secret-pattern leakage scenarios across every write path (ROADMAP success criterion 3)
- No regression to existing stats output
</success_criteria>

<output>
Create `.planning/phases/06-security-privacy-and-retention-hardening/06-06-SUMMARY.md` when done
</output>
