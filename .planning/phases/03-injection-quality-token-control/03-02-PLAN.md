---
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified: ["src/core/injection/tokenBudget.ts", "src/core/injection/formatStartupInjection.ts", "tests/unit/injection/token-budget.spec.ts", "tests/unit/injection/format-startup-injection.spec.ts"]
autonomous: true
requirements: ["RETR-03"]
---

# Plan 02: Injection Formatter & Token Budget

<task>
<read_first>
- `src/core/retrieve/retrieveMemories.ts`
- `src/core/api/contracts.ts`
</read_first>
<action>
1. Create `src/core/injection/tokenBudget.ts`. Initialize `js-tiktoken` with `getEncoding("o200k_base")`. Implement `countTokens(text)` and `trimLowestPriorityContent(included, options)` helpers.
2. Create `src/core/injection/formatStartupInjection.ts`. Implement `formatStartupInjection(rankedMemories, options)`. 
   - Group memories by type (`warning`, `decision`, `fact`, `summary`, `preference`, then fallback lexicographic).
   - Add fixed header `Relevant prior context`.
   - Format each with bullet, content, score breakdown, source adapter, date.
   - Run the token counting loop: while final string exceeds `tokenCap` (default 450), trim lower-priority content first, then drop lower-priority non-warning entries if needed. Always preserve critical warnings (even if exceeding cap).
</action>
<acceptance_criteria>
- `src/core/injection/tokenBudget.ts` exports `countTokens` and uses `js-tiktoken`.
- `src/core/injection/formatStartupInjection.ts` exports `formatStartupInjection`.
- Output starts with `Relevant prior context`.
- `tests/unit/injection/token-budget.spec.ts` passes testing token limits and trim ordering.
- `tests/unit/injection/format-startup-injection.spec.ts` passes with inline snapshots for output deterministic generation.
</acceptance_criteria>
</task>
