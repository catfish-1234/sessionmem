---
wave: 1
depends_on: []
files_modified: ["src/core/schema/migrations/004_memory_feedback.sql", "package.json", "src/core/api/contracts.ts", "src/core/storage/memoryRepo.ts", "src/core/storage/memoryFeedbackRepo.ts", "src/core/api/memoryCoreService.ts", "tests/integration/core/memory-feedback.spec.ts", "tests/unit/retrieve/importance-decay.spec.ts", "tests/integration/retrieve/retrieve-on-demand.spec.ts", "tests/integration/retrieve/retrieve-score-metadata.spec.ts"]
autonomous: true
requirements: ["RETR-04", "RETR-05"]
---

# Plan 01: Core Feedback, Migration, & Retrieval Extensions

<task>
<read_first>
- `src/core/schema/migrations/001_initial.sql`
- `src/core/api/contracts.ts`
</read_first>
<action>
1. Add `js-tiktoken` to dependencies using `npm install js-tiktoken`.
2. Create `src/core/schema/migrations/004_memory_feedback.sql`. Define table `memory_feedback` with `id`, `memory_id` (foreign key to `memories.id`), `feedback_type` (e.g. 'auto_use', 'manual'), `previous_importance`, `new_importance`, and `created_at`.
3. Create `src/core/storage/memoryFeedbackRepo.ts` with `insertMemoryFeedbackEvent(db, event)` logic.
4. Update `src/core/storage/memoryRepo.ts` to add `updateMemoryImportance(db, projectId, memoryId, nextImportance, usedAt)` and a transactional helper `recordUse(db, input)`. The transaction must bound importance to max 9 for auto path, update the memory, and insert the feedback event.
5. Create `src/core/retrieve/decay.ts` with a helper `decayOldBoosts(memories, now)` that deterministically lowers auto boosts older than a threshold (e.g. 7 days) returning the adjusted scores.
6. In `src/core/api/contracts.ts`, extend `retrieveMemoriesRequestSchema` with `mode: z.enum(["auto", "on-demand"]).default("auto")` and `depth: z.enum(["default", "deep"]).default("default")`. Add `score` and `semantic` to the output DTO schema.
7. In `src/core/api/memoryCoreService.ts`, add `recordMemoryUsed` to the service boundary which validates input and calls the storage transaction. Update retrieval mapping to include `score` and `semantic` using `toRetrievedMemoryDto` mapping.
</action>
<acceptance_criteria>
- `package.json` contains `js-tiktoken` dependency.
- `004_memory_feedback.sql` defines `memory_feedback` table.
- `src/core/storage/memoryFeedbackRepo.ts` exports `insertMemoryFeedbackEvent`.
- `src/core/storage/memoryRepo.ts` exports `updateMemoryImportance`.
- `src/core/retrieve/decay.ts` exports `decayOldBoosts`.
- `tests/integration/core/memory-feedback.spec.ts` passes asserting bounded importance boost to 9 and history persistence.
- `tests/unit/retrieve/importance-decay.spec.ts` passes.
- `tests/integration/retrieve/retrieve-on-demand.spec.ts` passes asserting deep mode.
- `tests/integration/retrieve/retrieve-score-metadata.spec.ts` passes asserting score inclusion.
</acceptance_criteria>
</task>
