---
wave: 3
depends_on: ["03-02-PLAN.md"]
files_modified: ["tests/quality/injection/injection-quality-harness.spec.ts"]
autonomous: true
requirements: ["RETR-03", "RETR-04"]
---

# Plan 03: Quality Harness

<task>
<read_first>
- `tests/unit/injection/format-startup-injection.spec.ts`
</read_first>
<action>
1. Create `tests/quality/injection/injection-quality-harness.spec.ts`.
2. Define realistic coding-session memory fixtures and synthetic edge-case fixtures.
3. Write benchmark tests that invoke `formatStartupInjection` and verify:
   - Expected top relevant memories appear.
   - The output string uses exactly `<= 450` tokens (via `countTokens`).
   - Identical inputs produce exact same output snapshots.
   - The relevance quality satisfies the expectations defined in the tests.
</action>
<acceptance_criteria>
- `tests/quality/injection/injection-quality-harness.spec.ts` runs without errors.
- Test suite verifies token budget compliance on complex fixtures.
- Test suite uses `toMatchInlineSnapshot` or `toMatchFileSnapshot` to prove deterministic output stability.
</acceptance_criteria>
</task>
