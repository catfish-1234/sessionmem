import { it } from "vitest";

it.todo("installCommand calls adapter.install() and prints success message on success");
it.todo("installCommand prints manual fallback block when adapter has no install() capability");
it.todo("installCommand prints manual fallback block when adapter.install() returns false");
