import { it } from "vitest";

it.todo("forgetCommand without --force performs dry-run: prints preview and does NOT delete the memory");
it.todo("forgetCommand with --force deletes the memory and prints confirmation");
it.todo("forgetCommand exits non-zero when memory ID is not found");
