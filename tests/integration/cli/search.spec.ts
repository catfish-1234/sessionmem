import { it } from "vitest";

it.todo("searchCommand prints a table of matching memories ranked by relevance");
it.todo("searchCommand respects --limit flag to constrain result count");
it.todo("searchCommand prints empty table when no memories match the query");
it.todo("searchCommand exits non-zero on service error");
