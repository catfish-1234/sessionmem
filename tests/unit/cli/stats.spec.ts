import { it } from "vitest";

it.todo("statsCommand prints memories count, db_size_bytes, and total_content_tokens for the project");
it.todo("statsCommand exits non-zero and prints error message on service failure");
